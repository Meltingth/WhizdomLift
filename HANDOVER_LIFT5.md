# ส่งงานให้เครื่องหน้าตู้ — Lift 5 (12 ก.ย. 2026)

เขียนจากเครื่อง Gateway ผ่าน repo เพราะช่องส่งข้อความตรงใช้ไม่ได้แล้วใน session นี้
**อ่านทั้งไฟล์ก่อนเริ่ม** มีข้อที่ถอนคำสรุปเดิมอยู่ด้วย

---

## งานของคุณ

**เขียน firmware ให้ Lift 5 แล้วทดสอบกับลิฟต์จริง** — Lift 5 คือ *lift service*

**⛔ Lift 4 ข้ามไปก่อน — ปิดใช้งานอยู่** (ผู้ใช้แจ้ง 12 ก.ย.)

### คำสั่ง build ที่ต้องใช้ — ขาดอย่างใดอย่างหนึ่งไม่ได้

```
arduino-cli compile --fqbn arduino:avr:mega:cpu=atmega2560 --warnings all \
  --export-binaries --build-property build.extra_flags=-DLIFT_ID=5 IODebug
python check_firmware_hex.py
```

- **ขาด `-DLIFT_ID=5`** → บอร์ดประกาศ `LIFT=0` ⇒ `port_resolver.py` หาไม่เจอ และ
  `log_lift.py` จะยอมบันทึกโดยไม่มีอะไรยืนยันว่าเป็นลิฟต์ไหน
- **ขาด `--export-binaries`** → `IODebug/build/` ไม่ถูกแตะ hex ที่ track ไว้ค้างเวอร์ชันเก่า

> ⚠️ **เลข 5 ยังไม่เคยยืนยันกับอาคาร** — มาจากการตัดตัวเลือก (1,2,3,4 ถูกระบุแล้ว เหลือ 5)
> ถามให้แน่ก่อนแฟลช เพราะเลขนี้ฝังในไบนารีและแก้ทีหลังต้องเข้าห้องเครื่องซ้ำ

---

## 🔴 ถอนคำสรุปเดิมเรื่อง SAFETY — อย่าใช้ต่อ

ผมเคยรายงานจากข้อมูล 5.8 วันว่า:

- *"7 ก.ย. Lift 1 หยุดให้บริการ 41 นาทีที่ชั้น 4 ควรถามผู้ดูแลว่ามีบันทึกงานไหม"*
- *"9 ก.ย. เห็นร่องรอยช่างเข้าบำรุงรักษาทั้งสามตัว"*
- *"§9 ข้อ 6 ปิดแล้ว — SAFETY ที่เปิดสั้น ๆ เป็น timer 3 นาที"*

**ผู้ใช้ยืนยันว่ายังไม่เคยทดสอบ SAFETY กับลิฟต์จริงเลย ข้อมูลที่เห็นเกิดจากการทดสอบกับบอร์ด**
⇒ ทั้งสามข้อถูกถอน และ §9 ข้อ 6 เปิดกลับแล้ว

**สาเหตุที่ผมพลาด**: ข้อมูลบอกได้แค่ว่า **ขา D17 อ่านได้ HIGH** ซึ่งเกิดได้ทั้งจาก
วงจรนิรภัยเปิดจริง **และ**จากการที่สายถูกถอดออกจากบอร์ด — ทั้งสองกรณีหน้าตาเหมือนกันเป๊ะ
เป็นกฎที่ CLAUDE.md เขียนไว้เองใน §6.5 และ §9 แต่ผมลืมถามว่าช่วงนั้นมีใครยุ่งกับฮาร์ดแวร์ไหม

⇒ **ตอนทดสอบ Lift 5 ให้จดเวลาที่คุณทำงานกับบอร์ดไว้ด้วย** แล้วบันทึกลง `LIFT_STATUS.md`
ไม่งั้นใครก็ตามที่มาอ่านข้อมูลทีหลังจะตีความผิดแบบเดียวกัน

---

## 🔴 เกณฑ์รับงานซ่อม VS2 ที่จดไว้เดิม **ใช้ไม่ได้** — อย่าเอาไปใช้ซ้ำกับ Lift 5

`§9 ข้อ 3` เดิมเขียนว่า *"D24 ต้องเริ่มสลับ, **รหัสสูงสุดต้องเป็น 46**"*

**ครึ่งหลังผ่านอยู่แล้วตอนสายยังตาย** — ชั้น 44 = รหัส 46 = `101110` ซึ่งบิต 0 เป็น 0 อยู่แล้ว
สายที่ตายทำให้เสีย**ความละเอียด** ไม่ได้เสีย**ช่วง** ⇒ ใครใช้เกณฑ์นี้จะเซ็นรับงานที่ยังไม่เสร็จ

**เกณฑ์ที่ใช้ได้ ต้องผ่านครบสี่ข้อ** (ดู CLAUDE.md §8.1):

1. D24 ค้างได้เกิน 50 ms — ไม่ใช่ไฟบ้านเหนี่ยวนำ
2. **รหัสเลขคี่ต้องโผล่มา** ← ง่ายที่สุด ไม่ต้องใช้เกณฑ์ตัวเลขใด ๆ
3. D24 สลับราว **2 เท่า** ของ D25
4. toggle cascade ลดลงครึ่งหนึ่งทุกขั้น

---

## สถานะ fleet ตอนนี้

| ลิฟต์ | พอร์ต | firmware | สถานะ |
|---|---|---|---|
| Lift 1 | COM20 | 1.2.2 `LIFT=1` | ✅ **ซ่อม VS2 สำเร็จ 12 ก.ย. 15:38** ครบ 10/10 เส้น |
| Lift 2 | COM22 | 1.2.2 `LIFT=2` | ✅ ครบ |
| Lift 3 | COM21 | 1.2.2 `LIFT=3` | ✅ ครบ · ตัวอ้างอิง |
| Lift 4 | — | — | ⛔ ปิดใช้งาน ข้ามไปก่อน |
| Lift 5 | — | **งานของคุณ** | 🔧 lift service |

**การเก็บข้อมูล**: 3 โปรเซสบน Gateway ทำงานต่อเนื่อง **5.8 วัน · rejected 0 · link lost 0 ·
บอร์ดไม่รีสตาร์ตเลย** รวม 324,000 บรรทัด

**ผลซ่อม VS2 ของ Lift 1** ยืนยันกับการวิ่งจริง (ขึ้นชั้น 44 เวลา 15:47 ลงชั้น 1 เวลา 15:48):
toggle cascade `154/76/38/18/8/4` ลดครึ่งทุกขั้น · single-step **154/154 = 100%** ·
รหัสเลขคี่ **78 จาก 155** (เดิม 0)

---

## ฝั่ง Gateway จะทำอะไรเมื่อ Lift 5 พร้อม

พอคุณส่ง dongle มา ผมจะ:

1. `python port_resolver.py` — หาว่า Lift 5 อยู่พอร์ตไหนจาก beacon ไม่ใช่เดาเลข COM
2. `python rs485_check.py COM<n> 5` — ต้องได้ `VERDICT: UP` และ exit 0
3. `python log_lift.py COM<n> 5 --listen` — ตรวจ `LIFT=5` ก่อนเขียนบรรทัดแรก
4. วิ่งจริงหนึ่งเที่ยวแล้วเทียบกับที่คนจด — single-step ต้อง 100%

> ⚠️ **เลข COM เปลี่ยนเองได้** dongle เป็น CH340 ไม่มี serial number ⇒ Windows ตั้งเลข
> ตามช่องที่เสียบ เกิดขึ้นจริงแล้ว (Lift 2 ย้าย COM20 → COM22 เอง) **อย่าส่งเลข COM มาให้**
> ส่งแค่ dongle แล้วให้ beacon เป็นตัวบอก

---

## ข้อที่ยังเปิดอยู่

| # | งาน | สถานะ |
|---|---|---|
| 1 | **ทดสอบ SAFETY กับลิฟต์จริง** | ยังไม่เคยทำ — ต้องกระตุ้นจริง เช่นเปิดประตูค้าง กดหยุดฉุกเฉิน พร้อมจดเวลา |
| 2 | **FIRE / FIRE RETURN** | ยังไม่เคยทำ · ⚠️ **D18 ไม่ใช่ FIRE** วัดแล้วปิด 25,000 ครั้งยาวสุด 3 ms = พื้นสัญญาณรบกวน |
| 3 | Lift 5 | งานของคุณ |
| 4 | Lift 4 | ปิดใช้งาน ข้ามไปก่อน |

ตารางรหัส→ชั้นปิดครบแล้วจากข้อมูล (รหัส 4, 9, 11 เป็นระดับวิ่งผ่าน ไม่ใช่ชานจอด)
ดู CLAUDE.md §3.4 — ไม่ต้องไปจอดทดสอบทีละชั้นแล้ว

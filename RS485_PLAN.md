# แผนย้ายจาก USB ไป RS485

เตรียมไว้ก่อนลงมือ — ข้อสรุปจากการวิเคราะห์โค้ดและ log จริง 30 ส.ค. 2026
โครงที่วางไว้: `Arduino UART ↔ ตัวแปลง RS485 ↔ สายคู่บิดเกลียว ↔ ตัวแปลง RS485-USB ↔ PC`

---

## 1. ข้อสรุปหลัก — ใช้ D0/D1 และ **ไม่ต้องแก้ firmware เลย**

ขา RX/TX ที่ silkscreen บนบอร์ดคือ **D0 (RX0) / D1 (TX0) = `Serial`** ซึ่งเป็น UART เดียวที่ sketch ใช้อยู่แล้ว
(`Serial.begin(115200)` ที่ [IODebug.ino:321](IODebug/IODebug.ino#L321), จุดพิมพ์ 104 แห่ง ทั้งหมดยิงเข้า `Serial`)

sketch **กัน D0/D1 ออกจากการสแกนไว้แล้วโดยเจตนา** — `DIG_FIRST = 2` ที่ [IODebug.ino:19](IODebug/IODebug.ino#L19)
พร้อมคอมเมนต์ "D0/D1 are the USB serial pins" ⇒ ต่อ transceiver ที่ D0/D1 แล้ว binary เดิมทำงานได้ทันที
ฝั่ง PC ก็ไม่ต้องแก้ `log_lift.py` รับพอร์ตจาก `sys.argv[1]` อยู่แล้ว แค่ใส่เลข COM ใหม่

> ตรวจแล้วทั้ง repo: ไม่มี `Serial1/Serial2/Serial3` หรือ `RS485` ปรากฏที่ไหนเลย

## 2. ⛔ ห้ามใช้ Serial1 / Serial2 เด็ดขาด

UART สำรองสองตัวนั่งทับสายสัญญาณลิฟต์พอดี:

| UART | ขา | ชนกับอะไร |
|---|---|---|
| Serial1 | D18 (TX1) / **D19 (RX1)** | **D19 = UP** (LOW 1,678 ครั้งใน `capture_lift_3.log`) และ D18 คือตำแหน่งที่คาดว่าเป็น **FIRE** |
| Serial2 | **D16 (TX2)** / **D17 (RX2)** | **D16 = RUNNING** (LOW 3,024 ครั้ง), **D17 = SAFETY** (LOW 3,373 ครั้ง) |
| Serial3 | D14 / D15 | ว่าง — ไม่เคย LOW เลยในทุก log แต่ดูข้อ 2.1 |

**อันตรายจริง ไม่ใช่ทฤษฎี:** `Serial2.begin()` ตั้งบิต `TXEN2` ซึ่ง **override `DDRH` บังคับ D16 เป็น push-pull output**
`pinMode()` เอาคืนไม่ได้ และ D16 คือหน้าสัมผัส RUNNING ที่ **ปิดลงกราวด์ทุกครั้งที่ลิฟต์วิ่ง**
⇒ ขับ HIGH ใส่หน้าสัมผัสที่ปิดอยู่ = ไฟช็อตเกิน 40 mA absolute max = **พอร์ตพัง**
นี่คือความล้มเหลวแบบเดียวกับที่ CLAUDE.md §4 ข้อ 1 ห้ามไว้เป๊ะ และจะเกิดครั้งแรกที่ลิฟต์เคลื่อนที่

### 2.1 ถ้าจะใช้ Serial3 ต้องแก้ sketch ก่อน

D14/D15 อยู่ในช่วง D2–D53 ที่ถูกสแกน ⇒ `readMask()` จะเอา TX3 เข้าไปใน mask ด้วย
พอส่ง ST ออกไป ขา D14 กระดิก → mask เปลี่ยน → emit ST ใหม่ → กระดิกอีก = **ลูปส่งไม่รู้จบ กลืนข้อมูลจริง**
ต้องเพิ่ม skip mask ใน 4 ลูป (`applyPinMode`, `resetStats`, `characterisePins`, `readMask`) และ
บังคับบิตนั้นเป็น 1 ใน `readMask()` — ห้ามขยับ `DIG_FIRST/DIG_LAST` เพราะ log เก่าทั้ง 3 ไฟล์จะถอดไม่ได้

## 3. 🔴 กับดัก 4 ข้อที่จะกัดแน่ถ้าไม่รู้ล่วงหน้า

### 3.1 ต้องใช้ transceiver 5V เท่านั้น

MAX3485 / SP3485 ที่ 3.3V ให้ `VOH ≥ 2.9V` แต่ Mega ต้องการ `VIH ≥ 3.0V` — **ต่ำกว่าเกณฑ์**
บนโต๊ะจะใช้ได้ แล้วไปเพี้ยนตอนร้อน ซึ่งห้องเครื่องลิฟต์ร้อน
ซ้ำ: D1 ส่งออก 5V เข้าขา DI ที่ absolute max แค่ 3.6V → ดัน ESD clamp เข้ารางไฟ
**ใช้ MAX485 / SN75176 / MAX13487E ที่ 5V** และวัด VCC บนโมดูลก่อนต่อ

### 3.2 ต้องมี fail-safe bias — อันตรายที่สุดในทั้งหมด

บัสที่ไม่มีใครขับและไม่มี bias จะลอยใกล้ 0V แล้ว RO จะกระดิกตามสัญญาณรบกวน
`capture_lift_3.log` มีช่องว่างระหว่างข้อความ **median 913 ms** และเกิน 1 วินาทีถึง 1,234 ครั้ง — บัสว่างเกือบตลอด

ปัญหาคือ sketch แปลผลทุกไบต์ที่เข้ามาเป็นคำสั่งทันที ([IODebug.ino:402-438](IODebug/IODebug.ino#L402)) ไม่มี framing:

| ไบต์รบกวน | ผลที่ได้ |
|---|---|
| `s` | พิมพ์ snapshot แม้อยู่ใน watch mode → **บั๊ก §6.1 กลับมา** (บล็อกลูป อ่านบิตตก) |
| `z` | ปิด pullup ทุกขา → §6.4 ขาลอยค้างนิ่งดูเหมือนข้อมูลจริง |
| `w` | **ปิด watch mode เงียบ ๆ** logger รันต่อโดยได้ 0 บรรทัด |
| `p` | บล็อกลูปเกิน 1 วินาที |

และตัวอักษรคำสั่งอยู่ชิดกันใน ASCII — พลิกบิตเดียว: `e`→`a`, `r`→`z`, `w`→`s`
heartbeat `e` ทุก 60 วินาทีจึงห่างจาก `a` แค่บิตเดียว

**แก้:** ใส่ bias จุดเดียวบนบัส (dongle หลายรุ่นมีมาแล้ว — เช็คก่อน อย่าใส่ซ้ำ)
ถ้ามี terminator ครบสองตัว (60Ω) ใช้ **560Ω pull-up ที่ A ไป 5V + 560Ω pull-down ที่ B ลง GND**
ถ้ามีตัวเดียวใช้ 1kΩ หรือเลือกชิปที่มี true fail-safe receiver ในตัว (MAX3082, SN65HVD1780, THVD1450)

### 3.3 กับดัก RTS บนตัวแปลง RS485-USB

โค้ดบังคับ `rts=False` ไว้ 5 จุด ([log_lift.py:113](log_lift.py#L113), [lift_decode.py:143](lift_decode.py#L143),
[scan_pins.py:54](scan_pins.py#L54), [selftest.py:128](selftest.py#L128) และ :185) แต่ `monitor.py:29` บังคับ `rts=True` ตรงข้าม

บน dongle หลายรุ่น **RTS คือขา DE (transmit enable)**:
- RTS = DE active-high → `rts=False` ⇒ PC ส่งอะไรออกไปไม่ได้เลย ทุกไบต์ถูกกลืน
- RTS = DE inverted → `rts=False` ⇒ PC ขับบัสค้างตลอด **บอร์ดไม่มีทางส่งอะไรได้**

ผลคือ `arm()` ไม่ได้รับการยืนยัน แล้วโยน SystemExit บอกว่า *"Is the IODebug sketch loaded on this board?"*
ซึ่งเป็นการวินิจฉัยที่ผิดทางโดยสิ้นเชิง — ปัญหาอยู่ที่ขา DE ไม่ใช่ที่บอร์ด

**แก้:** ซื้อ dongle แบบ auto-direction (FTDI ที่ตั้ง TXDEN ใน EEPROM หรือโมดูลที่มี one-shot ในตัว) ให้ RTS ไม่มีความหมาย

### 3.4 ห้ามต่อ USB พร้อม RS485ที่ D0/D1

CH340 บนบอร์ดต่อกับ D0 อยู่แล้ว ถ้าต่อ RO ของ transceiver เข้าไปด้วยจะมี **push-pull สองตัวชนกัน**
บอร์ดแท้มี R อนุกรม 1kΩ กันไว้ แต่บอร์ด clone บางรุ่นต่อตรง
และการ **อัปโหลด sketch ต้องใช้ USB** ⇒ ต้องถอด RS485 ออกก่อนทุกครั้ง

**แก้:** ใส่จัมเปอร์ 2 ขาในเส้น RO→D0 เพื่อยกออกได้ และเขียนไว้ในขั้นตอนว่า *"ถอดจัมเปอร์ RS485 ก่อนอัปโหลด"*

## 4. ไฟเลี้ยงและการแยกกราวด์

พอไม่มี USB บอร์ดต้องมีไฟเลี้ยงในห้องเครื่อง ซึ่งสร้าง **จุดอ้างอิงกราวด์ที่สอง** ที่เดิมไม่มี

⚠️ **ห้ามใช้ buck converter แบบไม่แยกกราวด์จากราง 24V ของตู้ลิฟต์**
เพราะจะมัด Arduino GND เข้ากับ 24V return ของตู้ = **ทำลายฉนวนกั้นเพียงชั้นเดียวที่ระบบนี้มี**
(หลักการทั้งหมดใน CLAUDE.md §2 คือหน้าสัมผัสแห้งไม่แชร์กราวด์กัน) และทำให้คำโฆษณา read-only เป็นโมฆะ

ใช้อย่างใดอย่างหนึ่ง:
- adapter Class II (ลอย ไม่มีขากราวด์) 7.5–9V เข้า barrel jack — *อย่าใช้ 12V เพราะ regulator จะร้อน 0.7W*
- DC-DC **แบบแยกกราวด์** 24V→5V (≥1.5kV isolation) จากรางในตู้

**ground loop:** สาย RS485 จะเชื่อมกราวด์ห้องเครื่องกับกราวด์ PC เป็นวงรอบปล่องลิฟต์
ซึ่งมี VFD สวิตช์กระแสสูง — ประเด็นคือ **หน้าสัมผัสรีเลย์ common คือ Arduino GND** ดังนั้นเสิร์จบนสาย
จะไปกดคร่อมฉนวน coil-to-contact ของรีเลย์ (~1.5kVrms) ที่เป็นด่านเดียวระหว่างเรากับตู้ควบคุม
**แนะนำ transceiver แบบ isolated** (ADM2582E / ADM2587E) ที่ฝั่ง Arduino — แยกจุดเดียวก็ตัดวงรอบได้แล้ว

## 5. ⚠️ ลิฟต์ 5 ตัวบนบัสเดียวใช้ไม่ได้กับดีไซน์ปัจจุบัน

sketch ส่งข้อมูลเมื่อไหร่ก็ได้ ไม่มี arbitration ไม่มี address ไม่มี checksum:

- **ชนกัน:** 1 frame ≈ 26 ไบต์ = 2.26 ms ที่ 115200 — 5 ลิฟต์จะชนราว **80 ครั้ง/ชม.** เสียหาย 160 records/ชม.
- **คำสั่งเป็น broadcast:** heartbeat `e` ทุก 60 วิ → ทั้ง 5 บอร์ดตอบพร้อมกัน = ชนแน่นอนทุกครั้ง
  และ `w` จะ toggle ทั้ง 5 ตัวพร้อมกันโดยที่แต่ละตัวอาจอยู่คนละสถานะ
- **Windows เปิด COM port แบบ exclusive** ⇒ 5 โปรเซส `log_lift.py` แชร์ dongle เดียวไม่ได้

**แนะนำ: หนึ่งคู่สาย RS485 ต่อหนึ่งลิฟต์** (5 dongle, 5 โปรเซส) เครื่องมือเดิมใช้ได้หมดโดยไม่ต้องแก้
ถ้าจำเป็นต้องใช้บัสเดียวจริง ต้องเปลี่ยนเป็น polled master/slave + address + CRC = เขียนใหม่ทั้ง firmware และ PC

## 6. หนี้ทางเทคนิคที่ RS485 จะไปกระตุ้น

| ที่ | ปัญหา | ทำไมเพิ่งเป็นปัญหาตอนนี้ |
|---|---|---|
| [lift_decode.py:140](lift_decode.py#L140) `open_watch()` | รอ 1.5 วิ แล้วยิง `w` ครั้งเดียวไม่ยืนยัน — **บั๊ก §6.2 ที่ยังไม่ได้แก้** | USB reset บอร์ดทุกครั้งที่เปิดพอร์ต จึงบังเอิญถูก **RS485 ไม่มี reset** ⇒ ถ้าบอร์ด watch อยู่แล้ว `w` จะ **ปิด** |
| `selftest.py` | half-duplex echo ทำให้ TEST 2/3 **ผ่านโดยไม่มีบอร์ดต่ออยู่เลย** (อ่านไบต์ตัวเองกลับมา) | USB ไม่มี echo |
| [log_lift.py:184](log_lift.py#L184) | `int(parts[2], 16)` ไม่เช็คความยาว — mask 12 หลักก็ parse ผ่าน **เลื่อนทุกบิต** เงียบ ๆ | สาย USB ไม่ทำข้อมูลเพี้ยนกลางทาง |
| [log_lift.py:206](log_lift.py#L206) | จับเฉพาะ `SerialException/OSError` — `ValueError` จาก hex เสียจะทำโปรเซสตาย | เช่นเดียวกัน |
| reconnect loop | ตรวจจับบอร์ดตายด้วยการดูพอร์ตหาย — **dongle RS485 ไม่เคยหาย** | USB ถอดแล้วพอร์ตหายจริง |

**สิ่งที่ควรทำก่อน:** เช็คว่า mask ยาว 13 หลักพอดี, นับบรรทัดที่ทิ้ง แล้วรายงานอัตรา reject
(บนลิงก์ point-to-point ค่านี้ต้องเป็น 0 — ไม่เป็น 0 เมื่อไหร่คือมีปัญหาสาย/termination/baud)

## 7. Termination

บัส 2 โหนด: **120Ω ที่ปลายทั้งสองข้าง = 60Ω รวม** ห้ามมีตัวที่สาม

⚠️ กับดักคือ **ใส่เกิน** ไม่ใช่ขาด — โมดูล MAX485 ราคาถูกส่วนใหญ่บัดกรี 120Ω มาให้แล้ว (มักเป็น R7)
และ dongle หลายรุ่นก็มี ถ้าใส่เพิ่มเองจะเหลือ 60Ω ตั้งแต่ยังไม่เริ่ม ตัวที่สามลงไปเหลือ 40Ω
ซึ่ง**ต่ำกว่า 54Ω ที่ TIA-485 รับประกัน** → แรงดัน differential ตกลงมาใกล้เกณฑ์ 200 mV แล้วลิงก์ล่ม

**วัด A-B ด้วยโอห์มมิเตอร์ทุกโมดูลตอนไม่จ่ายไฟก่อนต่อ** เป้าหมายคือ 60Ω คร่อมบัสที่ประกอบเสร็จ

---

## 8. Checklist ก่อนต่อของจริง

**บนโต๊ะ — ถอดบอร์ดออกจาก DNMEGA1 ให้หมดก่อน**

1. ยืนยันว่า transceiver เป็นชิป 5V (อ่าน marking + วัด VCC)
2. วัด A-B ทุกโมดูลตอนไม่จ่ายไฟ จดว่ามี terminator อะไรติดมาบ้าง จัดให้เหลือ 60Ω
3. ยืนยันว่า dongle เป็น auto-direction ไม่ใช่ RTS-controlled
4. ต่อลิงก์เต็มบนโต๊ะด้วยสายจริง แล้วเช็ค: banner มาครบ / **บอร์ดไม่กินคำสั่งจาก banner ตัวเอง** / `w` ค้างอยู่ได้ 10 นาทีโดยบัสว่าง
5. ปล่อยบัสว่างไม่มี bias 5 นาที ดูว่ามีคำสั่งหลอนไหม แล้วใส่ bias ทำซ้ำ — ต้องเห็นความต่าง
6. ทดสอบ burst: ของจริงมี 3 บรรทัดใน 3 ms — แตะ D24/D25 ลง GND รัว ๆ (0.15 mA ปลอดภัย) ดูว่าตกหล่นไหม

**ก่อนไปหน้างาน**

7. `grep` หา `Serial1`, `Serial2`, `Serial3`, `OUTPUT`, `digitalWrite` ในsketch — ต้องได้ 0 ทั้งห้าคำ
8. ยืนยันไฟเลี้ยงเป็น Class II ลอย หรือ DC-DC แยกกราวด์ และเป็น 7.5–9V ไม่ใช่ 12V
9. เพิ่ม `ValueError` เข้า except ที่ [log_lift.py:206](log_lift.py#L206)
10. ใส่จัมเปอร์ RO→D0 ให้ยกออกได้ตอนอัปโหลด

**ที่หน้างาน ตามลำดับนี้**

11. ลิฟต์จอด บอร์ดยังต่อ USB → รัน `p` เก็บภาพ "ก่อน" ไว้ (read-only ไม่ขับขา)
12. **ตรวจขั้ว DNMEGA1 ช่อง 14 และ 15 ด้วยตา** ว่าไม่มีสายลง — เป็นวิธีเดียวที่จะรู้ว่า Serial3 ใช้ได้จริงไหม
13. จ่ายไฟจากแหล่งใหม่ **ยังไม่ต่อ RS485** ยืนยันว่ายัง log ผ่าน USB ได้ปกติ
14. ถอด USB → ต่อ RS485 → ยืนยันว่า `arm()` สำเร็จ และได้บรรทัด baseline จาก `e`
15. **วิ่งลิฟต์ 1 เที่ยวเต็มแล้วเทียบชั้นที่ถอดได้กับที่จดด้วยมือ** — single-step ต้อง 100%
16. ปล่อย capture อย่างน้อย 1 ชั่วโมง แล้วตรวจบรรทัดเสีย ช่องว่างที่อธิบายไม่ได้ และ heartbeat ที่ไม่มีคนตอบ

> ข้อ 4, 5 และ 15 คือสามข้อที่พิสูจน์จริงว่าปลอดภัย ที่เหลือเป็นประกันราคาถูก
> **ห้ามข้ามข้อ 15** — ตาม CLAUDE.md §10 บั๊กร้ายแรงทุกตัวในโปรเจกต์นี้ถูกจับได้จากการเทียบผลอัตโนมัติกับความจริงที่ตรวจด้วยมือ
> และ RS485 คือการเปลี่ยนแปลงประเภทที่ให้ **ข้อมูลผิดที่ดูน่าเชื่อ** มากกว่าจะพังให้เห็นชัด ๆ

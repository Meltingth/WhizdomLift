# CLAUDE.md — WhizdomLift

บันทึกบริบทโปรเจกต์สำหรับ session ถัดไป: เป้าหมาย, สิ่งที่ยืนยันแล้ว, กฎความปลอดภัย,
บทเรียนจากบั๊กที่เคยเจอ และงานที่ยังเหลือ

---

## 1. เป้าหมายของโปรเจกต์

อ่านสัญญาณ **ตำแหน่งชั้น** และ **สถานะ** ของลิฟต์ในอาคาร 44 ชั้น (มีชั้นใต้ดิน, ลิฟต์ 5 ตัว)
เข้าสู่ Arduino Mega 2560 เพื่อนำไปใช้งานต่อ — โดยต้อง **อ่านอย่างเดียว ไม่แทรกแซงระบบลิฟต์**

โจทย์ตั้งต้นจากผู้ใช้: "Debug สัญญาณที่ต่อเข้ามาระบบ I/O ของ Arduino" แล้วขยายเป็น
ถอดรหัสว่าบิตแต่ละชุดหมายถึงชั้นไหน สถานะอะไร และตรวจว่า wiring ครบถ้วนหรือไม่

---

## 2. สถาปัตยกรรมสัญญาณ (ยืนยันจากไดอะแกรมตู้ควบคุม + การวัดจริง)

```
ตู้ควบคุมลิฟต์ — หน้าสัมผัสรีเลย์ MC-1 (common), MC-5..MC-10
    │  ตำแหน่ง 6 บิต (VS2..VS7) + สถานะ 6 เส้น, ไฟ 24V
    ▼
โมดูลรีเลย์ 24VDC  (6 ช่อง + 8 ช่อง, TONGLING JQC-3FF-S-Z, จัมเปอร์ Com–High)
    │  หน้าสัมผัสแห้ง (dry contact) — แยกกราวด์ให้ปลอดภัยกับ 5V
    ▼
DNMEGA1 (eletechsup) — แผงขั้วสกรู DIN rail, เป็น "ตัวนำล้วน" ไม่มีวงจรแปลงใด ๆ
    ▼
Arduino Mega 2560 — ทุกขาเป็น INPUT_PULLUP  ⇒ หน้าสัมผัสปิด = อ่านได้ LOW = สัญญาณ active
    ▼  USB @115200  โปรโตคอล  ST <board_ms> <52-bit hex mask>
PC — log_lift.py (หนึ่งโปรเซสต่อลิฟต์)
```

**ทิศทางข้อมูลคือ ตู้ลิฟต์ → Arduino เท่านั้น** Arduino ไม่เคยส่งอะไรกลับ

---

## 3. ข้อเท็จจริงที่ยืนยันแล้ว — อย่าไปคำนวณใหม่

### 3.0 ชื่อลิฟต์ — สองระบบที่ต้องแมปกัน

ตอนทดสอบเรียกตามลำดับที่ต่อบอร์ด (A, B, C…) แต่ชื่อจริงในอาคารคือเลขลิฟต์
ตัวอักษรฝังอยู่ในล็อกและรายงานไปแล้ว จึงต้องรองรับทั้งคู่ — ดู `lift_id()` ใน `lift_decode.py`

| เลขจริง | label ทดสอบ | ไฟล์ | สถานะ |
|---|---|---|---|
| Lift 1 | B | `capture_lift_1.log` | 🔴 VS2 ขาด |
| Lift 2 | C | `capture_lift_2.log` | ✅ ครบ (ซ่อม COM ขาออกที่ต่อลง 5V แทน GND แล้ว) |
| **Lift 3** | **A** | `capture_lift_3.log` | ✅ **ตัวอ้างอิง** |
| Lift 4 | D | `capture_lift_4.log` | ยังไม่ทดสอบ |
| Lift 5 | E | `capture_lift_5.log` | ยังไม่ทดสอบ (เลข 5 เป็นการอนุมาน ยังไม่ยืนยัน) |

เครื่องมือรับได้ทั้ง `2` และ `C` และ `lift 2`

### 3.1 แผนที่ขา Lift 3 (label A) — ตัวอ้างอิง

| ขา | สัญญาณ | บทบาท |
|---|---|---|
| D24 | VS2 | บิต 0 (น้ำหนัก 1) |
| D25 | VS3 | บิต 1 (2) |
| D26 | VS4 | บิต 2 (4) |
| D27 | VS5 | บิต 3 (8) |
| D28 | VS6 | บิต 4 (16) |
| D29 | VS7 | บิต 5 (32) |
| D16 | RUNNING 运行 | ปิดขณะวิ่ง เปิดขณะจอด |
| D17 | SAFETY 安全 | ปิดเกือบตลอด (normally-closed) |
| D19 | UP 上行 | ขึ้น 100% / ลง 0% |
| D20 | DN 下行 | ขึ้น 0% / ลง 100% |

คุณภาพ: single-step **100%** จาก 2,008 stable states

### 3.2 แผนที่ขา Lift 1 (label B)

VS3–VS7 ลงขา **D25–D29 เหมือนลิฟต์ A ทุกเส้น** แต่ **D24 (VS2) ไม่เคย LOW เลย
(0 จาก 591 states)** → สาย VS2 ขาด ทำให้อ่านค่าได้ = รหัสจริง >> 1

`POSITION_BITS_B = {25:0, 26:1, 27:2, 28:3, 29:4}` ใน `lift_decode.py`

> ⚠️ นี่คือแผนที่สำหรับอ่านข้อมูลเก่าที่ยังไม่ซ่อม — **ถ้าซ่อมสาย VS2 แล้ว Lift 1 จะใช้แผนที่เดียวกับ Lift 3**

### 3.3 การเข้ารหัส

ไบนารีตรง ๆ ไม่มีอะไรพิเศษ:

```
รหัส = 32·VS7 + 16·VS6 + 8·VS5 + 4·VS4 + 2·VS3 + 1·VS2
```

### 3.4 ตารางแปลง รหัส → ป้ายชั้น (สอบเทียบจากการจับเวลาจริง)

| ป้ายชั้น | รหัส | offset |
|---|---|---|
| B1 | 1 | — |
| 1 | 2 | +1 |
| 9 | 12 | +3 |
| 20 | 22 | +2 |
| 26 | 28 | +2 |
| 32 | 34 | +2 |
| 33 | 35 | +2 |
| 36 | 38 | +2 |
| 37 | 39 | +2 |
| 44 (สูงสุด) | 46 | +2 |

- ตั้งแต่ชั้น 20 ขึ้นไป offset คงที่ +2
- offset หด +3→+2 ระหว่างชั้น 9–20 ⇒ **ชั้น 19 ไม่มีชานจอด** (ตรงกับที่ผู้ใช้สังเกต)
- ผู้ใช้รายงานว่าอาคารข้าม **ชั้น 7 และ 19**
- ช่วงชั้น 1–9 ยังปิดตารางไม่ได้ (offset โต +1→+3 แปลว่ามีชานจอดแฝง 2 ระดับ)

---

## 4. ⚠️ กฎความปลอดภัย — ห้ามละเมิด

1. **ห้ามตั้งขาใดเป็น `OUTPUT` เด็ดขาด** ขาเหล่านี้ต่อกับหน้าสัมผัสรีเลย์
   หน้าสัมผัสที่ปิดอยู่ = ทางลัดลงคอมมอน การขับ HIGH = ไดรเวอร์ push-pull คร่อมไฟช็อต **พอร์ตพัง**
   (เคยเขียนฟังก์ชัน `mapRelays()` ที่ขับขา — **ลบทิ้งไปแล้ว อย่าเขียนกลับมา**)
2. ตอนเสียบเข้าระบบจริงครั้งแรก ใช้คำสั่ง `z` สลับเป็น `INPUT` high-Z ก่อน
   (high-Z จ่ายกระแสไม่พอทริกออปโตแม้จัมเปอร์ตั้งที่ Com–High)
3. อย่ารีเซ็ตบอร์ดโดยไม่จำเป็นระหว่างเก็บข้อมูล — `log_lift.py` ตั้ง `dtr=False, rts=False` ไว้แล้ว

---

## 5. เครื่องมือ

| ไฟล์ | หน้าที่ |
|---|---|
| `IODebug/IODebug.ino` | sketch บนบอร์ด — คำสั่ง: `h` help, `s` snapshot, `p` characterise, `z` toggle pullup/high-Z, `w` watch mode, `e` emit state now, `r` reset stats, `a`/`d` toggle analog/digital |
| `LIFT_STATUS.md` | **ตารางสถานะลิฟต์ทั้ง 5 ตัว + ขั้นตอนทดสอบทีละตัว** — อัปเดตทุกครั้งที่ทดสอบ |
| `log_lift.py` | ตัวบันทึกถาวร `python log_lift.py COM3 3` — **ต้องระบุเลขลิฟต์** (ไม่ใช่แค่พอร์ต), reconnect เอง, heartbeat 60 วิ, หยุดด้วยไฟล์ `STOP_CAPTURE` |
| `lift_health.py` | **ตรวจสุขภาพลิฟต์เทียบกับลิฟต์ A** `python lift_health.py 2` — ชี้ว่าเส้นไหนไม่ส่งสัญญาณ พร้อมวิธีชี้จุดขาด |
| `rs485_check.py` | **ตรวจลิงก์ RS485 โดยไม่ส่งอะไรออกสาย** `python rs485_check.py COM3 1` — แยก `UP` / `UP (DEGRADED)` / `SILENT` / `UNDRIVEN` / `FRAMING` / `INCONCLUSIVE` พร้อมชี้จุดตรวจถัดไปของแต่ละกรณี · exit 0 = ดี, 1 = มีปัญหา, 2 = บอร์ดผิดตัว |
| `lift_decode.py` | **config กลาง** — แผนที่บิต + `CODE_TO_LABEL` + `lift_id()/lift_log()` ตัวแปลงชื่อลิฟต์ |
| `analyze_lift.py` | ถอดแผนที่บิตอัตโนมัติ + ไทม์ไลน์ |
| `trips.py` | แยกเที่ยววิ่ง/จุดจอด/จุดแวะรับ + จำแนกสถานะจาก correlation |
| `compare_lifts.py` | เทียบ wiring ระหว่างลิฟต์ |
| `audit.py` | ตรวจข้อมูลดิบหาบิตหาย/ขาเงียบ |
| `reveal.py` | ตรวจคำตอบ blind test |
| `probe_pin.py` | ทดสอบขาแบบสด (แตะ GND ที่ขั้ว → ประกาศชื่อขา) รันคู่กับตัวบันทึกได้ |
| `scan_pins.py` | สแกนหาขาที่ต่อจริง แบบทดสอบซ้ำหลายรอบ |
| `selftest.py` | ทดสอบบอร์ด/พอร์ตเมื่อสงสัยว่าฮาร์ดแวร์เสีย |

Toolchain อยู่ที่ scratchpad (arduino-cli 1.5.2 + avr-gcc 7.3.0 + avrdude 8.0.0)
ตั้ง env `ARDUINO_DIRECTORIES_DATA/DOWNLOADS/USER` ชี้เข้าโฟลเดอร์นั้นก่อนใช้

---

## 6. 🔑 บทเรียนสำคัญจากบั๊กที่เคยเจอ — อ่านก่อนแก้โค้ด

### 6.1 sketch ห้ามพิมพ์อะไรระหว่างเก็บข้อมูล  ← บั๊กร้ายแรงที่สุด

เวอร์ชันแรกพิมพ์บรรทัด `EDGE` และ snapshot ทุก 3 วินาทีขณะ watch mode ทำงาน
ที่ 115200 บอด บัฟเฟอร์ TX เต็มแล้ว **บล็อกลูปสุ่มตัวอย่างหลายสิบมิลลิวินาที**
ผลคือ D24 ซึ่งเป็นบิตที่สลับเร็วที่สุดถูกอ่านตกหล่นจนดูเหมือน "พัลส์ต่อชั้น"
→ วินิจฉัยผิดว่าเป็น strobe → **เลื่อนบิตทุกตัวขึ้นหนึ่งตำแหน่ง** → อ่านชั้นสูงสุดได้ 23 แทน 46

หลักฐานที่ควรสังเกตตั้งแต่แรก: `46 >> 1 = 23` พอดี และอัตรา toggle D24:D25 = 32:16 = 2:1
ซึ่งคืออัตรา LSB ต่อบิตถัดไปตามทฤษฎีอยู่แล้ว

**กฎ: ใน watch mode ต้องไม่มี `Serial.print` อื่นใดเลย** (แก้แล้วในโค้ดปัจจุบัน)

### 6.2 คำสั่งที่ส่งตอนบอร์ดยังบูตอยู่จะหายไปเงียบ ๆ

เปิดพอร์ต → บอร์ดรีเซ็ต → bootloader กินสายราว 2 วินาที
เวอร์ชันแรกหน่วงแค่ 0.3 วิ แล้วส่ง `w` → คำสั่งหาย → watch mode ไม่เคยเปิด
→ ผู้ใช้วิ่งลิฟต์หลายรอบแต่ล็อกว่างเปล่า

**กฎ: หน่วง `BOOT_WAIT = 3.0` วิ และ *ยืนยันผลตอบกลับ* เสมอ**
คำสั่งเหล่านี้เป็น toggle ไม่ใช่ set — ยิงครั้งเดียวแล้วหวังผลไม่ได้
ต้องอ่านข้อความยืนยันแล้ว toggle ซ้ำถ้าได้สถานะตรงข้าม (`_set_mode()` ใน `log_lift.py`)

### 6.3 ล็อกว่าง ≠ ระบบพัง (และ ≠ ระบบดี)

"เงียบเพราะลิฟต์ไม่วิ่ง" กับ "เงียบเพราะระบบพัง" หน้าตาเหมือนกันเป๊ะ
จึงเพิ่มคำสั่ง `e` + heartbeat ทุก 60 วิ ให้บอร์ดรายงานสถานะตัวเองเป็นระยะ

### 6.4 ขาลอยนิ่งได้ในช่วงสั้น ๆ — ต้องทดสอบซ้ำแบบทิ้งช่วง

สแกน 3 รอบห่างกัน 1.5 วิ ได้ผลเหมือนกันเป๊ะ **แต่เป็นผลลวง**
เพราะขาลอยเก็บประจุค้างได้หลายวินาที พอทิ้งช่วง 4 วิ ผลก็เปลี่ยน
`scan_pins.py` จึงตั้ง default เป็น 5 รอบ ห่าง 4 วิ

### 6.5 แยกสัญญาณจริงจากการเหนี่ยวนำไฟ AC

ขาที่ลอยจริงจะอ่าน HIGH ทันทีเมื่อเปิด pullup (RC ≈ 30kΩ × 10pF ≈ 0.3µs)
การอ่านได้ LOW แปลว่ามีกระแสไหลออกราว 80µA — อาจเป็นโหลดจริง **หรือไฟ AC เหนี่ยวนำเข้าสายปลายเปิด**
แยกด้วยเวลา: โหลด DC กดค้าง แต่ไฟ 50Hz ตัดศูนย์ทุก 10ms
→ `pinIsWired()` อ่าน 120 ตัวอย่างคร่อม 120ms (6 รอบคลื่น) ต้อง LOW ทุกตัวอย่าง

### 6.6 การหาบิตด้วย "อันดับ toggle สูงสุด N ตัว" ผิด 2 แบบ

- **strobe/สัญญาณรบกวนแทรก**: พัลส์ต่อชั้นสลับ 2 ครั้ง/ชั้น มากกว่า LSB → แย่งอันดับ 1
- **MSB ตกขอบ**: บิตสูงสุดสลับน้อยที่สุด (ครั้งเดียวต่อ 32 ชั้น) → หลุด top-N → ชุดที่ถูกไม่เคยถูกลอง

**ต้องค้นหาแบบ subset จริง ๆ** (`itertools.combinations`) และคัดด้วยเงื่อนไขทางกายภาพ:
รหัส 0 ไม่มีจริง (ตารางเริ่มที่ 1) + ทุกการเปลี่ยนต้องขยับทีละ 1

### 6.7 บั๊กในการวิเคราะห์ trip (แก้แล้ว แต่ระวังถ้าเขียนใหม่)

| อาการ | สาเหตุ |
|---|---|
| ทุกการเปลี่ยนชั้นกลายเป็น "ทริป" ละ 1 ชั้น | ขาสถานะสลับขณะชั้นเท่าเดิม → เกิด state ใหม่ที่ชั้นไม่เปลี่ยน → ตัดจังหวะนับ **ต้องยุบชั้นซ้ำก่อน** |
| ทริป 7 ชั้นใช้เวลา 334 วิ | ช่วงจอดนิ่ง 320 วิ ถูกนับเป็นเวลาวิ่ง **การหยุดนานต้องตัดจบทริป** |
| ขาสถานะแสดง 100% ทุกช่อง | ตอนยุบชั้นใช้ `|=` union เซ็ตขา → ขาที่ปิดแวบเดียวถูกนับว่าปิดตลอด **ต้องถ่วงน้ำหนักตามเวลาจริงของแต่ละ sample** |
| จุดแวะรับถูกตีเป็นการกลับทิศ | มองข้ามช่วงจอดไปหาทิศก่อนหน้า **ต้องดูทิศ *ที่เข้า* และ *ที่ออก* จากชั้นนั้นโดยตรง** |

### 6.8 ตั้งชื่อไฟล์ตามลิฟต์ ไม่ใช่ตามพอร์ต (และใช้เลขจริง)

Windows สลับเลข COM ไปมาเมื่อสลับบอร์ด ถ้าตั้งชื่อล็อกตามพอร์ต ลิฟต์ C ที่บังเอิญได้ COM5
จะเขียนต่อท้ายข้อมูลลิฟต์ B ทันทีโดยไม่มีอะไรเตือน `log_lift.py` จึงประทับ `# lift: X`
ไว้ในไฟล์และ **ปฏิเสธที่จะเริ่ม** ถ้าชื่อไม่ตรง

ภายหลังเปลี่ยนมาใช้ **เลขลิฟต์จริง** เป็นชื่อไฟล์ (`capture_lift_3.log` ไม่ใช่ `capture_lift.log`)
เพราะ label A–E เป็นแค่ลำดับที่บังเอิญต่อบอร์ด ไม่มีความหมายกับทีมดูแลลิฟต์

### 6.9 นับ "เคยเป็น LOW" ไม่พอ ต้องดูว่าค้างนานแค่ไหน

Lift 2 อ่านได้ว่า RUNNING/SAFETY/UP/DN เป็น LOW หลักหมื่นครั้ง ดูเหมือนต่อครบ
แต่**พัลส์ยาวสุดแค่ 5–18 ms** ที่ ~55–62 Hz ซึ่งหน้าสัมผัสเชิงกลทำไม่ได้
(รีเลย์ใช้เวลาปิดตัวเอง 5–10 ms) — เป็นไฟ AC เหนี่ยวนำเข้าสายปลายเปิดล้วน ๆ
เทียบกับ Lift 3 ที่ขาเดียวกัน median 20–918 วินาที

`lift_health.py` จึงจำแนกเป็น `NOISE` เมื่อ **p99 ของเวลาปิด < 50 ms**

**ห้ามใช้ค่าสูงสุด (max) ตัดสิน** — รอบถัดมา D17 ได้ค่าโดดเดี่ยว 304 ms หนึ่งครั้ง
จาก 79,664 ครั้ง (0.001%) ขณะที่ median ยังเป็น 2 ms และ p99 = 4 ms
ค่าเดียวนั้นพลิกคำตัดสินจาก NOISE เป็น ok ทันที ต้องใช้เปอร์เซ็นไทล์สูงที่ทนค่าผิดปกติ

### 6.10 สายที่ปิดค้างตลอด ≠ สายที่ไม่มีสัญญาณ

`pulse_widths()` นับเฉพาะการปิดที่**จบแล้ว** (ต้องมีขอบขาเปิดปิดท้าย)
D17 (SAFETY) ของ Lift 2 ปิดค้างตลอดการเก็บ ไม่เคยเปิดเลย จึงได้ 0 พัลส์
แล้วถูกตีเป็น `MISSING` — **ทั้งที่นั่นคือพฤติกรรมที่ถูกต้องที่สุดของ normally-closed**
จับได้เพราะรายงานขัดกันเอง: บอก MISSING แต่ตารางสหสัมพันธ์บอก 100% ทั้งสามคอลัมน์
แก้โดยเช็คสัดส่วนตัวอย่างที่เป็น LOW ถ้าเกิน 90% ให้จัดเป็น `HELD` ไม่ใช่ MISSING

### 6.11 ตัวกรองต้อง debounce ทีละขา ไม่ใช่ทั้งสถานะ

ตัวกรอง 250 ms แบบดูทั้งสถานะพร้อมกันพังทันทีที่มีขาใดขาหนึ่งกระพริบ:
D17 ของ Lift 2 สั่น 55 Hz ทำให้ทุกสถานะอายุสั้นกว่าเกณฑ์ **132,696 ตัวอย่างเหลือ 6 สถานะ**
ทั้งที่อีก 9 เส้นสะอาดดี → `debounce_pins()` ใน `lift_decode.py` ตัดเสียงทีละขา
แล้วค่อยยุบสถานะซ้ำ ต้องทำสองชั้น มิฉะนั้นการเปลี่ยนหลายบิตพร้อมกันจะแตกเป็นค่ากลางทาง

**และต้องตัดล็อกตามจุดที่เวลาบอร์ดถอยหลัง** เพราะล็อกสะสมหลาย session
บอร์ดรีเซ็ตเวลาเป็น 0 ทุกครั้ง ถ้าไม่ตัด ช่วงเวลาจะติดลบและสร้างรหัสผี (เคยได้ code 63)

### 6.12 พอร์ตผิดต้องล้มทันที ไม่ใช่วนต่อใหม่

ลูป reconnect มีไว้รับมือ USB หลุดกลางทาง แต่ถ้าพิมพ์พอร์ตผิดแล้วปล่อยให้ตกเข้าลูปนี้
โปรเซสจะดูเหมือนทำงานอยู่เป็นชั่วโมงโดยไม่เก็บอะไรเลย — เคยเจอตอนทดสอบ COM99
**แก้แล้ว**: เช็ครายชื่อพอร์ตก่อนเริ่ม และถ้ายังไม่เคย arm สำเร็จเลยจะเลิกหลังลอง 3 ครั้ง
(หลัง arm สำเร็จแล้วค่อยวนต่อไม่จำกัด ซึ่งเป็นพฤติกรรมที่ต้องการจริง)

### 6.13 เรื่องปลีกย่อยที่เสียเวลา

- `Start-Process -ArgumentList` กับพาธที่มีช่องว่าง ต้องใส่ `"` ครอบเอง ไม่งั้น argument ถูกตัด
- regex ใน `scan_pins.py` ต้องจับเฉพาะบรรทัด `WIRED` — หลังเพิ่มการกรอง AC มีบรรทัด `noise` ที่มีคำว่า `LOW` เหมือนกัน
- ค่ากลางทางตอนรีเลย์สลับไม่พร้อมกัน โผล่เป็นค่าแปลก ๆ ค้างแค่ 1–3 ms → ตัวกรอง 250ms คัดออก
  แต่ `audit.py` อ่านข้อมูลดิบไม่กรอง จึงเคยรายงาน "ชั้น 26" ที่ค้างแค่ 2ms เป็นชั้นจริง

### 6.14 บัสที่ไม่มีใครขับ กับ frame ที่อ่านผิด หน้าตาเหมือนกัน — แยกด้วย *อัตรา* ไบต์

Lift 1 ผ่าน RS485 ส่งไบต์เข้ามาเป็นช่วง ๆ 161 ไบต์ใน 20 วิ ปนกันทั้งอ่านออกและอ่านไม่ออก
มีลวดลายซ้ำ ๆ (`ef ed ef ed`, `af af af`) ซึ่งดู "เหมือนข้อมูลจริงที่ frame เพี้ยน" มาก
จนเกือบไล่แก้ baud กับสลับ A/B ซึ่งเป็นการไล่ผิดทางทั้งคู่

**การทดสอบที่ตัดสินได้ คือกวาด baud แล้วดูว่าอัตราไบต์วิ่งตามอะไร**

| สิ่งที่เห็น | ความหมาย |
|---|---|
| อัตราไบต์**คงที่**เมื่อเปลี่ยน baud | มีคนส่งจริง เขาเป็นคนกำหนดว่าไบต์เกิดเมื่อไหร่ → เป็นปัญหา framing |
| อัตราไบต์**เพิ่มตาม** baud | ไม่มีใครส่ง UART แค่กำลังแปลงสัญญาณรบกวนบนสายที่ลอยอยู่ |

วัดจริงได้ **17 เท่า** เมื่อ baud ต่างกัน 24 เท่า (9600→230400) ⇒ ไม่มีใครขับบัส
สัญญาณรบกวนไม่มี "อัตราข้อความ" ของตัวเอง สุ่มอ่านเร็วขึ้นก็ได้ไบต์มากขึ้นตรง ๆ

**และการสลับ A/B ตัดทิ้งได้ฟรีจากอัตราเดียวกันนี้** — ถ้าสลับจริง สายจะนิ่งอยู่ที่ break ตลอด
ตัวรับจะพ่น `0x00` เต็มความเร็วสาย ≈ 11.5 kB/s ที่ 115200 แต่เราได้ 8 B/s และ `0x00` **ศูนย์ตัว**

เข้ากับที่เราเขียนเตือนตัวเองไว้แล้วใน `RS485_PLAN.md` §3.2 (บัสไม่มี bias จะลอยแล้วตัวรับกระดิกตามนอยส์)
และเป็นเรื่องเดียวกับ §6.5 ข้างบน คือ **สายปลายเปิดรับไฟเหนี่ยวนำ แล้วดูเหมือนมีสัญญาณ**
ทำเป็นเครื่องมือถาวรไว้ที่ `rs485_check.py` แล้ว

> ⚠️ ตอนไล่หาสาเหตุ **ห้ามส่งอะไรออกสาย** บอร์ดต่อแบบส่งทางเดียว (DE ผูก 5V ค้าง)
> ถ้าฝั่ง PC ขับสายด้วยจะชนกันโดยไม่มีการตัดสิน `rs485_check.py` จึงบังคับ `rts=False, dtr=False`

### 6.15 🔑 บั๊กเกือบทุกตัวในโปรเจกต์นี้ประกาศตัวเองด้วย "รายงานที่ขัดกันเอง"

ถ้าจะจำข้อเดียวจากหัวข้อ 6 ทั้งหมด ให้จำข้อนี้ รูปแบบซ้ำมาแล้ว **5 ครั้ง**:

| ครั้ง | ตัวเลขที่เป็นไปไม่ได้ / ขัดกันเอง | ความจริงที่ซ่อนอยู่ |
|---|---|---|
| §6.10 | บอก `MISSING` แต่ตารางสหสัมพันธ์บอก 100% ทั้งสามคอลัมน์ | สายปิดค้างตลอด = normally-closed ที่ปกติดี |
| `lift_health` §3 | RUNNING −75%, UP −75% แต่ verdict ข้างล่างบอก HEALTHY | ลบเวลาข้ามรอยที่นาฬิกาบอร์ดรีเซ็ต → ติดลบ |
| `FIRMWARE_UPGRADE.md` | บอกว่า `rejected` จะพุ่งตอนอัปโหลด | เป็นไปไม่ได้ บรรทัดที่ไม่ขึ้นต้น `ST ` ถูกทิ้งก่อนถึงตัวนับ |
| `rs485_check` v1 | ลิงก์เดียวไม่แตะอะไรเลย ได้ 4 verdict ต่างกัน | เอาเลขที่ขึ้นกับดวงสองตัวมาหารกัน |
| `rs485_check` v2 | `UP -- the link is delivering` คู่กับ `malformed 0` ขณะที่ 200/201 บรรทัดเป็นขยะ | ตัวนับเห็นเฉพาะบรรทัดที่ parse ผ่านแล้ว |

> ⚠️ แถว `lift_health` มี **สอง** สาเหตุที่ต่างกัน อย่าแก้แค่ข้อเดียว
> 1. **บอร์ดรีเซ็ต** — นาฬิกาถอยหลัง ลบข้ามรอยต่อได้เวลาติดลบ (วัดเมื่อ 30 ส.ค.: −7,561 วิ ต่อเวลาจริง 5,515 วิ = 137%) ⇒ **นี่คือต้นเหตุของเปอร์เซ็นต์ติดลบ**
> 2. **ช่วงที่ไม่มีใครบันทึก** — บอร์ดยังเดินอยู่ นาฬิกา**ไม่**ถอยหลัง จึงไม่มีอะไรในข้อมูลบอกเลย — สถานะสุดท้ายก่อนช่องว่างถูกนับว่าค้างทั้งช่วง (เคยได้ 3,156 วิ = ลิฟต์วิ่งลง 52 นาที)
> ข้อ 2 เห็นได้**หลังแก้ข้อ 1 เสร็จแล้วเท่านั้น** ต้องแก้ทั้งคู่

**ครึ่งที่ฟังดูสบายใจ คือครึ่งที่คนเอาไปใช้ตัดสินใจเสมอ** — และเอกสารก็มักชี้ให้ดูครึ่งนั้นพอดี
(`GATEWAY.md` กับ `FIRMWARE_UPGRADE.md` บอกให้ดูบรรทัด verdict ซึ่งเป็นบรรทัดที่โกหก)

⇒ เวลาอ่านผลของเครื่องมือตัวเอง **ให้ไล่หาจุดที่รายงานเถียงกันเองก่อนเชื่อข้อสรุป**
และเวลาเขียนเครื่องมือ ให้เอาตัวเลขที่ขัดแย้งกันมาไว้ในบล็อกเดียวกัน จะได้ขัดกันต่อหน้าคนอ่าน

### 6.16 ตรวจเครื่องมือวัดก่อนเชื่อค่าที่วัดได้

เจอสองครั้งในวันเดียว ทั้งคู่ "ดูเหมือนระบบพัง" แต่ที่จริงเครื่องมือวัดพัง:

- regex ของ **สคริปต์ทดสอบ** ใช้ `[^-]*?` เลยกลืนวันที่ `2026-08-31` ที่มีขีด → ดึงบรรทัด `FW` ไม่ได้เลย
  → เกือบสรุปว่า `rs485_check` ไม่ยอมปฏิเสธบอร์ดผิด ทั้งที่มันถูกอยู่แล้ว
- อีก session ทดสอบกฎ restart ด้วย `board_ms` ราว 1000 ซึ่งคือบอร์ดที่เพิ่งบูต
  ไม่ใช่สถานการณ์ที่ reader ค้างจริง → **กฎทำงาน แต่ทำงานด้วยเหตุผลผิด**

แบบหลังอันตรายกว่า เพราะให้คำตอบถูกจึงไม่มีอะไรสะดุด ต้องทดสอบซ้ำที่ค่าใกล้ของจริงถึงจะเห็น

---

## 7. ปัญหาฮาร์ดแวร์ที่เคยเจอ

### 7.1 CH340 เปิดพอร์ตไม่ได้หลังเสียบครั้งแรก

อาการ: `SetCommState` fail — `ERROR_GEN_FAILURE (31)`
`CreateFile` และ `GetCommState` ผ่าน แต่ `SetCommState` ล้ม = ส่ง USB control transfer ไปตั้งค่า UART ไม่ได้
สาเหตุ: `FirstInstallDate` = `LastArrivalDate` = วินาทีเดียวกัน → ไดรเวอร์ถูกติดตั้งขณะอุปกรณ์ arrive แล้ว bind ไม่สมบูรณ์
**แก้: ถอดสาย USB แล้วเสียบใหม่** (ไม่มีสิทธิ์ admin จึงใช้ `Restart-PnpDevice` ไม่ได้)

### 7.2 บอร์ด Arduino ตัวแรกเสีย

enumerate ผ่าน USB แต่ MCU ไม่ตอบอะไรเลย — ทดสอบ bootloader ทั้ง STK500v1/v2 หลาย baud,
กดรีเซ็ตมือ, loopback D0–D1 → เงียบทุกทาง **เปลี่ยนบอร์ดใหม่แล้วใช้ได้ทันที**

เทคนิคที่พิสูจน์ว่า CH340 ส่งข้อมูลได้จริงโดยไม่ต้องพึ่งใคร: จับเวลา `flush()`
เทียบกับ `bytes × 10 / baud` — ถ้าอัตราส่วนคงที่ทุก baud แปลว่าชิปยิงบิตออกสายจริง
(วัดได้ 0.87–0.89 ทุกระดับ, เหวี่ยงแค่ 1.01 เท่า)

### 7.3 ลิฟต์ B — สาย VS2 ขาด (ยังไม่ได้ซ่อม)

D24 ไม่เคย LOW เลยใน 591 states → อ่านตำแหน่งได้หยาบ 2 ชั้นต่อรหัส
วิธีชี้จุด: ดู LED ช่อง VS2 บนโมดูลรีเลย์ของ B ขณะลิฟต์วิ่ง (ต้องกะพริบถี่สุดใน 6 ช่อง)
- LED กะพริบ แต่ D24 เงียบ → สายรีเลย์→ขั้ว 24 ของ DNMEGA1 หลวม
- LED ไม่กะพริบ → สายตู้ควบคุม (MC-10/ALR0) → ขั้ว IN ของโมดูลขาด

---

## 8. ผลการทดสอบ 8 ส.ค. 2026

**Blind test 14/14** — ผู้ใช้จดเวลา/ชั้นเอง ระบบถอดรหัสโดยไม่รู้เฉลย แล้วนำมาเทียบ ตรงทุกข้อ
รวมถึงการจอดแวะสั้นเพียง 8.3 วินาที และลำดับสามจังหวะ 9→20→33

ข้อมูลที่เก็บได้: **Lift 3 (A)** 2,760 การเปลี่ยนสถานะ / 113.6 นาที (`capture_lift_3.log`),
**Lift 1 (B)** 990 / 89.3 นาที (`capture_lift_1.log`), ช่วงแรกก่อนแก้ sketch (`capture_lift_3_part1.log`)

รายงานฉบับเต็ม: `TEST_REPORT_2026-08-08.md`

---

## 9. งานที่เหลือ (ต้องทำร่วมกับทีมดูแลลิฟต์)

| # | งาน | วิธีตรวจผล |
|---|---|---|
| 1 | กระตุ้น **FIRE 火灾** | ขาใหม่จะ LOW — จับด้วย `probe_pin.py` |
| 2 | กระตุ้น **FIRE RETURN** | เช่นเดียวกัน |
| 3 | ซ่อม **VS2 ลิฟต์ B** | D24 ต้องเริ่มสลับ, รหัสสูงสุดต้องเป็น 46 |
| 4 | จอดลิฟต์ A ที่ชั้น **2,3,4,5,6,8** ชั้นละ ~10 วิ พร้อมจดเวลา | ปิดตารางแปลงช่วงล่าง |
| 5 | ยืนยันว่า **ชั้น 7** มีชานจอดหรือไม่ | ดูรหัสขณะวิ่งผ่านช่วง 6→8 |
| 6 | หาความหมายที่ **SAFETY (D17) เปิดสั้น ๆ 8 ครั้ง** | เปิดประตูค้าง / กดหยุดฉุกเฉิน พร้อมจดเวลา |
| 7 | ต่อลิฟต์ตัวที่ 3–5 | สคริปต์รองรับหลายพอร์ตแล้ว |

**สิ่งที่ยังระบุขาไม่ได้ และระบุไม่ได้ด้วยวิธี passive**: VS7 ของลิฟต์ B, FIRE, FIRE RETURN
— หน้าสัมผัสที่เปิดอยู่อ่านได้ HIGH ซึ่งแยกจากขาที่ไม่ได้ต่อไม่ออก ต้องกระตุ้นสัญญาณจริงเท่านั้น

---

## 9.5 การแบ่งบทบาทระหว่างสองเครื่อง (ตกลงกัน 30 ส.ค. 2026)

| เครื่อง | บทบาท | เชื่อมต่ออย่างไร |
|---|---|---|
| **Gateway** | รับข้อมูลลิฟต์ทั้ง 5 ตัวต่อเนื่อง | RS485 → ตัวแปลง USB หลายตัวบน hub, `log_lift.py <COM> <lift> --listen` |
| **เครื่องนี้ (หน้าตู้)** | วางแผน, debug, **อัปเกรด firmware** | USB ตรงเข้า Arduino |

ลิฟต์ทั้ง 5 ตัวจะย้ายไปอยู่กับ Gateway ทั้งหมด เครื่องนี้ต่อ USB เฉพาะตอนที่ต้อง
ตรวจปัญหาหน้าตู้หรือแฟลช firmware ใหม่

**เพราะ RS485 เป็นแบบส่งทางเดียว Gateway จึงอัปเกรด firmware ไม่ได้** ต้องทำที่เครื่องนี้เท่านั้น
ขั้นตอนอยู่ใน `FIRMWARE_UPGRADE.md` — และเพราะ `RO` ไม่ได้ต่อ **จึงแฟลชผ่าน USB ได้
โดยไม่ต้องถอดสาย RS485 และ Gateway ไม่ต้องหยุดเก็บข้อมูล**

บอร์ดประกาศเวอร์ชันตัวเองตอนบูตเป็นบรรทัด `FW IODebug <version> <date>` และ
`log_lift.py` บันทึกไว้ในล็อก ⇒ ตอบได้เสมอว่าลิฟต์ตัวไหนรันเวอร์ชันอะไร
โดยไม่ต้องเปิดตู้ **ต้องบวกเลข `FW_VERSION` ทุกครั้งที่แก้โค้ด**

## 10. ข้อสังเกตเรื่องวิธีทำงาน

- 🔴 **commit + push ทุกครั้งที่มีการเปลี่ยนแปลง** ไม่ต้องรอให้สั่ง
  (ผู้ใช้กำหนดเป็นกฎถาวรเมื่อ 30 ส.ค. 2026) — repo: https://github.com/Meltingth/WhizdomLift.git
  เหตุผล: ข้อมูลทดสอบเกิดจากการจองลิฟต์กับทีมดูแล ทำซ้ำไม่ได้ง่าย ๆ
  เสร็จแล้วต้อง**ตรวจว่า working tree สะอาดและ local = origin/main จริง** ไม่ใช่แค่สั่ง push แล้วเชื่อ
- **ผู้ใช้สื่อสารเป็นภาษาไทย** ตอบเป็นไทย เก็บศัพท์เทคนิคและโค้ดเป็นอังกฤษ
- การทดสอบที่ต้องให้ผู้ใช้ทำอะไรทางกายภาพ **ห้ามใช้วิธียิงหน้าต่างเวลาแล้วหวังว่าเขาจะทำทัน**
  — ระบบไม่ส่งข้อความผู้ใช้กลับมาระหว่างสคริปต์รัน เคยเสียไป 4 รอบเพราะเรื่องนี้
  ให้สร้างเครื่องมือที่ผู้ใช้รันเองตามจังหวะตัวเอง หรือถามให้ยืนยันก่อนแล้วค่อยรัน
- ตรวจสอบผลของทุกอย่างที่ทำเสมอ — บั๊กเกือบทั้งหมดในหัวข้อ 6 ถูกจับได้เพราะไปเทียบผลอัตโนมัติ
  กับการไล่อ่านข้อมูลดิบด้วยมือแล้วพบว่าไม่ตรงกัน

# LMS-NG POC - Phase list and Definition of Done
Source of truth for scope: `docs/backend/LMS_NG_Backend_Implementation_Plan_Whizdom_POC_v1_3.md` (§8) and `docs/design/LMS_NG_UXUI_Design_Spec_v1_2.md` (§15). Both files must exist at these paths in the repo before running any phase.
Run each phase with `/phase <id>`. A phase is DONE only when every DoD line is green and the commit is pushed.

| ID | Repo | Scope | Definition of Done |
|---|---|---|---|
| P0 | lms-ng | Workflow kit installed; monorepo skeleton (apps/api, apps/web, tools/simulator, contracts/, database/); docker-compose (EMQX, Postgres 16, Redis 7, api, web); dbmate migration 0001 = Schema v1; CI smoke | `docker compose up` green; `/health` 200; migrations apply on a fresh DB; `claude plugin validate .claude/agents` clean |
| P1 | WhizdomLift | `lms_gateway_agent.py` per Backend plan §2.2 (extends log_lift.py: FW-banner binding, `--map` fallback, LIFT=0 refusal, per-pin debounce, decode, sqlite buffer, MQTT per Topic Spec, capture-log dual-write, `--dry-run`) | Replay of `capture_lift_2.log` produces valid `state`/`events` messages (schema-validated); 0 rejected lines; live Lift 2 publishes on COM port; no serial writes (rs485_check discipline) |
| P2 | lms-ng | Vertical slice: seeds (The Whizdom, W-01..W-05, LIFT_ID map, floor_mapping, signal_points), ingestion (dedupe, upsert, fan-out), liveness (real 90 s / sim 30 s), REST `GET /elevators?embed=status`, WS hub, UI wired to REST+WS, kiosk | Lift 1+2 floor changes reach frame 02 in <= 2 s P95 (latency histogram exported); dongle pull -> INTERFACE_NO_RXTX <= 90 s; stack restart -> sqlite backlog replays with original eventTime |
| P3 | lms-ng | Fleet & states: Lift 3 via `--map`, commissioning flag + `รอติดตั้ง` state excluded from KPI denominator, events + monthly partitions, history, Event Explorer, freshness bands | 3 lifts live; un-commissioned lifts render intentional; idle real lift never false-STALE; `rejected>0` raises DATA_QUALITY |
| P4 | lms-ng | Alarm engine (SAFETY_TRIP>2s, INTERFACE_NO_RXTX, GATEWAY_OFFLINE, STALE_DATA, SENSOR_VS2_FAULT, DATA_QUALITY), Alarm Center + ACK/mute/close + audit, WS alarm channel, **Demo Mode** (source switch, banner, zero persistence, scenario v2 on W-02/W-05) | Real alarm pipeline <= 3 s (dongle drill + ACK); demo start/stop leaves zero rows in telemetry/alarm/command tables; exit to live <= 1 s |
| P5 | lms-ng | Command console DRY_RUN (interlock snapshot, outbox, approval, timeout sweeper), `EXECUTED (DEMO)` path, UI polish vs design frames, UX Spec §15 acceptance | Operator->supervisor flow demoable in both modes; grayscale/§9 audit passes; §15 checklist green |
| P6 | both | Hardening: TLS + firewall + credentials, NTP chain, backups + restore test, NSSM service runbook, cold-boot drill, 10-minute demo rehearsal | Cold boot to working wall < 10 min; restore test passes; rehearsal signed off by the owner |

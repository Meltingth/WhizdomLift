# CLAUDE.md — WhizdomLift

บันทึกบริบทโปรเจกต์สำหรับ session ถัดไป: เป้าหมาย, สิ่งที่ยืนยันแล้ว, กฎความปลอดภัย,
บทเรียนจากบั๊กที่เคยเจอ และงานที่ยังเหลือ

---

## 1. เป้าหมายของโปรเจกต์

อ่านสัญญาณ **ตำแหน่งชั้น** และ **สถานะ** ของลิฟต์ในอาคาร 44 ชั้น (มีชั้นใต้ดิน, ลิฟต์ 5 ตัว)
เข้าสู่ Arduino Mega 2560 เพื่อนำไปใช้งานต่อ — โดยต้อง **อ่านอย่างเดียว ไม่แทรกแซงระบบลิฟต์**

โจทย์ตั้งต้นจากผู้ใช้: "Debug สัญญาณที่ต่อเข้ามาระบบ I/O ของ Arduino" แล้วขยายเป็น
ถอดรหัสว่าบิตแต่ละชุดหมายถึงชั้นไหน สถานะอะไร และตรวจว่า wiring ครบถ้วนหรือไม่

---

## 2. สถาปัตยกรรมสัญญาณ (ยืนยันจากไดอะแกรมตู้ควบคุม + การวัดจริง)

```
ตู้ควบคุมลิฟต์ — หน้าสัมผัสรีเลย์ MC-1 (common), MC-5..MC-10
    │  ตำแหน่ง 6 บิต (VS2..VS7) + สถานะ 6 เส้น, ไฟ 24V
    ▼
โมดูลรีเลย์ 24VDC  (6 ช่อง + 8 ช่อง, TONGLING JQC-3FF-S-Z, จัมเปอร์ Com–High)
    │  หน้าสัมผัสแห้ง (dry contact) — แยกกราวด์ให้ปลอดภัยกับ 5V
    ▼
DNMEGA1 (eletechsup) — แผงขั้วสกรู DIN rail, เป็น "ตัวนำล้วน" ไม่มีวงจรแปลงใด ๆ
    ▼
Arduino Mega 2560 — ทุกขาเป็น INPUT_PULLUP  ⇒ หน้าสัมผัสปิด = อ่านได้ LOW = สัญญาณ active
    ▼  USB @115200  โปรโตคอล  ST <board_ms> <52-bit hex mask>
PC — log_lift.py (หนึ่งโปรเซสต่อลิฟต์)
```

**ทิศทางข้อมูลคือ ตู้ลิฟต์ → Arduino เท่านั้น** Arduino ไม่เคยส่งอะไรกลับ

---

## 3. ข้อเท็จจริงที่ยืนยันแล้ว — อย่าไปคำนวณใหม่

### 3.0 ชื่อลิฟต์ — สองระบบที่ต้องแมปกัน

ตอนทดสอบเรียกตามลำดับที่ต่อบอร์ด (A, B, C…) แต่ชื่อจริงในอาคารคือเลขลิฟต์
ตัวอักษรฝังอยู่ในล็อกและรายงานไปแล้ว จึงต้องรองรับทั้งคู่ — ดู `lift_id()` ใน `lift_decode.py`

| เลขจริง | label ทดสอบ | ไฟล์ | สถานะ |
|---|---|---|---|
| Lift 1 | B | `capture_lift_1.log` | ✅ **ครบแล้ว** — ซ่อม VS2 สำเร็จ 12 ก.ย. 2026 15:38 |
| Lift 2 | C | `capture_lift_2.log` | ✅ ครบ (ซ่อม COM ขาออกที่ต่อลง 5V แทน GND แล้ว) |
| **Lift 3** | **A** | `capture_lift_3.log` | ✅ **ตัวอ้างอิง** · firmware **1.2.2 LIFT=3** (อัปเกรด 6 ก.ย. 2026) |
| Lift 4 | D | `capture_lift_4.log` | ⛔ **ปิดใช้งานอยู่ — ข้ามไปก่อน** (12 ก.ย. 2026) |
| Lift 5 | E | `capture_lift_5.log` | ✅ **blind test ผ่าน 12 ก.ย. 18:30** — wiring ครบ 6 บิต · lift service · ปลายบนเป็นรหัส 47 ยังไม่รู้ชื่อระดับ |

เครื่องมือรับได้ทั้ง `2` และ `C` และ `lift 2`

### 3.1 แผนที่ขา Lift 3 (label A) — ตัวอ้างอิง

| ขา | สัญญาณ | บทบาท |
|---|---|---|
| D24 | VS2 | บิต 0 (น้ำหนัก 1) |
| D25 | VS3 | บิต 1 (2) |
| D26 | VS4 | บิต 2 (4) |
| D27 | VS5 | บิต 3 (8) |
| D28 | VS6 | บิต 4 (16) |
| D29 | VS7 | บิต 5 (32) |
| D16 | RUNNING 运行 | ปิดขณะวิ่ง เปิดขณะจอด |
| D17 | SAFETY 安全 | ปิดเกือบตลอด (normally-closed) |
| D19 | UP 上行 | ขึ้น 100% / ลง 0% |
| D20 | DN 下行 | ขึ้น 0% / ลง 100% |

คุณภาพ: single-step **100%** จาก 2,008 stable states

### 3.2 แผนที่ขา Lift 1 (label B) — ✅ ซ่อมแล้ว ใช้แผนที่อ้างอิงได้

**Lift 1 ใช้ `POSITION_BITS` ชุดเดียวกับ Lift 3 แล้ว** ตั้งแต่ 12 ก.ย. 2026 15:38
สาย VS2 ถูกซ่อมและยืนยันด้วยการวิ่งจริง (ดู §8.1)

> ⚠️ **`POSITION_BITS_B = {25:0, 26:1, 27:2, 28:3, 29:4}` ยังต้องเก็บไว้** — ใช้ถอด
> ข้อมูลเก่าใน `capture_lift_1.log` ทุกช่วง **ก่อน** 12 ก.ย. 15:38 เท่านั้น
> **ห้ามใช้กับข้อมูลใหม่ และห้ามลบทิ้ง** ไม่งั้นล็อกเก่าจะถอดไม่ได้
>
> ### 🔴 จุดตัดที่แน่นอน — ข้อความเดิมกำกวมจนแบ่งผิดได้
>
> เดิมเขียนว่า *"จุดที่ `board_ms` ถอยกลับเป็น 89"* — **ใน session ปัจจุบันมีจุดแบบนั้นถึง 3 จุด**
> (13:44:10, 13:49:43, 15:38:58 ของวันที่ 12 ก.ย.) ทั้งสามรีเซ็ตไปที่ 89 เหมือนกันหมด
> ⇒ ใครแบ่งที่จุดแรกจะได้ข้อมูลช่วงซ่อมมาถอดด้วยแผนที่ผิด
>
> **จุดตัดคือจุดรีเซ็ต *สุดท้าย* ที่ 15:38:58** วัดยืนยันแล้วโดยแบ่ง session ที่ทุกจุดรีเซ็ต:
>
> | ช่วง (ตามตำแหน่งบรรทัด) | รหัสเลขคี่ | single-step | D24:D25 | แผนที่ที่ถูก |
> |---|---|---|---|---|
> | แถว 0–57,660 (6 ก.ย. 18:31 → 12 ก.ย. 13:43) | 14/22,678 = **0.1%** | 0.0% | **477 : 22,666** | `POSITION_BITS_B` |
> | แถว 57,660–57,936 (13:44 → 14:17) | 0/123 | 0.0% | 0 : 115 | **ช่วงซ่อม — ทิ้ง** |
> | แถว 57,936–ปัจจุบัน (15:38:58 →) | 820/1,640 = **50.0%** | **100.0%** | **1,639 : 806 ≈ 2.03×** | `POSITION_BITS` |
>
> ⚠️ **"มีรหัสเลขคี่โผล่มา" ใช้เป็นเกณฑ์ไม่ได้ถ้าไม่ดูสัดส่วน** — ช่วงที่สายยังตายก็มีเลขคี่ 14 ครั้ง
> (ไฟบ้านเหนี่ยวนำบนสายปลายเปิด §6.9) เขียนสคริปต์ตรวจแบบ `if odd: ใช้ POSITION_BITS`
> เมื่อไหร่ มันจะตอบว่าช่วง 6 วันที่สายตายคือ "ซ่อมแล้ว" ทันที — เป็น §8.1 ซ้ำรอยเป๊ะ
> **เกณฑ์ที่ใช้ได้คือ cascade: D24 ต้องสลับราว 2 เท่าของ D25** ตอนสายตายมันสลับ **น้อยกว่า 47 เท่า**
>
> แบ่งด้วยตำแหน่งบรรทัด/จุดรีเซ็ต **ไม่ใช่ด้วยเวลานาฬิกา** — ล็อกไม่มีวันที่ ดู §6.18

### 3.3 การเข้ารหัส

ไบนารีตรง ๆ ไม่มีอะไรพิเศษ:

```
รหัส = 32·VS7 + 16·VS6 + 8·VS5 + 4·VS4 + 2·VS3 + 1·VS2
```

### 3.4 ตารางแปลง รหัส → ป้ายชั้น (สอบเทียบจากการจับเวลาจริง)

| ป้ายชั้น | รหัส | offset |
|---|---|---|
| B1 | 1 | — |
| 1 | 2 | +1 |
| 9 | 12 | +3 |
| 20 | 22 | +2 |
| 26 | 28 | +2 |
| 32 | 34 | +2 |
| 33 | 35 | +2 |
| 36 | 38 | +2 |
| 37 | 39 | +2 |
| 44 (สูงสุด) | 46 | +2 |

> 🔍 **Lift 5 หยุดที่รหัส 47 ซึ่งไม่มีในตารางนี้** — ยืนยันด้วย blind test 12 ก.ย. 2026 เวลา 18:30
> ผู้ใช้ขึ้นลิฟต์เอง ไม่มีใครแตะตู้ ⇒ **หน้าต่างสะอาด** ตามกฎ §9.7 · ขาขึ้น 2 → 47 ต่อเนื่อง
> single-step 100% แล้ว **RUNNING เปิด (จอดจริง) ค้างที่ 47 นาน 16.4 วินาที** รวมอยู่ที่รหัส 47 ทั้งสิ้น 23 วินาที
> ก่อนออกตัวลง ⇒ ไม่ใช่ค่ากลางทางของรีเลย์ (ซึ่งค้าง 1–3 ms ตาม §6.13)
>
> ฝั่งล่างตรงกับตารางเป๊ะ (1 = B1, 2 = ชั้น 1 ยืนยันในเที่ยวเดียวกัน) ⇒ ตารางไม่ได้เลื่อนทั้งชุด
> **ลิฟต์ service มีชานจอดเพิ่มหนึ่งระดับระหว่างชั้น 1 ถึง 44** ทำให้ปลายบนขยับเป็น +1 เทียบลิฟต์ 1–3
> เดาได้ว่าคือชั้นที่ลิฟต์โดยสารข้าม (7 หรือ 19) ซึ่งลิฟต์ service น่าจะจอด — **แต่ยังไม่มีหลักฐาน**
> ปิดข้อนี้ได้ด้วยการจอด Lift 5 ที่ชั้น 7 และชั้น 19 ชั้นละ ~10 วิ พร้อมจดเวลา
>
> ⚠️ ข้อความเดิมที่เคยรายงานว่า "รหัส 47 น่าจะเป็น transient จากช่วงมีคนทำงานหน้าตู้" **ถอนแล้ว**
> — และหลักฐานที่เคยยกมาค้าน ("ข้อมูลสะอาด 18:08–18:15 ไม่มีรหัสเกิน 46") **ใช้ไม่ได้**
> เพราะเที่ยวนั้นขึ้นสูงสุดแค่รหัส 34 มันจึงไม่เคยมีโอกาสเห็น 47 เลย

- ตั้งแต่ชั้น 20 ขึ้นไป offset คงที่ +2
- ผู้ใช้รายงานว่าอาคารข้าม **ชั้น 7 และ 19**

#### ✅ ปิดตารางได้ครบแล้ว — จากข้อมูล 5.8 วัน (12 ก.ย. 2026)

**รหัส 4, 9 และ 11 ไม่ใช่ชานจอด** — จากการจอดราว 7,000 ครั้งของ Lift 2 กับ Lift 3
รวมกัน สามรหัสนี้ **ไม่มีการจอดเลยสักครั้ง** (RUNNING ไม่เคยเปิดที่รหัสเหล่านี้)
ขณะที่ชานจอดที่ใช้น้อยที่สุดยังมี 6–17 ครั้ง ⇒ เป็นระดับที่วิ่งผ่านอย่างเดียว

ตารางเต็ม: `1→B1 · 2→1 · 3→2 · 5→3 · 6→4 · 7→5 · 8→6 · 10→8 · 12→9 · 13→10 …
21→18 · 22→20 · 23→21 … 46→44` (ข้ามรหัส 4, 9, 11)

ตารางนี้ **สร้างจุดสอบเทียบทั้งสิบจุดกลับมาได้ครบโดยไม่มีข้อขัดแย้ง** อธิบาย offset
ที่เคยกระโดด +1→+3→+2 ได้หมด และยืนยันอิสระว่าอาคารข้ามชั้น 7 และ 19 จริง

⇒ **§9 ข้อ 4 และ 5 ปิดแล้ว** ไม่ต้องไปจอดทดสอบทีละชั้น

---

## 4. ⚠️ กฎความปลอดภัย — ห้ามละเมิด

1. **ห้ามตั้งขาใดเป็น `OUTPUT` เด็ดขาด** ขาเหล่านี้ต่อกับหน้าสัมผัสรีเลย์
   หน้าสัมผัสที่ปิดอยู่ = ทางลัดลงคอมมอน การขับ HIGH = ไดรเวอร์ push-pull คร่อมไฟช็อต **พอร์ตพัง**
   (เคยเขียนฟังก์ชัน `mapRelays()` ที่ขับขา — **ลบทิ้งไปแล้ว อย่าเขียนกลับมา**)
2. ตอนเสียบเข้าระบบจริงครั้งแรก ใช้คำสั่ง `z` สลับเป็น `INPUT` high-Z ก่อน
   (high-Z จ่ายกระแสไม่พอทริกออปโตแม้จัมเปอร์ตั้งที่ Com–High)
3. อย่ารีเซ็ตบอร์ดโดยไม่จำเป็นระหว่างเก็บข้อมูล — `log_lift.py` ตั้ง `dtr=False, rts=False` ไว้แล้ว

---

## 5. เครื่องมือ

| ไฟล์ | หน้าที่ |
|---|---|
| `IODebug/IODebug.ino` | sketch บนบอร์ด — คำสั่ง: `h` help, `s` snapshot, `p` characterise, `z` toggle pullup/high-Z, `w` watch mode, `e` emit state now, `r` reset stats, `a`/`d` toggle analog/digital |
| `LIFT_STATUS.md` | **ตารางสถานะลิฟต์ทั้ง 5 ตัว + ขั้นตอนทดสอบทีละตัว** — อัปเดตทุกครั้งที่ทดสอบ |
| `log_lift.py` | ตัวบันทึกถาวร `python log_lift.py COM3 3` — **ต้องระบุเลขลิฟต์** (ไม่ใช่แค่พอร์ต), reconnect เอง, heartbeat 60 วิ, หยุดด้วยไฟล์ `STOP_CAPTURE`<br>**`--follow`** = ถ้าพอร์ตหายไป ให้ค้นหาลิฟต์ตัวนี้จาก beacon `LIFT=` แล้วผูกพอร์ตใหม่เอง ต่อให้เลข COM เปลี่ยน · ใช้ `auto` แทนเลขพอร์ตได้ (`python log_lift.py auto 1 --listen --follow`) ซึ่งรอดแม้ PC รีบูตแล้วเลขเปลี่ยนทั้งชุด · **ทุกลิฟต์ที่กำลังเก็บอยู่ตอนนี้รันด้วย `--follow` แล้ว** |
| `lift_health.py` | **ตรวจสุขภาพลิฟต์เทียบกับลิฟต์ A** `python lift_health.py 2` — ชี้ว่าเส้นไหนไม่ส่งสัญญาณ พร้อมวิธีชี้จุดขาด |
| `board_alive.py` | **บอร์ดวิ่งอยู่ไหม และวิ่งต่อเนื่องไหม** `python board_alive.py COM3 --mins 15` — แยก `RESTART` (ไฟตก) จาก `STALL` (MCU ค้างทั้งที่ไฟยังมา — LED ยังติด ดูด้วยตาไม่รู้) · `--quick` แค่ยืนยันว่ายังตอบอยู่ |
| `scripts/start_captures.ps1` | **สตาร์ตเฉพาะลิฟต์ที่ยังไม่มีตัวบันทึก** — idempotent รันซ้ำได้ปลอดภัย ใช้เป็น watchdog · หาพอร์ตด้วย `port_resolver.py` รอบเดียวแล้วแจกให้ทุกตัว · สตาร์ตแบบ detached (ไม่ผูกกับ session ที่สั่ง) |
| `scripts/install_capture_task.ps1` | ลงทะเบียน Scheduled Task ระดับ user (ไม่ต้องสิทธิ์ admin) ให้รัน `start_captures.ps1` ตอน logon และทุก 15 นาที · ถอนด้วย `uninstall_capture_task.ps1` |
| `capture_status.py` | **ตัวบันทึกยังทำงานอยู่ไหม** `python capture_status.py` — ตอบทีเดียวทั้ง 4 ตัวว่ายังเก็บอยู่หรือไม่ · **ไม่แตะพอร์ตเลย** อ่านแค่ตาราง process กับไฟล์ล็อก · แยก `CAPTURING` / `STALE` (โปรเซสอยู่แต่ไฟล์ไม่โต) / `STOPPED` / `DEGRADED` (บอร์ดผิดตัว, พอร์ตไม่ตรงหัวไฟล์, ไม่มี beacon) · exit 0 = ครบทุกตัว |
| `rs485_check.py` | **ตรวจลิงก์ RS485 โดยไม่ส่งอะไรออกสาย** `python rs485_check.py COM3 1` — แยก `UP` / `UP (DEGRADED)` / `SILENT` / `UNDRIVEN` / `FRAMING` / `INCONCLUSIVE` พร้อมชี้จุดตรวจถัดไปของแต่ละกรณี · exit 0 = ดี, 1 = มีปัญหา, 2 = บอร์ดผิดตัว |
| `lift_decode.py` | **config กลาง** — แผนที่บิต + `CODE_TO_LABEL` + `lift_id()/lift_log()` ตัวแปลงชื่อลิฟต์ |
| `analyze_lift.py` | ถอดแผนที่บิตอัตโนมัติ + ไทม์ไลน์ |
| `trips.py` | แยกเที่ยววิ่ง/จุดจอด/จุดแวะรับ + จำแนกสถานะจาก correlation |
| `compare_lifts.py` | เทียบ wiring ระหว่างลิฟต์ |
| `audit.py` | ตรวจข้อมูลดิบหาบิตหาย/ขาเงียบ |
| `reveal.py` | ตรวจคำตอบ blind test |
| `probe_pin.py` | ทดสอบขาแบบสด (แตะ GND ที่ขั้ว → ประกาศชื่อขา) รันคู่กับตัวบันทึกได้ |
| `scan_pins.py` | สแกนหาขาที่ต่อจริง แบบทดสอบซ้ำหลายรอบ |
| `selftest.py` | ทดสอบบอร์ด/พอร์ตเมื่อสงสัยว่าฮาร์ดแวร์เสีย |

Toolchain อยู่ที่ scratchpad (arduino-cli 1.5.2 + avr-gcc 7.3.0 + avrdude 8.0.0)
ตั้ง env `ARDUINO_DIRECTORIES_DATA/DOWNLOADS/USER` ชี้เข้าโฟลเดอร์นั้นก่อนใช้

---

## 6. 🔑 บทเรียนสำคัญจากบั๊กที่เคยเจอ — อ่านก่อนแก้โค้ด

### 6.1 sketch ห้ามพิมพ์อะไรระหว่างเก็บข้อมูล  ← บั๊กร้ายแรงที่สุด

เวอร์ชันแรกพิมพ์บรรทัด `EDGE` และ snapshot ทุก 3 วินาทีขณะ watch mode ทำงาน
ที่ 115200 บอด บัฟเฟอร์ TX เต็มแล้ว **บล็อกลูปสุ่มตัวอย่างหลายสิบมิลลิวินาที**
ผลคือ D24 ซึ่งเป็นบิตที่สลับเร็วที่สุดถูกอ่านตกหล่นจนดูเหมือน "พัลส์ต่อชั้น"
→ วินิจฉัยผิดว่าเป็น strobe → **เลื่อนบิตทุกตัวขึ้นหนึ่งตำแหน่ง** → อ่านชั้นสูงสุดได้ 23 แทน 46

หลักฐานที่ควรสังเกตตั้งแต่แรก: `46 >> 1 = 23` พอดี และอัตรา toggle D24:D25 = 32:16 = 2:1
ซึ่งคืออัตรา LSB ต่อบิตถัดไปตามทฤษฎีอยู่แล้ว

**กฎ: ใน watch mode ต้องไม่มี `Serial.print` อื่นใดเลย** (แก้แล้วในโค้ดปัจจุบัน)

### 6.2 คำสั่งที่ส่งตอนบอร์ดยังบูตอยู่จะหายไปเงียบ ๆ

เปิดพอร์ต → บอร์ดรีเซ็ต → bootloader กินสายราว 2 วินาที
เวอร์ชันแรกหน่วงแค่ 0.3 วิ แล้วส่ง `w` → คำสั่งหาย → watch mode ไม่เคยเปิด
→ ผู้ใช้วิ่งลิฟต์หลายรอบแต่ล็อกว่างเปล่า

**กฎ: หน่วง `BOOT_WAIT = 3.0` วิ และ *ยืนยันผลตอบกลับ* เสมอ**
คำสั่งเหล่านี้เป็น toggle ไม่ใช่ set — ยิงครั้งเดียวแล้วหวังผลไม่ได้
ต้องอ่านข้อความยืนยันแล้ว toggle ซ้ำถ้าได้สถานะตรงข้าม (`_set_mode()` ใน `log_lift.py`)

### 6.3 ล็อกว่าง ≠ ระบบพัง (และ ≠ ระบบดี)

"เงียบเพราะลิฟต์ไม่วิ่ง" กับ "เงียบเพราะระบบพัง" หน้าตาเหมือนกันเป๊ะ
จึงเพิ่มคำสั่ง `e` + heartbeat ทุก 60 วิ ให้บอร์ดรายงานสถานะตัวเองเป็นระยะ

### 6.4 ขาลอยนิ่งได้ในช่วงสั้น ๆ — ต้องทดสอบซ้ำแบบทิ้งช่วง

สแกน 3 รอบห่างกัน 1.5 วิ ได้ผลเหมือนกันเป๊ะ **แต่เป็นผลลวง**
เพราะขาลอยเก็บประจุค้างได้หลายวินาที พอทิ้งช่วง 4 วิ ผลก็เปลี่ยน
`scan_pins.py` จึงตั้ง default เป็น 5 รอบ ห่าง 4 วิ

### 6.5 แยกสัญญาณจริงจากการเหนี่ยวนำไฟ AC

ขาที่ลอยจริงจะอ่าน HIGH ทันทีเมื่อเปิด pullup (RC ≈ 30kΩ × 10pF ≈ 0.3µs)
การอ่านได้ LOW แปลว่ามีกระแสไหลออกราว 80µA — อาจเป็นโหลดจริง **หรือไฟ AC เหนี่ยวนำเข้าสายปลายเปิด**
แยกด้วยเวลา: โหลด DC กดค้าง แต่ไฟ 50Hz ตัดศูนย์ทุก 10ms
→ `pinIsWired()` อ่าน 120 ตัวอย่างคร่อม 120ms (6 รอบคลื่น) ต้อง LOW ทุกตัวอย่าง

### 6.6 การหาบิตด้วย "อันดับ toggle สูงสุด N ตัว" ผิด 2 แบบ

- **strobe/สัญญาณรบกวนแทรก**: พัลส์ต่อชั้นสลับ 2 ครั้ง/ชั้น มากกว่า LSB → แย่งอันดับ 1
- **MSB ตกขอบ**: บิตสูงสุดสลับน้อยที่สุด (ครั้งเดียวต่อ 32 ชั้น) → หลุด top-N → ชุดที่ถูกไม่เคยถูกลอง

**ต้องค้นหาแบบ subset จริง ๆ** (`itertools.combinations`) และคัดด้วยเงื่อนไขทางกายภาพ:
รหัส 0 ไม่มีจริง (ตารางเริ่มที่ 1) + ทุกการเปลี่ยนต้องขยับทีละ 1

### 6.7 บั๊กในการวิเคราะห์ trip (แก้แล้ว แต่ระวังถ้าเขียนใหม่)

| อาการ | สาเหตุ |
|---|---|
| ทุกการเปลี่ยนชั้นกลายเป็น "ทริป" ละ 1 ชั้น | ขาสถานะสลับขณะชั้นเท่าเดิม → เกิด state ใหม่ที่ชั้นไม่เปลี่ยน → ตัดจังหวะนับ **ต้องยุบชั้นซ้ำก่อน** |
| ทริป 7 ชั้นใช้เวลา 334 วิ | ช่วงจอดนิ่ง 320 วิ ถูกนับเป็นเวลาวิ่ง **การหยุดนานต้องตัดจบทริป** |
| ขาสถานะแสดง 100% ทุกช่อง | ตอนยุบชั้นใช้ `|=` union เซ็ตขา → ขาที่ปิดแวบเดียวถูกนับว่าปิดตลอด **ต้องถ่วงน้ำหนักตามเวลาจริงของแต่ละ sample** |
| จุดแวะรับถูกตีเป็นการกลับทิศ | มองข้ามช่วงจอดไปหาทิศก่อนหน้า **ต้องดูทิศ *ที่เข้า* และ *ที่ออก* จากชั้นนั้นโดยตรง** |

### 6.8 ตั้งชื่อไฟล์ตามลิฟต์ ไม่ใช่ตามพอร์ต (และใช้เลขจริง)

Windows สลับเลข COM ไปมาเมื่อสลับบอร์ด ถ้าตั้งชื่อล็อกตามพอร์ต ลิฟต์ C ที่บังเอิญได้ COM5
จะเขียนต่อท้ายข้อมูลลิฟต์ B ทันทีโดยไม่มีอะไรเตือน `log_lift.py` จึงประทับ `# lift: X`
ไว้ในไฟล์และ **ปฏิเสธที่จะเริ่ม** ถ้าชื่อไม่ตรง

ภายหลังเปลี่ยนมาใช้ **เลขลิฟต์จริง** เป็นชื่อไฟล์ (`capture_lift_3.log` ไม่ใช่ `capture_lift.log`)
เพราะ label A–E เป็นแค่ลำดับที่บังเอิญต่อบอร์ด ไม่มีความหมายกับทีมดูแลลิฟต์

### 6.9 นับ "เคยเป็น LOW" ไม่พอ ต้องดูว่าค้างนานแค่ไหน

Lift 2 อ่านได้ว่า RUNNING/SAFETY/UP/DN เป็น LOW หลักหมื่นครั้ง ดูเหมือนต่อครบ
แต่**พัลส์ยาวสุดแค่ 5–18 ms** ที่ ~55–62 Hz ซึ่งหน้าสัมผัสเชิงกลทำไม่ได้
(รีเลย์ใช้เวลาปิดตัวเอง 5–10 ms) — เป็นไฟ AC เหนี่ยวนำเข้าสายปลายเปิดล้วน ๆ
เทียบกับ Lift 3 ที่ขาเดียวกัน median 20–918 วินาที

`lift_health.py` จึงจำแนกเป็น `NOISE` เมื่อ **p99 ของเวลาปิด < 50 ms**

**ห้ามใช้ค่าสูงสุด (max) ตัดสิน** — รอบถัดมา D17 ได้ค่าโดดเดี่ยว 304 ms หนึ่งครั้ง
จาก 79,664 ครั้ง (0.001%) ขณะที่ median ยังเป็น 2 ms และ p99 = 4 ms
ค่าเดียวนั้นพลิกคำตัดสินจาก NOISE เป็น ok ทันที ต้องใช้เปอร์เซ็นไทล์สูงที่ทนค่าผิดปกติ

### 6.10 สายที่ปิดค้างตลอด ≠ สายที่ไม่มีสัญญาณ

`pulse_widths()` นับเฉพาะการปิดที่**จบแล้ว** (ต้องมีขอบขาเปิดปิดท้าย)
D17 (SAFETY) ของ Lift 2 ปิดค้างตลอดการเก็บ ไม่เคยเปิดเลย จึงได้ 0 พัลส์
แล้วถูกตีเป็น `MISSING` — **ทั้งที่นั่นคือพฤติกรรมที่ถูกต้องที่สุดของ normally-closed**
จับได้เพราะรายงานขัดกันเอง: บอก MISSING แต่ตารางสหสัมพันธ์บอก 100% ทั้งสามคอลัมน์
แก้โดยเช็คสัดส่วนตัวอย่างที่เป็น LOW ถ้าเกิน 90% ให้จัดเป็น `HELD` ไม่ใช่ MISSING

### 6.11 ตัวกรองต้อง debounce ทีละขา ไม่ใช่ทั้งสถานะ

ตัวกรอง 250 ms แบบดูทั้งสถานะพร้อมกันพังทันทีที่มีขาใดขาหนึ่งกระพริบ:
D17 ของ Lift 2 สั่น 55 Hz ทำให้ทุกสถานะอายุสั้นกว่าเกณฑ์ **132,696 ตัวอย่างเหลือ 6 สถานะ**
ทั้งที่อีก 9 เส้นสะอาดดี → `debounce_pins()` ใน `lift_decode.py` ตัดเสียงทีละขา
แล้วค่อยยุบสถานะซ้ำ ต้องทำสองชั้น มิฉะนั้นการเปลี่ยนหลายบิตพร้อมกันจะแตกเป็นค่ากลางทาง

**และต้องตัดล็อกตามจุดที่เวลาบอร์ดถอยหลัง** เพราะล็อกสะสมหลาย session
บอร์ดรีเซ็ตเวลาเป็น 0 ทุกครั้ง ถ้าไม่ตัด ช่วงเวลาจะติดลบและสร้างรหัสผี (เคยได้ code 63)

### 6.12 พอร์ตผิดต้องล้มทันที ไม่ใช่วนต่อใหม่

ลูป reconnect มีไว้รับมือ USB หลุดกลางทาง แต่ถ้าพิมพ์พอร์ตผิดแล้วปล่อยให้ตกเข้าลูปนี้
โปรเซสจะดูเหมือนทำงานอยู่เป็นชั่วโมงโดยไม่เก็บอะไรเลย — เคยเจอตอนทดสอบ COM99
**แก้แล้ว**: เช็ครายชื่อพอร์ตก่อนเริ่ม และถ้ายังไม่เคย arm สำเร็จเลยจะเลิกหลังลอง 3 ครั้ง
(หลัง arm สำเร็จแล้วค่อยวนต่อไม่จำกัด ซึ่งเป็นพฤติกรรมที่ต้องการจริง)

### 6.13 เรื่องปลีกย่อยที่เสียเวลา

- `Start-Process -ArgumentList` กับพาธที่มีช่องว่าง ต้องใส่ `"` ครอบเอง ไม่งั้น argument ถูกตัด
- regex ใน `scan_pins.py` ต้องจับเฉพาะบรรทัด `WIRED` — หลังเพิ่มการกรอง AC มีบรรทัด `noise` ที่มีคำว่า `LOW` เหมือนกัน
- ค่ากลางทางตอนรีเลย์สลับไม่พร้อมกัน โผล่เป็นค่าแปลก ๆ ค้างแค่ 1–3 ms → ตัวกรอง 250ms คัดออก
  แต่ `audit.py` อ่านข้อมูลดิบไม่กรอง จึงเคยรายงาน "ชั้น 26" ที่ค้างแค่ 2ms เป็นชั้นจริง

### 6.14 บัสที่ไม่มีใครขับ กับ frame ที่อ่านผิด หน้าตาเหมือนกัน — แยกด้วย *อัตรา* ไบต์

Lift 1 ผ่าน RS485 ส่งไบต์เข้ามาเป็นช่วง ๆ 161 ไบต์ใน 20 วิ ปนกันทั้งอ่านออกและอ่านไม่ออก
มีลวดลายซ้ำ ๆ (`ef ed ef ed`, `af af af`) ซึ่งดู "เหมือนข้อมูลจริงที่ frame เพี้ยน" มาก
จนเกือบไล่แก้ baud กับสลับ A/B ซึ่งเป็นการไล่ผิดทางทั้งคู่

**การทดสอบที่ตัดสินได้ คือกวาด baud แล้วดูว่าอัตราไบต์วิ่งตามอะไร**

| สิ่งที่เห็น | ความหมาย |
|---|---|
| อัตราไบต์**คงที่**เมื่อเปลี่ยน baud | มีคนส่งจริง เขาเป็นคนกำหนดว่าไบต์เกิดเมื่อไหร่ → เป็นปัญหา framing |
| อัตราไบต์**เพิ่มตาม** baud | ไม่มีใครส่ง UART แค่กำลังแปลงสัญญาณรบกวนบนสายที่ลอยอยู่ |

วัดจริงได้ **17 เท่า** เมื่อ baud ต่างกัน 24 เท่า (9600→230400) ⇒ ไม่มีใครขับบัส
สัญญาณรบกวนไม่มี "อัตราข้อความ" ของตัวเอง สุ่มอ่านเร็วขึ้นก็ได้ไบต์มากขึ้นตรง ๆ

**และการสลับ A/B ตัดทิ้งได้ฟรีจากอัตราเดียวกันนี้** — ถ้าสลับจริง สายจะนิ่งอยู่ที่ break ตลอด
ตัวรับจะพ่น `0x00` เต็มความเร็วสาย ≈ 11.5 kB/s ที่ 115200 แต่เราได้ 8 B/s และ `0x00` **ศูนย์ตัว**

เข้ากับที่เราเขียนเตือนตัวเองไว้แล้วใน `RS485_PLAN.md` §3.2 (บัสไม่มี bias จะลอยแล้วตัวรับกระดิกตามนอยส์)
และเป็นเรื่องเดียวกับ §6.5 ข้างบน คือ **สายปลายเปิดรับไฟเหนี่ยวนำ แล้วดูเหมือนมีสัญญาณ**
ทำเป็นเครื่องมือถาวรไว้ที่ `rs485_check.py` แล้ว

> ⚠️ ตอนไล่หาสาเหตุ **ห้ามส่งอะไรออกสาย** บอร์ดต่อแบบส่งทางเดียว (DE ผูก 5V ค้าง)
> ถ้าฝั่ง PC ขับสายด้วยจะชนกันโดยไม่มีการตัดสิน `rs485_check.py` จึงบังคับ `rts=False, dtr=False`

### 6.15 🔑 บั๊กเกือบทุกตัวในโปรเจกต์นี้ประกาศตัวเองด้วย "รายงานที่ขัดกันเอง"

ถ้าจะจำข้อเดียวจากหัวข้อ 6 ทั้งหมด ให้จำข้อนี้ รูปแบบซ้ำมาแล้ว **5 ครั้ง**:

| ครั้ง | ตัวเลขที่เป็นไปไม่ได้ / ขัดกันเอง | ความจริงที่ซ่อนอยู่ |
|---|---|---|
| §6.10 | บอก `MISSING` แต่ตารางสหสัมพันธ์บอก 100% ทั้งสามคอลัมน์ | สายปิดค้างตลอด = normally-closed ที่ปกติดี |
| `lift_health` §3 | RUNNING −75%, UP −75% แต่ verdict ข้างล่างบอก HEALTHY | ลบเวลาข้ามรอยที่นาฬิกาบอร์ดรีเซ็ต → ติดลบ |
| `FIRMWARE_UPGRADE.md` | บอกว่า `rejected` จะพุ่งตอนอัปโหลด | เป็นไปไม่ได้ บรรทัดที่ไม่ขึ้นต้น `ST ` ถูกทิ้งก่อนถึงตัวนับ |
| `rs485_check` v1 | ลิงก์เดียวไม่แตะอะไรเลย ได้ 4 verdict ต่างกัน | เอาเลขที่ขึ้นกับดวงสองตัวมาหารกัน |
| `rs485_check` v2 | `UP -- the link is delivering` คู่กับ `malformed 0` ขณะที่ 200/201 บรรทัดเป็นขยะ | ตัวนับเห็นเฉพาะบรรทัดที่ parse ผ่านแล้ว |

> ⚠️ แถว `lift_health` มี **สอง** สาเหตุที่ต่างกัน อย่าแก้แค่ข้อเดียว
> 1. **บอร์ดรีเซ็ต** — นาฬิกาถอยหลัง ลบข้ามรอยต่อได้เวลาติดลบ (วัดเมื่อ 30 ส.ค.: −7,561 วิ ต่อเวลาจริง 5,515 วิ = 137%) ⇒ **นี่คือต้นเหตุของเปอร์เซ็นต์ติดลบ**
> 2. **ช่วงที่ไม่มีใครบันทึก** — บอร์ดยังเดินอยู่ นาฬิกา**ไม่**ถอยหลัง จึงไม่มีอะไรในข้อมูลบอกเลย — สถานะสุดท้ายก่อนช่องว่างถูกนับว่าค้างทั้งช่วง (เคยได้ 3,156 วิ = ลิฟต์วิ่งลง 52 นาที)
> ข้อ 2 เห็นได้**หลังแก้ข้อ 1 เสร็จแล้วเท่านั้น** ต้องแก้ทั้งคู่

**ครึ่งที่ฟังดูสบายใจ คือครึ่งที่คนเอาไปใช้ตัดสินใจเสมอ** — และเอกสารก็มักชี้ให้ดูครึ่งนั้นพอดี
(`GATEWAY.md` กับ `FIRMWARE_UPGRADE.md` บอกให้ดูบรรทัด verdict ซึ่งเป็นบรรทัดที่โกหก)

⇒ เวลาอ่านผลของเครื่องมือตัวเอง **ให้ไล่หาจุดที่รายงานเถียงกันเองก่อนเชื่อข้อสรุป**
และเวลาเขียนเครื่องมือ ให้เอาตัวเลขที่ขัดแย้งกันมาไว้ในบล็อกเดียวกัน จะได้ขัดกันต่อหน้าคนอ่าน

### 6.17 🔴 การเปิดพอร์ต USB รีเซ็ตบอร์ด — ค่า uptime หลังต่อจึงเป็นผลจากการวัดเอง

Lift 1 ที่หน้าตู้ 6 ก.ย. 2026 วัดได้ตรง ๆ:

```
open #1: board says   0.1s uptime   ← เปิดพอร์ตแล้วบอร์ดรีเซ็ตทันที
open #2: board says   8.2s uptime   ← เปิดซ้ำไม่รีเซ็ต นาฬิกาเดินต่อ
   board time +8.1s   vs   PC clock +8.2s
```

**รีเซ็ตเฉพาะการเปิดครั้งแรกหลังเสียบสาย USB** การเปิดซ้ำถัดมาไม่รีเซ็ต
และเกิดขึ้น**ทั้งที่ตั้ง `dtr=False, rts=False` ไว้แล้ว** ซึ่งเป็นการตั้งค่าที่ควรจะกันไว้

ผลคือเคยสรุปผิดสองรอบว่า *"บอร์ดเพิ่งได้ไฟจาก USB"* เพราะ uptime อ่านได้ไม่กี่วินาที
— ทั้งที่นั่นคือเวลานับจาก**การเชื่อมต่อของตัวเอง** ไม่ใช่ประวัติของบอร์ด
เป็น §6.16 เวอร์ชันที่ร้ายกว่า เพราะเครื่องมือไม่ได้พัง แต่**การวัดไปเปลี่ยนสิ่งที่ถูกวัด**

> ⛔ **USB ตอบไม่ได้ว่าบอร์ดรอดจากการถอดสาย USB หรือไม่**
> เสียบกลับแล้วเปิดพอร์ต = รีเซ็ต ⇒ "ไฟดับไป" กับ "เพิ่งโดนรีเซ็ต" อ่านได้ `uptime ≈ 0` เหมือนกันเป๊ะ
> ต้องตอบด้วย **RS485 ฝั่ง Gateway** หรือ **มัลติมิเตอร์ที่ขา 5V** เท่านั้น

**สิ่งที่ USB ตอบได้จริง** คือบอร์ดวิ่งอยู่ไหมและวิ่งต่อเนื่องไหม — ใช้ `board_alive.py`
ซึ่งเปิดพอร์ต**ครั้งเดียวแล้วถือค้างไว้** ไม่เปิดซ้ำ เพราะทุกการเปิดใหม่จะลบประวัติที่กำลังวัด

### 6.18 🔴 ล็อกไม่มีวันที่ — ห้ามกรองช่วงเวลาด้วยการเทียบสตริง

บรรทัดข้อมูลมีแต่ `HH:MM:SS.mmm` ไม่มีวันที่ และไฟล์เดียวสะสมหลายวัน
⇒ การกรองแบบ `if "15:44" <= clock[:5] <= "15:50"` **ดึงช่วงเวลานั้นของทุกวันมาปนกัน**

เจอจริงตอนตรวจผลซ่อม VS2 — วิเคราะห์ครั้งแรกสรุปว่า *"สายทำงานแล้วหยุดอีก"*
ซึ่งผิดสนิท เพราะข้อมูลเย็นของหกวันถูกผสมเข้ามา พอแก้เป็นยึดตำแหน่งในไฟล์
ภาพก็ชัดทันทีว่าซ่อมสำเร็จ

**วิธีที่ถูก: ยึดจุดที่ `board_ms` ถอยกลับ** (บอร์ดรีบูต) หรือยึดตำแหน่งบรรทัด
ไม่ใช่เวลานาฬิกา:

```python
cut = max(i for i in range(1,len(rows)) if rows[i][1] < rows[i-1][1])
rows = rows[cut:]          # session ล่าสุดเท่านั้น
```

เป็นตัวอย่างของ §6.16 อีกครั้ง — เครื่องมือวัดพัง ไม่ใช่ระบบพัง

### 6.16 ตรวจเครื่องมือวัดก่อนเชื่อค่าที่วัดได้

เจอสองครั้งในวันเดียว ทั้งคู่ "ดูเหมือนระบบพัง" แต่ที่จริงเครื่องมือวัดพัง:

- regex ของ **สคริปต์ทดสอบ** ใช้ `[^-]*?` เลยกลืนวันที่ `2026-08-31` ที่มีขีด → ดึงบรรทัด `FW` ไม่ได้เลย
  → เกือบสรุปว่า `rs485_check` ไม่ยอมปฏิเสธบอร์ดผิด ทั้งที่มันถูกอยู่แล้ว
- อีก session ทดสอบกฎ restart ด้วย `board_ms` ราว 1000 ซึ่งคือบอร์ดที่เพิ่งบูต
  ไม่ใช่สถานการณ์ที่ reader ค้างจริง → **กฎทำงาน แต่ทำงานด้วยเหตุผลผิด**

แบบหลังอันตรายกว่า เพราะให้คำตอบถูกจึงไม่มีอะไรสะดุด ต้องทดสอบซ้ำที่ค่าใกล้ของจริงถึงจะเห็น

---

## 7. ปัญหาฮาร์ดแวร์ที่เคยเจอ

### 7.1 CH340 เปิดพอร์ตไม่ได้หลังเสียบครั้งแรก

อาการ: `SetCommState` fail — `ERROR_GEN_FAILURE (31)`
`CreateFile` และ `GetCommState` ผ่าน แต่ `SetCommState` ล้ม = ส่ง USB control transfer ไปตั้งค่า UART ไม่ได้
สาเหตุ: `FirstInstallDate` = `LastArrivalDate` = วินาทีเดียวกัน → ไดรเวอร์ถูกติดตั้งขณะอุปกรณ์ arrive แล้ว bind ไม่สมบูรณ์
**แก้: ถอดสาย USB แล้วเสียบใหม่** (ไม่มีสิทธิ์ admin จึงใช้ `Restart-PnpDevice` ไม่ได้)

### 7.2 บอร์ด Arduino ตัวแรกเสีย

enumerate ผ่าน USB แต่ MCU ไม่ตอบอะไรเลย — ทดสอบ bootloader ทั้ง STK500v1/v2 หลาย baud,
กดรีเซ็ตมือ, loopback D0–D1 → เงียบทุกทาง **เปลี่ยนบอร์ดใหม่แล้วใช้ได้ทันที**

เทคนิคที่พิสูจน์ว่า CH340 ส่งข้อมูลได้จริงโดยไม่ต้องพึ่งใคร: จับเวลา `flush()`
เทียบกับ `bytes × 10 / baud` — ถ้าอัตราส่วนคงที่ทุก baud แปลว่าชิปยิงบิตออกสายจริง
(วัดได้ 0.87–0.89 ทุกระดับ, เหวี่ยงแค่ 1.01 เท่า)

### 7.3 ✅ Lift 1 — สาย VS2 ขาด **(ซ่อมแล้ว 12 ก.ย. 2026)**

อาการเดิม: D24 ไม่เคย LOW เลย → อ่านตำแหน่งได้หยาบ 2 ชั้นต่อรหัส รหัสเป็นเลขคู่ล้วน
**กินเวลารวม 35 วัน** นับจากการทดสอบครั้งแรก 8 ส.ค. จนซ่อมเสร็จ

วิธีชี้จุดที่ใช้ได้ (เก็บไว้ใช้กับลิฟต์ตัวอื่น): ขณะลิฟต์วิ่ง ดู LED ช่อง VS2
บนโมดูลรีเลย์ — ต้องกะพริบถี่ที่สุดใน 6 ช่อง
- LED กะพริบ แต่ขาเงียบ → สายรีเลย์ → ขั้ว 24 ของ DNMEGA1 หลวม
- LED ไม่กะพริบ → สายตู้ควบคุม (MC-10/ALR0) → ขั้ว IN ของโมดูลขาด

**บทเรียนจากการวินิจฉัยรอบนี้** — อาการของสายเส้นเดียวเปลี่ยนไปสามแบบ และสองแบบหลัง
หลอกให้คิดว่าซ่อมสำเร็จ:

| อาการที่เห็น | ความจริง |
|---|---|
| ไม่เคย LOW เลย | สายเปิด ไม่มีอะไรมาถึง |
| ปิด 239 ครั้ง แต่ p99 = 6 ms | **ไฟบ้านเหนี่ยวนำบนสายปลายเปิด** ไม่ใช่สัญญาณ (§6.9) |
| ปิดค้าง 110 วินาที ขณะรถกำลังวิ่ง | **สายทดสอบของช่างคีบค้างไว้** — หน้าสัมผัสจริงทำแบบนี้ไม่ได้ |
| ปิดค้างเป็นนาทีขณะรถจอด | ยังบอกไม่ได้ — สายที่ค้างปิดกับสายที่ดี **หน้าตาเหมือนกันตอนรถจอด** |

⇒ **ตัดสินได้ก็ต่อเมื่อรถวิ่งเท่านั้น** ใช้ `python pin_watch.py 1 D24` ซึ่งปฏิเสธที่จะ
ตัดสินจนกว่ารถจะขยับจริง

---

## 8. ผลการทดสอบ 8 ส.ค. 2026

**Blind test 14/14** — ผู้ใช้จดเวลา/ชั้นเอง ระบบถอดรหัสโดยไม่รู้เฉลย แล้วนำมาเทียบ ตรงทุกข้อ
รวมถึงการจอดแวะสั้นเพียง 8.3 วินาที และลำดับสามจังหวะ 9→20→33

ข้อมูลที่เก็บได้: **Lift 3 (A)** 2,760 การเปลี่ยนสถานะ / 113.6 นาที (`capture_lift_3.log`),
**Lift 1 (B)** 990 / 89.3 นาที (`capture_lift_1.log`), ช่วงแรกก่อนแก้ sketch (`capture_lift_3_part1.log`)

รายงานฉบับเต็ม: `TEST_REPORT_2026-08-08.md`

---

## 8.1 ✅ ซ่อม VS2 ของ Lift 1 สำเร็จ — 12 ก.ย. 2026 15:38

ผู้ใช้ซ่อมสายแล้ววิ่งทดสอบ: ขึ้นถึงชั้น 44 เวลา 15:47 แล้วลงชั้น 1 ทันที ถึง 15:48
ระบบถอดรหัสจากสัญญาณโดยไม่รู้เฉลย ตรงทั้งชั้นและเวลา:

| ที่ผู้ใช้จด | ที่ระบบถอดได้ |
|---|---|
| ถึงชั้น 44 เวลา 15:47 | **รหัส 46 = ชั้น 44** ที่ **15:47:27** |
| ถึงชั้น 1 เวลา 15:48 | **รหัส 2 = ชั้น 1** ที่ **15:48:29** |

**Toggle cascade — ตัวชี้ขาด** (ต้องลดลงครึ่งหนึ่งทุกขั้น):

```
D24 VS2  154      D27 VS5   18   2.11x
D25 VS3   76  2.03x   D28 VS6    8   2.25x
D26 VS4   38  2.00x   D29 VS7    4   2.00x
```

- **single-step 154/154 = 100.0%** (เดิมกระโดดทีละ 2 ทุกครั้ง)
- **รหัสเลขคี่ 78 จาก 155** (เดิม **0** — เป็นเลขคู่ล้วน 100%)
- ช่วงที่อ่านได้ **รหัส 2–46 = ชั้น 1 ถึง 44** เดิมแยกได้แค่ 24 ตำแหน่งหยาบ ๆ

### 🔴 เกณฑ์รับงานเดิมใน §9 ข้อ 3 **ใช้ไม่ได้** — ผ่านทั้งที่สายยังตาย

เกณฑ์เดิมเขียนว่า *"D24 ต้องเริ่มสลับ, **รหัสสูงสุดต้องเป็น 46**"*
**ครึ่งหลังเป็นจริงอยู่แล้วตั้งแต่ตอนสายยังตาย** — วัดได้ว่า Lift 1 มีรหัสสูงสุด 46
เท่ากับอีกสองตัว ทั้งที่ VS2 เปิดสนิท

เหตุผลเป็นเลขคณิตล้วน: **ชั้น 44 = รหัส 46 = ไบนารี `101110` ซึ่งบิต 0 เป็น 0 อยู่แล้ว**
⇒ ปลายบนของช่วงเป็นค่าที่ความเสียนี้แตะไม่ถึง **สายที่ตายทำให้เสียความละเอียด ไม่ได้เสียช่วง**

⇒ ใครใช้เกณฑ์นี้จะ**เซ็นรับงานซ่อมที่ไม่เคยเกิดขึ้น** — เป็นรูปแบบ §6.15 ที่อยู่ใน
ขั้นตอนของเราเอง **เกณฑ์ที่ใช้ได้จริงมีสี่ข้อ และต้องผ่านครบ:**

1. D24 ค้างได้เกิน 50 ms (ไม่ใช่ไฟบ้านเหนี่ยวนำ)
2. **รหัสเลขคี่ต้องโผล่มา** ← ง่ายที่สุดและไม่ต้องใช้เกณฑ์ตัวเลขใด ๆ
3. D24 สลับราว **2 เท่า** ของ D25
4. cascade ลดลงครึ่งหนึ่งทุกขั้น

---

## 9. งานที่เหลือ (สถานะ 12 ก.ย. 2026)

| # | งาน | สถานะ |
|---|---|---|
| 1 | กระตุ้น **FIRE 火灾** | ⏳ **ยังไม่ได้ทำ** — ต้องกระตุ้นจริงเท่านั้น ขาที่เปิดอยู่แยกจากขาที่ไม่ได้ต่อไม่ออก<br>⚠️ **D18 ไม่ใช่ FIRE** — เคยสงสัยเพราะลำดับขั้วทำนายไว้ แต่วัดแล้วปิด 25,000 ครั้ง **ยาวสุด 3 ms** ทุกครั้งกว้าง 1 sample = พื้นสัญญาณรบกวน ไม่ใช่หน้าสัมผัส |
| 2 | กระตุ้น **FIRE RETURN** | ⏳ ยังไม่ได้ทำ เช่นเดียวกัน |
| 3 | ซ่อม **VS2 Lift 1** | ✅ **เสร็จ 12 ก.ย. 2026 15:38** — ดู §8.1 |
| 4 | ปิดตารางชั้นล่าง (จอดชั้น 2,3,4,5,6,8) | ✅ **ปิดแล้วจากข้อมูล ไม่ต้องจอดทดสอบ** — ดู §3.4 |
| 5 | ยืนยันว่าชั้น 7 มีชานจอดหรือไม่ | ✅ **ปิดแล้ว** — รหัส 4, 9, 11 ไม่มีการจอดเลยใน ~7,000 ครั้ง ดู §3.4 |
| 6 | หาความหมายที่ **SAFETY (D17) เปิดสั้น ๆ** | ⏳ **ยังไม่ปิด — เห็นรูปแบบแล้ว แต่ยังไม่ยืนยันกับลิฟต์จริง**<br>สิ่งที่*วัดได้*: ทุกลิฟต์เหมือนกัน ~590 ครั้ง/ตัว/5.8 วัน ยาว **677 ms** สม่ำเสมอ เริ่มที่ **179 หรือ 187 วินาทีหลังรถจอด** และไม่มีสักครั้งที่เกิดขณะรถวิ่ง กระจายเท่ากันทั้ง 24 ชั่วโมง ⇒ มีลักษณะเป็น timer<br>🔴 **แต่ยังไม่เคยทดสอบ SAFETY กับลิฟต์จริง** (ผู้ใช้ยืนยัน 12 ก.ย.) ⇒ **ยังสรุปความหมายไม่ได้** ต้องกระตุ้นของจริงร่วมกับทีมดูแลลิฟต์ เช่น เปิดประตูค้าง กดหยุดฉุกเฉิน แล้วจดเวลา — ดู §9.7 ว่าทำไมข้อมูลที่มีถึงยังตอบไม่ได้ |
| 7 | ต่อลิฟต์ 3–5 | Lift 3 ✅ เสร็จ · **Lift 4, 5 ⏳ ยังไม่เริ่ม** |

### งานที่เหลือจริง ๆ ตอนนี้

1. **Lift 5 (lift service)** — เครื่องหน้าตู้กำลังเขียน firmware และจะทดสอบกับลิฟต์จริง
   ⚠️ ต้องแฟลชด้วย `-DLIFT_ID=5` ไม่งั้นบอร์ดจะประกาศ `LIFT=0` ผูกพอร์ตอัตโนมัติไม่ได้
   ⚠️ **เลข 5 ยังไม่เคยยืนยันกับอาคาร** — อนุมานจากการตัดตัวเลือก ควรถามให้แน่ก่อนแฟลช
2. **Lift 4 — ⛔ ปิดใช้งานอยู่ ข้ามไปก่อน** (12 ก.ย. 2026)
2. **FIRE / FIRE RETURN** — ต้องทำร่วมกับทีมดูแลลิฟต์
3. **ทดสอบ SAFETY กับลิฟต์จริง** — ยังไม่เคยทำ ดู §9.7

## 9.5 การแบ่งบทบาทระหว่างสองเครื่อง (ตกลงกัน 30 ส.ค. 2026)

| เครื่อง | บทบาท | เชื่อมต่ออย่างไร |
|---|---|---|
| **Gateway** | รับข้อมูลลิฟต์ทั้ง 5 ตัวต่อเนื่อง | RS485 → ตัวแปลง USB หลายตัวบน hub, `log_lift.py <COM> <lift> --listen` |
| **เครื่องนี้ (หน้าตู้)** | วางแผน, debug, **อัปเกรด firmware** | USB ตรงเข้า Arduino |

ลิฟต์ทั้ง 5 ตัวจะย้ายไปอยู่กับ Gateway ทั้งหมด เครื่องนี้ต่อ USB เฉพาะตอนที่ต้อง
ตรวจปัญหาหน้าตู้หรือแฟลช firmware ใหม่

**เพราะ RS485 เป็นแบบส่งทางเดียว Gateway จึงอัปเกรด firmware ไม่ได้** ต้องทำที่เครื่องนี้เท่านั้น
ขั้นตอนอยู่ใน `FIRMWARE_UPGRADE.md` — และเพราะ `RO` ไม่ได้ต่อ **จึงแฟลชผ่าน USB ได้
โดยไม่ต้องถอดสาย RS485 และ Gateway ไม่ต้องหยุดเก็บข้อมูล**

บอร์ดประกาศเวอร์ชันตัวเองตอนบูตเป็นบรรทัด `FW IODebug <version> <date>` และ
`log_lift.py` บันทึกไว้ในล็อก ⇒ ตอบได้เสมอว่าลิฟต์ตัวไหนรันเวอร์ชันอะไร
โดยไม่ต้องเปิดตู้ **ต้องบวกเลข `FW_VERSION` ทุกครั้งที่แก้โค้ด**

## 9.7 🔴 ข้อมูล SAFETY ที่มีตอนนี้ **ตีความไม่ได้** — แก้ไขเมื่อ 12 ก.ย. 2026

**ผู้ใช้ยืนยันว่ายังไม่เคยทดสอบสาย SAFETY กับลิฟต์จริงเลย ข้อมูลที่เห็นเกิดจากการทดสอบกับบอร์ด**

สิ่งที่เคยสรุปไว้แล้ว**ถอนออก**:

| เคยเขียนว่า | ความจริง |
|---|---|
| "7 ก.ย. Lift 1 หยุดให้บริการ 41 นาที ที่ชั้น 4 — ควรถามผู้ดูแลว่ามีบันทึกงานไหม" | **ถอน** — ช่วงนั้นมีคนทำงานกับบอร์ด/สายของ Lift 1 อยู่ (เป็นช่วงเดียวกับที่พยายามซ่อม VS2) สาย SAFETY ที่อ่านได้ว่าเปิดจึงเป็น**ผลของการถอดสายทดสอบ ไม่ใช่วงจรนิรภัยของลิฟต์เปิดจริง** |
| "9 ก.ย. เห็นร่องรอยช่างเข้าบำรุงรักษาทั้งสามตัว" | **ถอน** ด้วยเหตุผลเดียวกัน — เป็นการทำงานกับบอร์ด |
| "§9 ข้อ 6 ปิดแล้ว — เป็น timer 3 นาที" | **เปิดกลับ** — รูปแบบ 677 ms ที่ 179/187 วินาทียัง*วัดได้*จริงและสม่ำเสมอ แต่**ยังไม่มีอะไรผูกมันกับพฤติกรรมของลิฟต์จริง** |

### บทเรียน: ข้อมูลบอกว่า "เกิดอะไรขึ้นบนสาย" ไม่ได้บอกว่า "ลิฟต์ทำอะไร"

ผมเห็นสาย SAFETY เปิด 41 นาทีแล้วสรุปว่า**ลิฟต์**หยุดให้บริการ ทั้งที่ข้อมูลบอกได้แค่ว่า
**ขา D17 อ่านได้ HIGH** — ซึ่งเกิดได้ทั้งจากวงจรนิรภัยเปิดจริง **และ**จากการที่สายถูกถอด
ออกจากบอร์ด ทั้งสองกรณีให้ข้อมูลเหมือนกันเป๊ะ

นี่คือ §6.5 และ §9 ที่เขียนไว้แล้วว่า *"หน้าสัมผัสที่เปิดอยู่อ่านได้ HIGH ซึ่งแยกจากขาที่
ไม่ได้ต่อไม่ออก"* — ผมมีกฎนี้อยู่ในมือแล้วยังพลาด เพราะข้อมูล 5.8 วันดูน่าเชื่อถือ
จนลืมถามว่า**มีใครไปยุ่งกับฮาร์ดแวร์ในช่วงนั้นหรือเปล่า**

⇒ **กฎใหม่: ก่อนตีความเหตุการณ์ใด ๆ จากข้อมูล ให้ถามก่อนว่าช่วงนั้นมีใครทำงานกับบอร์ดอยู่ไหม**
บันทึกช่วงเวลาที่มีการทำงานกับฮาร์ดแวร์ลงใน `LIFT_STATUS.md` ทุกครั้ง

---

> 📨 **ส่งงานให้เครื่องหน้าตู้เรื่อง Lift 5 อยู่ที่ `HANDOVER_LIFT5.md`**

## 9.6 📍 สถานะระบบปัจจุบัน — อ่านตรงนี้ก่อน (12 ก.ย. 2026)

**ลิฟต์ทั้งสามตัวอ่านสัญญาณได้ครบสมบูรณ์เป็นครั้งแรกของโปรเจกต์**

| ลิฟต์ | พอร์ต | firmware | สายครบ | decode |
|---|---|---|---|---|
| Lift 1 | COM20 | 1.2.2 `LIFT=1` | ✅ 10/10 (ซ่อม VS2 แล้ว) | รหัส 2–46 |
| Lift 2 | COM22 | 1.2.2 `LIFT=2` | ✅ 10/10 | รหัส 1–46 · single-step 100% |
| Lift 3 | COM21 | 1.2.2 `LIFT=3` | ✅ 10/10 · **ตัวอ้างอิง** | รหัส 1–46 · single-step 100% |
| **Lift 4** | — | — | ⛔ **ปิดใช้งาน ข้ามไปก่อน** (12 ก.ย.) | — |
| **Lift 5** | ยังอยู่เครื่องหน้าตู้ (RS485, COM14) | **1.2.2 `LIFT=5`** ✅ | ✅ **blind test ผ่าน 12 ก.ย. 18:30** — *lift service* | กำลังเก็บ |

**การเก็บข้อมูล** — `log_lift.py <COM> <lift> --listen` หนึ่งโปรเซสต่อลิฟต์ ทำงานต่อเนื่อง
มาแล้ว **5.8 วันโดยไม่ขาดตอน · rejected 0 · link lost 0 · บอร์ดไม่รีสตาร์ตเลย**
รวม 324,000 บรรทัด หยุดทุกตัวพร้อมกันด้วยการสร้างไฟล์ `STOP_CAPTURE`

### Lift 5 — แฟลชแล้ว 12 ก.ย. 2026 (เครื่องหน้าตู้)

**✅ เลข 5 ยืนยันแล้ว ไม่ใช่การอนุมานอีกต่อไป** — เดิมมาจากการตัดตัวเลือก (1–4 ถูกระบุแล้ว)
และค้างเป็นข้อไม่ยืนยันมาตลอด **12 ก.ย. ผู้ใช้ระบุเองโดยตรง**ว่าบอร์ดที่ต่อ USB คือ Lift 5

บอร์ดนี้เดิมรัน**โปรแกรมของทีมอื่น ตัวเดียวกับที่เคยอยู่บน Lift 2** (`Binary: … / COIL1 / COIL2`)
อ่าน flash เดิมเก็บไว้ก่อนเขียนทับแล้วที่ `firmware_backup/lift5_original_*` — ย้อนกลับได้

**บันทึกเวลาที่ยุ่งกับฮาร์ดแวร์** (ตามกฎ §9.5 — ถ้าไม่จด คนอ่านข้อมูลทีหลังจะตีความผิด):

| เวลา | ทำอะไร |
|---|---|
| 16:37 | เสียบ USB เข้าบอร์ด Lift 5 — บอร์ดรีเซ็ตตาม §6.17 |
| 16:38–16:39 | อ่าน flash เดิมออกเก็บไว้ (บอร์ดรีเซ็ตอีกครั้งจาก bootloader) |
| 16:41 | แฟลช IODebug 1.2.2 `LIFT=5` |
| 16:44 | เริ่ม `log_lift.py COM3 5 --listen` |

⇒ **ข้อมูลก่อน 16:44 ใน `capture_lift_5.log` ไม่มี** และสัญญาณใด ๆ ที่ดูผิดปกติในช่วง 16:37–16:44
เป็นผลจากการทำงานกับบอร์ด ไม่ใช่จากลิฟต์


#### ✅ Blind test Lift 5 — 12 ก.ย. 2026 18:30 น. **ผ่านครบทุกข้อ**

ผู้ใช้ขึ้นลิฟต์เองแล้วจดเวลามาให้ทีหลัง ระบบถอดรหัสจากข้อมูลที่บันทึกไว้แล้ว ไม่มีใครแตะตู้
⇒ **หน้าต่างสะอาด** ตามกฎ §9.7 (session ที่เริ่ม 18:29:20 — anchor ที่รอยรีเซ็ตนาฬิกาบอร์ด ไม่ใช่กรองด้วยสตริงเวลา §6.18)

| ผู้ใช้รายงาน | ข้อมูลบันทึกได้ | ตรง |
|---|---|---|
| อยู่ชั้น 1 เวลา 18:30 | รหัส 2 (ชั้น 1) จอดนิ่งตั้งแต่ต้น session ออกตัว 18:30:40 | ✅ |
| ถึงชั้น 44 เวลา 18:31 | ขึ้น 69.5 วิ · UP 95.6% · แตะรหัส 46 (ชั้น 44) 18:31:50 · จอด (RUNNING เปิด) 18:31:55 ที่รหัส 47 | ✅ |
| รีบลงชั้น 1 ถึง 18:33 | ออกตัว 18:32:11 · DN **100.0%** · ลง 69.5 วิ · ถึงรหัส 2 (ชั้น 1) 18:33:18 จอด 18:33:21 | ✅ |
| เคลื่อนต่อไป B1 | ออกตัว 18:33:37 · ถึงรหัส 1 (B1) 18:33:41 · จอด 18:33:44 | ✅ |

คุณภาพในเที่ยวนี้: **single-step 92/92 = 100.0%** (ยังเป็น 100% ที่ debounce 1000 ms ด้วย),
toggle cascade D24..D29 = 92/46/22/10/4/2 (LSB:บิตถัดไป = 2.00 เป๊ะ), **ไม่มีรหัส 0**,
SAFETY (D17) ปิดค้าง 106/107 states ตามพฤติกรรม normally-closed
⇒ **wiring ของ Lift 5 ครบทั้ง 6 บิต ไม่มีเส้นขาด** และช่วงเดินทางครบตั้งแต่ B1 ถึงปลายบน

### 📡 เก็บข้อมูลต่อเนื่อง เพื่อเตรียมทำ backend + API (12 ก.ย. 2026)

ผู้ใช้ออกจากหน้างานแล้ว **ให้ `log_lift.py` ของลิฟต์ 1, 2, 3, 5 ทำงานต่อไป** ห้ามหยุด
ปลายทางคือระบบ backend + API ที่ดึงสถานะการเคลื่อนที่ของลิฟต์ไปแสดงบนหน้าจอ
งานทดสอบชั้นของ Lift 5 ที่ค้างอยู่ และแผนการทดสอบ อยู่ใน `LIFT_STATUS.md` หัวข้อ “งานค้าง”

ตรวจว่ายังเก็บอยู่ด้วย `python capture_status.py` — ตอบทีเดียวทั้งสี่ตัว **ไม่แตะพอร์ต**
(ตัวบันทึกถือพอร์ตแบบ exclusive อยู่ §6.12 เครื่องมือตรวจจึงต้องอ่านจากไฟล์กับตาราง process เท่านั้น)

#### สามวิธีที่ capture หายไป และตัวที่รับมือแต่ละแบบ

| capture หายเพราะ | อะไรกู้คืนให้ |
|---|---|
| มีคนถอด USB แล้วเสียบช่องอื่น เลข COM เปลี่ยน | **`--follow`** ในตัวบันทึกเอง — ค้นจาก beacon `LIFT=` แล้วผูกพอร์ตใหม่ |
| โปรเซสตาย (crash, ถูกฆ่า, session ที่สั่งปิดไป) | **`scripts/start_captures.ps1`** ที่ Scheduled Task เรียกทุก 15 นาที |
| เครื่องรีบูต | **trigger `AtLogOn`** ของ Scheduled Task เดียวกัน |

⚠️ **สามข้อนี้แก้คนละเรื่องกัน อย่าคิดว่าข้อเดียวครอบคลุมหมด** — `--follow` ไม่ช่วยถ้าโปรเซสตาย
และ watchdog ไม่ช่วยถ้าโปรเซสยังอยู่แต่เกาะพอร์ตผิด

🔴 **ข้อจำกัดของ Scheduled Task แบบ user-level**: `AtLogOn` ทำงานตอนมีคน logon
ถ้าเครื่องรีบูตแล้วไม่มีใคร logon จะไม่มีอะไรสตาร์ตเลย การรันโดยไม่ต้อง logon ต้องเก็บ
credential หรือใช้ S4U ซึ่งต้องสิทธิ์ admin — ถ้า Gateway ต้องรันแบบ headless ค่อยขอสิทธิ์นั้น

#### 🔴 ห้าข้อที่คนเขียน backend ต้องรู้ ไม่งั้นอ่านข้อมูลผิดแบบเงียบ ๆ

1. **`capture_lift_1.log` เปลี่ยนแผนที่บิตกลางไฟล์** — ก่อน 15:38:58 ของ 12 ก.ย. ใช้
   `POSITION_BITS_B` หลังจากนั้นใช้ `POSITION_BITS` ถ้าถอดด้วยแผนที่เดียวทั้งไฟล์
   ครึ่งหนึ่งของประวัติจะออกมาเป็นชั้นที่ผิดไปเท่าตัว ดู §3.2 — และ **จุดตัดคือจุดรีเซ็ตสุดท้าย
   ไม่ใช่จุดแรก** ในไฟล์มีจุดรีเซ็ตสามจุดที่หน้าตาเหมือนกันหมด

2. **ช่วงที่ข้อมูลหายไปไม่มีเครื่องหมายอะไรทิ้งไว้เลย** — วัดได้จริงใน `capture_lift_1.log`:
   **เงียบสนิท 81.8 นาที (14:17:10 → 15:38:57) ไม่มีบรรทัดสักบรรทัด** ไม่มี `link lost`
   ไม่มี beacon เพราะ RS485 เป็นทางเดียว บอร์ดที่ไม่มีไฟก็แค่เงียบ ฝั่ง PC ไม่มีอะไรให้สังเกต
   ⇒ **API ต้องตรวจช่องว่างด้วยเวลาเอง** (ไม่มีบรรทัดใดเกิน ~90 วินาที = ขาดตอน)
   และต้องไม่ถือว่า “สถานะล่าสุดก่อนช่องว่าง” ค้างอยู่ตลอดช่วงนั้น (§6.15 ข้อ 2)

3. **บรรทัดข้อมูลไม่มีวันที่** มีแต่ `HH:MM:SS.mmm` ⇒ ห้ามกรองช่วงเวลาด้วยการเทียบสตริง (§6.18)
   และ **นาฬิกาบอร์ดเดินช้ากว่านาฬิกา PC 0.78%** ⇒ ใช้เวลา PC เป็นแกนเวลาเสมอ
   ส่วน `board_ms` ใช้ได้อย่างเดียวคือหารอยรีสตาร์ต (และมันวนที่ 49.7 วัน)

4. **ต้อง debounce ทีละขา 250 ms ก่อนถอดรหัสทุกครั้ง** (`debounce_pins()` ใน `lift_decode.py`)
   ข้อมูลดิบมีค่ากลางทางตอนรีเลย์สลับไม่พร้อมกัน ค้างแค่ 1–3 ms ซึ่งถอดออกมาเป็นชั้นที่ไม่มีจริง
   (เคยได้ “ชั้น 26” ที่ค้าง 2 ms และรหัส 63) §6.11 · §6.13

5. **หนึ่งลิฟต์ = หนึ่งโปรเซสที่เขียนไฟล์นั้น** สองตัวเขียนไฟล์เดียวกันแล้วแก้ด้วยมือไม่ได้
   `log_lift.py` ตรวจ `# lift:` ในไฟล์และตรวจ `LIFT=` จาก beacon ให้แล้ว — อย่าปิดการตรวจนี้

> ทั้งห้าข้อคือรูปแบบเดียวกับ §6.15: ข้อมูลจะ**ดูปกติ**ทั้งที่ถอดผิด ไม่มี exception ไม่มี error

### ⚠️ เลข COM เปลี่ยนเองได้ — อย่ายึดเป็นตัวระบุลิฟต์

dongle เป็น CH340 **ไม่มี USB serial number** ⇒ Windows ตั้งเลขตาม**ช่องที่เสียบ**
เกิดขึ้นจริงแล้ว: Lift 2 ย้ายจาก COM20 ไป COM22 เอง

**ตัวระบุที่เชื่อได้คือ beacon ที่บอร์ดประกาศเองทุก 30 วินาที** —
`FW IODebug <ver> <date> LIFT=<n>` ⇒ หาพอร์ตด้วย:

```
python port_resolver.py        # ถามทุกพอร์ตว่าลิฟต์ไหนอยู่ตรงไหน
```

`log_lift.py` ตรวจ `LIFT=` สองจุด (ก่อนเขียนบรรทัดแรก และทุกครั้งที่ beacon มา)
และ**หยุดตัวเองภายใน 30 วินาที**ถ้าเจอบอร์ดผิด ⇒ ข้อมูลลิฟต์หนึ่งไปโผล่ใน log
อีกลิฟต์ไม่ได้

> **เดิมจงใจไม่ผูกพอร์ตใหม่อัตโนมัติ** — โปรแกรมที่เจอบอร์ดผิดรู้แค่ข้อเท็จจริงเชิงลบข้อเดียว
> แยกไม่ออกว่า "คนย้ายสาย" หรือ "dongle ตายแล้วบอร์ดอื่นมาตอบแทน"
> การซ่อมตัวเองที่เงียบแยกไม่ออกจากการซ่อมผิดที่เงียบ (§6.15)
> ⇒ ให้หยุดดัง ๆ แล้วให้คนหรือ agent ตัดสิน

> ### ✅ 12 ก.ย. 2026 — ผูกพอร์ตใหม่อัตโนมัติได้แล้วด้วย `--follow`
>
> **ข้อความเดิมด้านบนยังถูกอยู่ แต่ถูกกับ *การเดา* เท่านั้น** — ที่อันตรายคือโปรแกรมที่เจอบอร์ดแปลกหน้า
> แล้ว "เลือกพอร์ตอื่นมาแทน" เพราะมันแยกไม่ออกว่าคนย้ายปลั๊กหรือ dongle ตาย
>
> `--follow` ไม่ได้เดา มันผูกเฉพาะพอร์ตที่บอร์ด**เพิ่งประกาศเลขลิฟต์ตรงกับตัวเอง** ซึ่งคือหลักฐาน
> ชุดเดียวกับที่ `listen_check()` เรียกร้องก่อนเขียนบรรทัดแรกอยู่แล้ว ⇒ **ตัวตนคือกุญแจ เลข COM เป็นแค่ที่จับ**
>
> มันปฏิเสธสามกรณี และการปฏิเสธคือหัวใจของมัน:
> 1. พอร์ตที่ประกาศเลขลิฟต์อื่น → ข้าม ไม่รับมาแทน
> 2. พอร์ตที่ตัวบันทึกอื่นถืออยู่ → ข้าม (Windows คืน `PermissionError` ซึ่งต่างจาก `FileNotFoundError`
>    ของพอร์ตที่หายไป ⇒ ค้นหาข้าง ๆ ตัวบันทึกที่กำลังทำงาน 4 ตัวได้โดยไม่แย่งพอร์ตใคร — **ทดสอบจริงแล้ว**)
> 3. **สองพอร์ตประกาศเลขลิฟต์เดียวกัน → หยุดโปรเซสทันที** นั่นคือบอร์ดสองตัวถูกแฟลชด้วย `-DLIFT_ID`
>    เดียวกัน เลือกตัวไหนก็ทำให้ข้อมูลลิฟต์หนึ่งไปอยู่ในล็อกอีกลิฟต์ ซึ่งคือความเสียหายที่การตรวจตัวตน
>    ทั้งหมดในไฟล์นี้มีไว้กันตั้งแต่แรก
>
> ทุกครั้งที่ค้น มันเขียนสิ่งที่เห็นลงล็อก (`--- searching for Lift 1: COM20=2 COM22=claimed ... ---`)
> และเขียน `--- rebound to COM30 (was COM20) ---` ตอนย้าย ⇒ **การซ่อมตัวเองไม่เงียบ** ซึ่งเป็นเงื่อนไข
> ที่ข้อความเดิมตั้งไว้พอดี · `capture_status.py` อ่าน marker พวกนี้แล้วรายงานพอร์ตปัจจุบันให้ถูกต้อง


### ข้อจำกัดที่ยังอยู่ — ต้องรู้ก่อนต่อ Lift 4/5

- **บอร์ดที่ไม่ส่ง `LIFT=` ผูกอัตโนมัติไม่ได้เลย** ทั้ง firmware ก่อน 1.2.0 และ build
  ที่ลืม `-DLIFT_ID` (จะได้ `LIFT=0`) ⇒ **แฟลชให้ครบก่อนต่อ**
- เส้นทาง USB (ไม่ใช่ `--listen`) **ไม่ตรวจตัวตนตอนเชื่อมต่อ** — `arm()` ไม่รับ `LIFT`
  มีช่วง ~27 วินาทีก่อน beacon แรกที่เขียนข้อมูลผิดลิฟต์ได้
- เปิดพอร์ตไม่ได้จะ**วนลองใหม่ทุก 3 วินาทีไม่มีที่สิ้นสุด** และบรรทัดสรุปทุก 15 วินาที
  ไปไม่ถึง ⇒ โปรเซสดูเหมือนทำงานอยู่ทั้งที่ไม่ได้เก็บอะไร
- นาฬิกาบอร์ดเดินช้ากว่านาฬิกา PC **0.78%** (สะสม 68 นาทีใน 5.8 วัน) คงที่มาตั้งแต่
  สิงหาคม ⇒ **ใช้เวลา PC เสมอ อย่าใช้ `board_ms` คำนวณช่วงเวลาจริง**

---

## 10. ข้อสังเกตเรื่องวิธีทำงาน

- 🔴 **commit + push ทุกครั้งที่มีการเปลี่ยนแปลง** ไม่ต้องรอให้สั่ง
  (ผู้ใช้กำหนดเป็นกฎถาวรเมื่อ 30 ส.ค. 2026) — repo: https://github.com/Meltingth/WhizdomLift.git
  เหตุผล: ข้อมูลทดสอบเกิดจากการจองลิฟต์กับทีมดูแล ทำซ้ำไม่ได้ง่าย ๆ
  เสร็จแล้วต้อง**ตรวจว่า working tree สะอาดและ local = origin/main จริง** ไม่ใช่แค่สั่ง push แล้วเชื่อ
- **ผู้ใช้สื่อสารเป็นภาษาไทย** ตอบเป็นไทย เก็บศัพท์เทคนิคและโค้ดเป็นอังกฤษ
- การทดสอบที่ต้องให้ผู้ใช้ทำอะไรทางกายภาพ **ห้ามใช้วิธียิงหน้าต่างเวลาแล้วหวังว่าเขาจะทำทัน**
  — ระบบไม่ส่งข้อความผู้ใช้กลับมาระหว่างสคริปต์รัน เคยเสียไป 4 รอบเพราะเรื่องนี้
  ให้สร้างเครื่องมือที่ผู้ใช้รันเองตามจังหวะตัวเอง หรือถามให้ยืนยันก่อนแล้วค่อยรัน
- ตรวจสอบผลของทุกอย่างที่ทำเสมอ — บั๊กเกือบทั้งหมดในหัวข้อ 6 ถูกจับได้เพราะไปเทียบผลอัตโนมัติ
  กับการไล่อ่านข้อมูลดิบด้วยมือแล้วพบว่าไม่ตรงกัน

## 11. Models and effort - never pin model IDs
Aliases resolve to the newest release of each family automatically (`fable`, `opus`, `sonnet`),
so a model upgrade is picked up without editing anything. Do not write `claude-opus-5`-style IDs
anywhere in this repo.
- Lead / orchestrator: the interactive session runs on `fable` with **ultracode** on
  (`.claude/settings.json`: `"model": "fable"`, `"ultracode": true`). The lead plans, reviews plans,
  and handles escalations (workflow steps 1 and 7) - ultracode = xhigh reasoning + automatic dynamic
  workflows, so the lead fans large review/verification work out to parallel agents by itself.
- Subagents (`.claude/agents/`), each pinned by alias with `effort: max` in frontmatter:
  `implementer-sonnet` (step 2) · `reviewer-opus` (step 4) · `fixer-opus` (steps 5, 8) ·
  `verifier-opus` (step 6) · `verifier-sonnet` (step 9) · `fable-advisor` (step 7, optional).
- NEVER set `CLAUDE_CODE_EFFORT_LEVEL`: it overrides every subagent effort and disables ultracode orchestration.
- Verify with `/status` (lead: Fable + ultracode) and `/tasks` (each subagent row shows model + effort).


## 12. The phase loop - run `/phase <id>`
1 plan (lead) -> 2 implement (implementer-sonnet) -> 3 deploy (`scripts/deploy-phase.ps1`) ->
4 review + test plan + test tools (reviewer-opus) -> 5 fix (fixer-opus) -> 6 verify (verifier-opus) ->
7 escalate: diagnose + fix plan + revised test plan (lead / fable-advisor) -> 8 fix per plan (fixer-opus) ->
9 verify per plan (verifier-sonnet) -> 10 loop until the phase DoD is green, then commit + push ->
11 next phase.

## 13. Guards
- Max 4 loop cycles per phase, then stop and ask the human.
- Never commit a red tree; never force-push; no contract changes (MQTT v1 / OpenAPI v1 / Schema v1 / UI enums) without a human decision.
- No fake data on real-lift asset IDs (W-01..W-05); simulated data is tagged SIMULATED or runs in Demo Mode.
- In the WhizdomLift repo, its CLAUDE.md §4 safety rules and §6 lessons are absolute (read-only serial, never OUTPUT pins).

## 14. Artifacts
`docs/workflow/PHASES.md` (phases + DoD) · `WORKFLOW_STATE.md` (live state) · `docs/workflow/plans/` ·
`docs/workflow/tests/` (test plans) · `tests/phase-<id>/` (executable tests, `run.ps1`) · `docs/workflow/logs/phase-<id>.md` (cycle log).

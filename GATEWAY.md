# Gateway — รับข้อมูลลิฟต์ทั้ง 5 ตัวผ่าน RS485

เครื่อง Gateway เป็นเครื่องแยกจากเครื่องที่ใช้ debug หน้าตู้ ทำหน้าที่รับข้อมูลจาก
ลิฟต์ทั้ง 5 ตัวพร้อมกันผ่านตัวแปลง RS485-USB หลายตัวบน USB hub

---

## 📌 สถานะเครื่อง Gateway ณ 30 ส.ค. 2026 (ก่อนย้ายสถานที่)

เครื่องถูกปิดเพื่อย้ายไปเชื่อมต่อผ่าน hub ที่มี USB uplink บันทึกไว้เพื่อกลับมาทำต่อ

**สภาพแวดล้อมที่ติดตั้งแล้ว — ไม่ต้องทำซ้ำ**

| รายการ | สถานะ |
|---|---|
| Python | 3.12.5 ที่ `C:\Python312` |
| `pyserial` | **3.5 ติดตั้งแล้ว** (เดิมไม่มี ทำให้เครื่องมือ serial ทั้ง 5 ตัวตายตั้งแต่ import) |
| git identity | `Meltingth <meltingth@gmail.com>` ตั้งเป็น repo-local |
| repo | `D:\WhizdomLift` sync กับ origin/main, tree สะอาด |
| บอร์ดที่ต่ออยู่ | **ไม่มีเลย** — เหลือแค่ COM1/COM2 ที่เป็นพอร์ตในตัวเมนบอร์ด |

**ยังไม่เคยรับลิฟต์ตัวไหนมาเก็บข้อมูลบนเครื่องนี้** เคยต่อบอร์ด Lift 1 ผ่าน USB
ตรง ๆ ที่ COM14 เพื่อทดสอบสภาพแวดล้อม (ผ่านทั้ง handshake, watch mode, 12/12 heartbeat)
แล้วบอร์ดถูกถอดไปซ่อมสาย VS2

### ⚠️ ตรวจก่อนเสียบครั้งแรกหลังย้าย: RS232 ≠ RS485

ถ้า hub ที่ใช้เป็น **USB-to-RS232 หลายพอร์ต** จะต่อกับ MAX485 **ไม่ได้**
สองมาตรฐานนี้ต่างกันทางไฟฟ้าคนละเรื่อง:

| | RS232 | RS485 |
|---|---|---|
| สัญญาณ | single-ended เทียบกราวด์ | **differential คู่ A/B** |
| แรงดัน | ±3 ถึง ±15 V | 0–5 V ต่างศักย์ |
| โครงสร้าง | จุดต่อจุด | multi-drop |

เสียบ A/B ของ RS485 เข้าพอร์ต RS232 จะ **ไม่ได้ข้อมูล หรือได้ขยะ** และแรงดัน RS232
ที่สวิงถึง ±15 V อาจทำ transceiver พัง สิ่งที่ต้องการคือ **USB hub ธรรมดา + dongle RS485 ตัวละลิฟต์**
(hub ธรรมดาใช้ได้ปกติ — แต่ละ dongle เป็นลิงก์อิสระของตัวเอง)

ถ้าเป็น hub ที่มีพอร์ต RS485 จริงให้ตรวจว่าเป็น **repeater ที่รวมทุกพอร์ตเป็นบัสเดียวหรือไม่**
ถ้ารวม จะชนกันตามที่อธิบายในหัวข้อ "หนึ่งคู่สายต่อหนึ่งลิฟต์" ด้านล่าง

### ขั้นตอนกลับมาทำต่อ

1. `git pull` — อีก session ทำงานต่อเนื่องที่เครื่องหน้าตู้ มีของใหม่แน่นอน
2. ดูพอร์ตที่เห็นจริง (phantom เยอะมากบนเครื่องนี้ — ครั้งก่อนมีค้าง 19 รายการ)
   ```
   python -c "from serial.tools import list_ports; [print(p.device, p.description, p.hwid) for p in list_ports.comports()]"
   ```
   **ดู Status = OK เท่านั้น** อย่าเชื่อเลข COM ที่เคยใช้
3. ยืนยันลิงก์ก่อนเชื่อข้อมูล — `log_lift.py <COM> <lift> --listen` ต้องขึ้น
   `listening - board is reporting on its own` ภายใน 75 วินาที
4. ตรวจ `rejected` ต้องเป็น **0** ถ้าไม่เป็นศูนย์คือสาย/termination/baud มีปัญหาจริง

### เรื่องที่ยังค้างอยู่

- **Lift 1** ซ่อมสาย VS2 อยู่ที่เครื่องหน้าตู้ จะส่งพอร์ตมาให้เมื่อเสร็จ
- **การระบุว่าพอร์ตไหนคือลิฟต์ไหน** — ผู้ใช้เลือกทางฝัง `LIFT_ID` ตอน build
  (`FW IODebug 1.2.0 ... LIFT=2`, default `LIFT=0` เพื่อให้ build ที่ลืมใส่แฟล็กฟ้องตัวเอง)
  เหตุผลที่ต้องมี: dongle CH340 **ไม่มี serial number** (`SER=` ว่าง, instance ID เป็นเส้นทาง hub)
  ⇒ ย้ายช่องเสียบเมื่อไหร่เลข COM เปลี่ยน และเมื่อ Lift 1 ซ่อมเสร็จ ทั้ง 5 ตัวจะส่งข้อมูลเหมือนกันหมด
- `log_lift.py` ฝั่งตรวจ `LIFT=` เทียบกับ argument ยังไม่เสร็จ (เครื่องหน้าตู้ทำอยู่)
  **ต้องยอมรับบอร์ดที่ไม่ส่ง id** ไม่งั้นจะล็อกลิฟต์ที่ยังไม่ได้อัปเกรดออกทั้งหมด

---

## สถาปัตยกรรม

```
Lift 1  Arduino ─ MAX485 ─┐
Lift 2  Arduino ─ MAX485 ─┤  คู่สายบิดเกลียว
Lift 3  Arduino ─ MAX485 ─┤  แยกกันคนละคู่
Lift 4  Arduino ─ MAX485 ─┤
Lift 5  Arduino ─ MAX485 ─┘
                           │
              ตัวแปลง RS485-USB ตัวละลิฟต์
                           │
                      USB hub
                           │
                   Gateway PC  ── log_lift.py 5 โปรเซส
```

## ⛔ หนึ่งคู่สายต่อหนึ่งลิฟต์ — ห้ามรวมบัส

sketch ส่งข้อมูลเมื่อไหร่ก็ได้ **ไม่มี address ไม่มี arbitration ไม่มี checksum**
เอาหลายลิฟต์มาไว้บัสเดียวจะชนกัน (คำนวณไว้ใน `RS485_PLAN.md` §5)

**RS485 hub 4 พอร์ตไม่ได้ช่วย** — repeater hub รวมทุกพอร์ตเป็นบัสตรรกะเดียว
ข้อมูลจาก 4 ลิฟต์จะมาชนกันบนสายเส้นเดียวเหมือนเดิม

และ **Windows เปิด COM port แบบ exclusive** ⇒ 5 โปรเซสแชร์ dongle ตัวเดียวไม่ได้อยู่แล้ว

## การรัน

หนึ่งโปรเซสต่อหนึ่งลิฟต์:

```
python log_lift.py COM7  1 --listen
python log_lift.py COM8  2 --listen
python log_lift.py COM9  3 --listen
python log_lift.py COM10 4 --listen
python log_lift.py COM11 5 --listen
```

`--listen` คือโหมดไม่ส่งอะไรเลย จำเป็นสำหรับ RS485 แบบส่งทางเดียว
หยุดทุกตัวพร้อมกันด้วยการสร้างไฟล์ `STOP_CAPTURE`

## ⚠️ เลข COM สลับได้ ห้ามยึดเป็นตัวระบุลิฟต์

Windows เปลี่ยนเลข COM เมื่อสลับอุปกรณ์หรือเสียบคนละพอร์ต **ต้องระบุเลขลิฟต์เสมอ**
`log_lift.py` ประทับ `# lift: N` ไว้ในไฟล์และปฏิเสธที่จะเริ่มถ้าชื่อไม่ตรง
— กันไม่ให้ข้อมูลลิฟต์หนึ่งไปเขียนต่อท้ายอีกลิฟต์โดยไม่มีอะไรเตือน

**วิธียืนยันว่าพอร์ตไหนคือลิฟต์ไหน**: ดูจากข้อมูลที่ถอดได้ ไม่ใช่จากเลขพอร์ต
สั่งลิฟต์ตัวนั้นวิ่งแล้วดูว่าพอร์ตไหนมีข้อมูลขยับ

## ตรวจสุขภาพลิงก์

`log_lift.py` รายงานสามค่าขณะทำงาน:

| ค่า | ความหมาย | ค่าที่ควรได้ |
|---|---|---|
| `lines` | จำนวน state change ที่บันทึก | เพิ่มขึ้นเมื่อลิฟต์วิ่ง |
| `beats` | heartbeat ที่ร้องขอ | 0 เสมอในโหมด `--listen` |
| `rejected` | บรรทัดที่รูปแบบผิด | **ต้องเป็น 0** |

`rejected` ไม่เป็นศูนย์เมื่อไหร่ = สาย, termination หรือ baud มีปัญหา
มาสก์ที่ขาดไปหนึ่งหลักยัง parse เป็นเลขฐานสิบหกได้ **แต่เลื่อนทุกบิต** จึงต้องตรวจความยาว

**ไม่มีข้อยกเว้น แม้ระหว่างอัปเกรด firmware** — ทราฟฟิก bootloader เป็นไบนารี
ไม่ขึ้นต้นด้วย `ST ` จึงถูกทิ้งโดยไม่ถูกนับ (ทดสอบแล้ว 110,000 ไบต์ ได้ rejected 0)
ช่วงอัปเกรดจะเห็นเป็น **ความเงียบ** ไม่ใช่บรรทัดเสีย ⇒ `rejected` ขยับเมื่อไหร่คือของจริงเสมอ
ดู `FIRMWARE_UPGRADE.md`

## git ขณะที่ตัวบันทึกกำลังทำงาน

`git pull --ff-only` เฉย ๆ **ปลอดภัย** — มันไม่แตะไฟล์ที่ไม่ได้อยู่ใน diff ขาเข้า
และตามกฎ "หนึ่งลิฟต์ต่อหนึ่งเครื่อง" ไฟล์ `capture_lift_N.log` ที่เครื่องนี้ถืออยู่
จะไม่มีใครแก้จากอีกฝั่ง จึงไม่มีวันชนกัน

⛔ **ห้าม `git stash` ไฟล์ log ที่ logger เปิดค้างอยู่** Windows ล็อกไฟล์ไว้ ผลคือ:

```
git stash push capture_lift_2.log
  error: unable to write file 'capture_lift_2.log': Permission denied
git stash pop
  Dropped refs/stash@{0}
```

stash **ถูกสร้างและ stage ไปแล้ว** แต่เขียนทับ working file ไม่ได้ พอ pop ก็ drop ทิ้ง
เหลือ index ค้างสถานะ `MM` ที่ไม่ตรงกับไฟล์จริง

ข้อมูลรอดเพราะ**ล็อกของ Windows กันไว้เอง** ไม่ใช่เพราะ git ระวัง — อย่าพึ่งโชคซ้ำ
ถ้าเผลอทำไปแล้ว ล้างด้วย `git reset HEAD -- <file>` แล้ว**ตรวจว่าข้อมูลครบจริง**:

```
git show HEAD:capture_lift_2.log | tr -d '\r' > /tmp/head.txt
head -n $(wc -l < /tmp/head.txt) capture_lift_2.log | tr -d '\r' | diff - /tmp/head.txt
```

ต้องไม่มีความต่าง — แปลว่าไฟล์เดิมเป็น prefix ของไฟล์ปัจจุบันพอดี คือ append อย่างเดียว ไม่มีอะไรหาย
**ต้องตัด `\r` ก่อนเทียบ** ไม่งั้นจะเห็นว่าต่างกันทั้งไฟล์ทั้งที่เหมือนกันหมด เพราะ working file
เป็น CRLF ส่วน blob ใน git เป็น LF

## เช็คลิสต์ต่อลิฟต์ใหม่

1. Arduino มี sketch `IODebug` เวอร์ชันที่ arm ตัวเอง (ตรวจ: เปิดพอร์ตแล้วต้องมี `ST` มาเองโดยไม่ต้องส่งคำสั่ง)
2. MAX485 ต่อแบบส่งทางเดียว — `DI`→ขั้ว 1, `DE`+`RE`→5V, `RO` ไม่ต่อ
3. ไฟเลี้ยงจากอะแดปเตอร์ 2 ขาแยก **ห้ามดึงจากราง 24V ของตู้**
4. terminator รวมกันทั้งบัส 60Ω (ปลายละ 120Ω) — โมดูล MAX485 มี R7=120Ω ติดมาแล้ว
5. รัน `log_lift.py COM<n> <lift> --listen` ต้องขึ้น `listening - board is reporting on its own`
6. วิ่งลิฟต์หนึ่งเที่ยวเต็ม **จดชั้นด้วยมือแล้วเทียบ** — `lift_health.py <lift>` ต้องได้ single-step 100% และ `HEALTHY`

**ห้ามข้ามข้อ 6** — RS485 เป็นการเปลี่ยนแปลงประเภทที่ให้ข้อมูลผิดที่ดูน่าเชื่อ
มากกว่าจะพังให้เห็นชัด ๆ

## กฎที่ห้ามละเมิด

1. **ห้ามตั้งขาใดเป็น `OUTPUT`** — ขาต่อกับหน้าสัมผัสรีเลย์ ขับ HIGH ใส่หน้าสัมผัสที่ปิดอยู่ = พอร์ตพัง
2. **ห้ามมัด GND ของ Arduino เข้ากับลบของ 24V หรือกราวด์ตู้** — ทำลายฉนวนกั้นชั้นเดียวที่มี
3. **ห้ามเรียก `Serial1.begin()` / `Serial2.begin()`** — UART สองตัวนั้นนั่งทับ D16/D17/D19 ซึ่งเป็นสาย RUNNING, SAFETY, UP

รายละเอียดทั้งหมดอยู่ใน `CLAUDE.md` §4 และ `RS485_PLAN.md` §2

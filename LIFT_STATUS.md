# สถานะการทดสอบลิฟต์ทั้ง 5 ตัว

ตารางนี้เป็นเอกสารที่ **อัปเดตทุกครั้งที่ทดสอบ** — ดูที่นี่ที่เดียวว่าลิฟต์ไหนตรวจแล้ว เจออะไร ต้องซ่อมอะไร

## ชื่อลิฟต์

ระหว่างทดสอบเราเรียกตามลำดับที่ต่อบอร์ด (A, B, C…) แต่ชื่อจริงในอาคารคือเลขลิฟต์
เครื่องมือทุกตัว**รับได้ทั้งสองแบบ** (`lift_health.py 2` เท่ากับ `lift_health.py C`)

| เลขลิฟต์จริง | label ตอนทดสอบ | ไฟล์ข้อมูล | firmware บนบอร์ดจริง | เครื่องที่บันทึกอยู่ |
|---|---|---|---|---|
| **Lift 1** | B | `capture_lift_1.log` | **1.2.2 LIFT=1** ✅ | **Gateway COM20** — ✅ ซ่อม VS2 แล้ว 12 ก.ย. |
| **Lift 2** | C | `capture_lift_2.log` | **1.2.2 LIFT=2** ✅ | **Gateway** — หาพอร์ตจาก beacon `LIFT=2` อย่ายึดเลข COM |
| **Lift 3** | A | `capture_lift_3.log` ← **ตัวอ้างอิง** | **1.2.2 LIFT=3** ✅ | **Gateway — RS485 ขึ้นแล้ว** 6 ก.ย. (beacon `LIFT=3`) |
| **Lift 4** | D | `capture_lift_4.log` | — | ⛔ ปิดใช้งาน |
| **Lift 5** | E | `capture_lift_5.log` | **1.2.2 LIFT=5** ✅ | **เครื่องหน้าตู้ COM14 (RS485)** — ✅ blind test ผ่าน 12 ก.ย. 18:30 |

> firmware ในคอลัมน์นี้คือสิ่งที่**อ่านกลับมาจากบอร์ดจริง** ไม่ใช่สิ่งที่คิดว่าแฟลชไป —
> ยืนยันจากบรรทัด `FW IODebug <ver> <date> LIFT=<n>` ที่บอร์ดประกาศเองทุก 30 วินาที

### 🔴 กฎ: หนึ่งลิฟต์ บันทึกได้ทีละหนึ่งเครื่องเท่านั้น

`capture_lift_N.log` เป็นไฟล์เดียวใน repo เดียว ถ้าสองเครื่อง append พร้อมกันแล้วต่างคน
ต่าง push จะชนกัน และการ merge ล็อกแบบต่อท้ายสองสายที่สลับกันมา **แก้ด้วยมือไม่ได้**
การส่งต่อลิฟต์ระหว่างเครื่องต้อง**บอกกันก่อนเสมอ** ให้ฝั่งเดิมหยุดจริงก่อนฝั่งใหม่เริ่ม

### ส่งมอบ Lift 1 ให้ Gateway — 30 ส.ค. 2026 (ส่งไปทั้งที่ยังเสีย)

ย้ายตัวแปลง USB-RS485 ของ Lift 1 ไปเครื่อง Gateway **โดยที่ลิงก์ยังไม่ขึ้น** เจตนาคือ
ให้ Gateway ได้ข้อมูลทันทีที่มีคนไปแก้ไฟเลี้ยงหน้าตู้ ไม่ใช่เพราะคิดว่าซ่อมแล้ว

**ย้ายดongเกิลไม่ได้แก้ปัญหานี้** — จุดเสียอยู่ที่ปลายทางฝั่งลิฟต์ Gateway จะเจอนอยส์ชุดเดิมเป๊ะ

สิ่งที่ Gateway ควรเห็น **ถ้ายังไม่มีใครไปแก้**:

```
python rs485_check.py COM<n> 1     # 45 วิ ได้คำตอบ -> VERDICT: UNDRIVEN
python log_lift.py COM<n> 1 --listen
  -> listen_check ไม่เจอ ST เลยใน 75 วิ (นานกว่า heartbeat 60 วิ)
     ลองใหม่ 3 ครั้งแล้วเลิก พร้อมข้อความที่จบด้วย "and that the board has power"
```

> 🔴 **ใช้ได้เฉพาะพอร์ตที่ยืนยันแล้วว่าเป็น Lift 1 จริง** (เห็น `LIFT=1` หรือเห็นลิฟต์วิ่งแล้วข้อมูลขยับ)
> บนพอร์ตที่ยังไม่ยืนยัน `UNDRIVEN` แปลว่า **ไม่รู้ว่าพอร์ตนี้คือลิฟต์อะไร** ไม่ได้แปลว่าลิฟต์เสีย
> ตอนนี้มี dongle มากกว่าลิฟต์ที่ต่ออยู่ พอร์ตที่ไม่มีสายลงก็ให้ผลหน้าตาเดียวกันเป๊ะ

⚠️ **และ `SILENT` กับ `UNDRIVEN` แยกสองสถานการณ์ออกจากกันไม่ได้** — ลิงก์ตายเส้นเดียว
ที่ไม่ได้แตะอะไรเลยเคยให้ผลครบทั้ง `INCONCLUSIVE / UNDRIVEN ×3 / SILENT` ใน 5 รอบติด
(วัดบน dongle ของ Lift 1 เอง — ดู `rs485_check.py` docstring) ⇒ **ทั้งสองคำตอบคือ "ไม่มีใครขับสาย" เหมือนกัน**

**พฤติกรรมที่ถูกต้องคือตัวบันทึกไม่ยอมนั่งเขียนไฟล์ว่างเงียบ ๆ**
(บทเรียน §6.3 และ §6.12) ให้ตรวจด้วย `rs485_check.py` ก่อนเสมอ เพราะได้คำตอบใน 45 วินาที
แทนที่จะรอ 4 นาทีให้ตัวบันทึกลองครบ 3 รอบ

ทันทีที่บอร์ดมีไฟ ลิงก์จะขึ้นเองโดยไม่ต้องแตะอะไรอีก — sketch arm ตัวเองตั้งแต่บูต และ
ประกาศ `LIFT=1` ทุก 30 วินาที ⇒ `--listen` จะยืนยันบอร์ดได้เองแล้วเริ่มบันทึก
**แต่ VS2 จะยังขาดอยู่** รหัสสูงสุดจะยังเป็น 23 จนกว่าจะซ่อมสายเส้นนั้นแยกต่างหาก

> Lift 5 = E เป็นการอนุมาน — ผู้ใช้ระบุมา 4 ตัว (1,2,3,4) เหลือเลข 5 ตัวเดียว **ยังไม่ได้ยืนยัน**

## สถานะปัจจุบัน

| ลิฟต์ | วันที่ทดสอบ | สถานะ | สิ่งที่พบ |
|---|---|---|---|
| **Lift 1** (B) | 8 ส.ค. → **12 ก.ย. 2026** | ✅ **HEALTHY — ครบทั้ง 10 เส้น** | **ซ่อม VS2 สำเร็จ 12 ก.ย. 15:38** หลังเสียมา 35 วัน · ยืนยันด้วยการวิ่งจริงขึ้นชั้น 44 แล้วลงชั้น 1: toggle cascade **154/76/38/18/8/4 = 2.00–2.25× ทุกขั้น** · single-step **154/154 = 100%** · รหัสเลขคี่ **78 จาก 155** (เดิม 0) · ช่วงรหัส 2–46 = ชั้น 1–44<br>⚠️ ใช้ `POSITION_BITS` กับข้อมูล**หลังจุดรีเซ็ตสุดท้าย 12 ก.ย. 15:38:58** เท่านั้น ก่อนหน้านั้นใช้ `POSITION_BITS_B` — 🔴 **session ปัจจุบันมีจุดที่ `board_ms` ถอยเป็น 89 ถึง 3 จุด (13:44:10 · 13:49:43 · 15:38:58) ต้องใช้จุดสุดท้าย** วัดยืนยันแล้ว ดู CLAUDE.md §3.2<br>🔴 **ถอนข้อสังเกตเดิมเรื่อง SAFETY 7 ก.ย. 41 นาที** — ช่วงนั้นมีคนทำงานกับสาย/บอร์ดของ Lift 1 อยู่ ⇒ ตีความเป็นพฤติกรรมลิฟต์ไม่ได้ ดู CLAUDE.md §9.7 |
| **Lift 2** (C) | 30 ส.ค. 2026 | ✅ **HEALTHY** (ผ่าน RS485, อยู่ที่ Gateway) | ครบทั้ง 10 เส้น decode 100% codes 1–46 — **ซ่อมแล้ว**: COM ฝั่งขาออกของโมดูล 8 ช่องเคยต่อลงขั้ว `5V` แทน `GND` ทำให้หน้าสัมผัสปิดแล้ว Arduino มองไม่เห็น · **ย้ายมารับสัญญาณผ่าน RS485 สำเร็จแล้ว** (MAX485 ส่งทางเดียว, mask ถูกต้อง 292/292) · **30 ส.ค. 20:2x ย้ายไปเครื่อง Gateway แล้ว** (ตอนนั้นได้ COM20 — *เลขนี้เป็นประวัติ ไม่ใช่ปัจจุบัน*: 6 ก.ย. อ่านได้ที่ COM22) อ่านกลับได้ `FW IODebug 1.2.2 2026-08-31 LIFT=2` จากบอร์ดที่รันอยู่ — ยืนยันว่าเลขประจำลิฟต์ถูกแฟลชลงบอร์ดจริง |
| **Lift 3** (A) | 8 ส.ค. + **6 ก.ย. 2026** | ✅ **HEALTHY — ผ่าน RS485 แล้ว** | **MAX485 ติดตั้งอยู่จริงและทำงาน** (ข้อสงสัยว่ายังไม่ได้ติด — ตัดออกได้) · COM21, beacon `LIFT=3`, `VERDICT: UP`<br>**พิสูจน์แล้วว่าบั๊ก §6.1 หายจริง**: toggle cascade ได้ **D24=16 · D25=8 · D26=4 · D27=2 คือ 2.00× ทุกขั้นพอดี** และ single-step **16/16 = 100%** ด้วยแผนที่ 6 บิตอ้างอิง · malformed 0<br>⚠️ หน้าต่างที่วัดลิฟต์วิ่งแค่ชั้นล่าง (codes 2–10) **D28/D29 จึงยังไม่ถูกกระตุ้น** — ยังไม่ได้ยืนยันว่าแตะรหัส 46 ได้ ต้องรอให้วิ่งขึ้นชั้นสูง |
| **Lift 4** (D) | — | ⛔ **ปิดใช้งาน ข้ามไปก่อน** | ผู้ใช้แจ้ง 12 ก.ย. 2026 |
| **Lift 5** (E) | **12 ก.ย. 2026** | ✅ **HEALTHY** — *lift service* · ⚠️ มีข้อสังเกต 2 ข้อ | **เลข 5 ยืนยันแล้ว** ผู้ใช้ระบุเอง · firmware `1.2.2 LIFT=5` · โปรแกรมเดิมของทีมอื่นสำรองไว้ที่ `firmware_backup/lift5_original_*`<br>**ตัดสินจากช่วงสะอาด 16:46:50 เป็นต้นไป**: ครบทั้ง 10 เส้น · cascade `87/43/21/9/4/1` ลดครึ่งทุกขั้น · **single-step 125/125 = 100%** · รหัสคี่ 63/126 (VS2 ทำงานเต็มที่) · floor-0 readings 0 · rejected 0<br>ตรงกับที่ผู้ใช้จดทุกจุด: ถึงยอด 16:51:48–16:52:12 (จด 16:52) · ชั้น 1 ที่ 16:53:18 (จด 16:53) · **B1 ที่ 16:53:41** · กลับขึ้นชั้น 1 ที่ 16:54:04<br>🔍 **ข้อสังเกต 1 — รหัส 47 ไม่มีในตาราง** ลิฟต์นี้ขึ้นเหนือชั้น 44 (รหัส 46) ไปอีกหนึ่งระดับ จอดจริง 25 วินาที (ช่วง 18.5 วินาที RUNNING ดับ = จอดสนิท ไม่ใช่ค่ากลางทาง) · ฝั่งล่างตรงกับตารางอ้างอิงเป๊ะ (รหัส 1 = B1, รหัส 2 = ชั้น 1) ⇒ **ยืนยันซ้ำด้วย blind test 12 ก.ย. 18:30 ในหน้าต่างสะอาด ไม่มีใครแตะตู้** — จอดที่รหัส 47 โดย RUNNING เปิด 16.4 วินาที (รวมอยู่ที่รหัสนั้น 23 วินาที) · single-step 92/92 = 100% · cascade 92/46/22/10/4/2<br>⚠️ **แต่ยังบอกไม่ได้ว่าระดับที่เพิ่มมาอยู่ตรงไหน** — ถ้าอยู่ระหว่างชั้น 1–44 แปลว่า *ชั้น 44 ของลิฟต์นี้คือรหัส 47* · ถ้าอยู่เหนือชั้น 44 แปลว่า *ชั้น 44 ยังเป็นรหัส 46* **สองแบบนี้แยกได้ด้วยการจอดทดสอบเท่านั้น** ดูหัวข้อ “งานค้าง” ด้านล่าง<br>🔍 **ข้อสังเกต 2 — ช่วง 16:42–16:46:50 สถานะทั้งสี่เส้นหายพร้อมกัน** ขณะที่บิตตำแหน่งเดินปกติ **และรถวิ่งอยู่จริง** (วิ่งไม่ได้ถ้าวงจรนิรภัยเปิดจริง) = ลายเซ็นเดียวกับ Lift 2 คือ COM ฝั่งขาออกของโมดูลสถานะไม่ลง · กลับมาเองที่ 16:46:50 แล้วนิ่งตลอด (SAFETY 99%, RUNNING 93%) **ควรขันย้ำขั้ว COM ขาออกของโมดูล 8 ช่องก่อนส่งมอบ** |

---

## ขั้นตอนทดสอบลิฟต์หนึ่งตัว (ใช้เวลาราว 10–15 นาที)

### 1. เสียบบอร์ดแล้วหาพอร์ต

```
python -c "from serial.tools import list_ports; [print(p.device, p.description) for p in list_ports.comports()]"
```

> ถ้าเปิดพอร์ตไม่ได้ (`ERROR_GEN_FAILURE 31`) ให้**ถอดสาย USB แล้วเสียบใหม่** — เป็นอาการ CH340 bind ไม่สมบูรณ์ตอนเสียบครั้งแรก

### 2. อัปโหลด sketch (ครั้งแรกของบอร์ดนั้นเท่านั้น)

```
arduino-cli upload -p COM5 --fqbn arduino:avr:mega:cpu=atmega2560 IODebug
```

ต้องตั้ง env ชี้ toolchain ใน scratchpad ก่อน — ดู CLAUDE.md §5

### 3. เริ่มเก็บข้อมูล — **ระบุเลขลิฟต์เสมอ**

```
python log_lift.py COM5 2      # Lift 2
```

เลขลิฟต์สำคัญกว่าเลขพอร์ต เพราะ Windows สลับเลข COM ไปมาได้
สคริปต์ประทับเลขลิฟต์ไว้ในไฟล์และ**ปฏิเสธที่จะเขียนทับข้อมูลของลิฟต์อื่น**

### 4. สั่งลิฟต์วิ่ง — ต้องครอบคลุมสิ่งเหล่านี้

| ต้องทำ | เหตุผล |
|---|---|
| **ชั้น 1 → ชั้นสูงสุด (44) อย่างน้อย 1 รอบ** | เป็นทางเดียวที่ VS7 (บิตสูงสุด) จะทำงาน ถ้าไม่ทำ จะสรุปไม่ได้ว่า VS7 เสียหรือแค่ไม่ถูกเรียกใช้ |
| **วิ่งทั้งขึ้นและลง** | แยกขา UP กับ DN ออกจากกัน ถ้าวิ่งทางเดียวทั้งสองขาจะดูเหมือนกันหมด |
| **จอดนิ่งสัก 30 วิ** | ยืนยันว่า RUNNING ดับตอนจอด และ SAFETY ยังปิดอยู่ |
| ลง B1 ถ้าลิฟต์ตัวนั้นไปได้ | ตรวจรหัสชั้นใต้ดิน (Lift 3 ได้รหัส 1) |

### 5. หยุดเก็บข้อมูล

```
New-Item STOP_CAPTURE -ItemType File
```
หยุด logger ทุกตัวพร้อมกัน — ลบไฟล์นี้ก่อนเริ่มรอบใหม่

### 6. ตรวจสุขภาพ

```
python lift_health.py 2
python lift_health.py 2 --md     # ได้ block ภาษาไทยไว้แปะตารางข้างบน
```

### 7. บันทึกผลลงตารางด้านบน แล้ว commit

---

## อ่านผลอย่างไร

**สิ่งที่ต้องเห็นถ้าลิฟต์ปกติ:**

- ทั้ง 10 เส้นขึ้น `ok` — ไม่มี `MISSING`
- toggle counts ของ 6 บิตต้อง **ลดลงครึ่งหนึ่งทุกขั้น** (เช่น Lift 3: 1832 → 910 → 458 → 212 → 94 → 39)
- `reference map single-step 100%` และ `floor-0 readings 0`
- `top code 46` (= ชั้น 44)
- UP/DN ต้องแยกกันเด็ดขาด (100%/0% สลับกัน)

**อาการเสียที่พบแล้วและวิธีชี้จุด:**

| อาการในรายงาน | ความหมาย |
|---|---|
| ทุกเส้นสถานะขึ้น `NOISE` พร้อมกัน แต่บิตตำแหน่งปกติ | COM ฝั่งขาออกของโมดูลนั้นต่อผิดขั้ว — เคยเกิดกับ Lift 2 (ต่อลง `5V` แทน `GND`) วัดโวลต์ระหว่าง COM ขาออกกับ GND ของ DNMEGA1 ต้องได้ 0 V |
| ขาใดขาหนึ่ง `MISSING` + `single-step 0%` | สายเส้นนั้นขาด — ดู verdict ว่าเป็นเส้นไหน |
| `best-fit map` เลื่อนบิตลงจากอ้างอิง | มีบิต**ต่ำกว่า**ตายอยู่ ทำให้ทุกเส้นข้างบนเลื่อนตาม |
| `top code` ได้ครึ่งเดียว (23 แทน 46) | บิต 0 (VS2) ตาย — อาการเดียวกับ Lift 1 |
| ขาขึ้น `NOISE` (p99 ของเวลาปิด < 50 ms) | สายปลายเปิดรับไฟ AC เหนี่ยวนำ สัญญาณจริงไม่มาถึง — อาการของ Lift 2 |
| UP หรือ DN ขึ้น `NO - check this line` | สายสถานะเส้นนั้นมีปัญหา |

**ชี้จุดขาดด้วยตาเปล่า** — ขณะลิฟต์วิ่ง ดู LED ของช่องนั้นบนโมดูลรีเลย์:

- **LED กะพริบ แต่ขา Arduino เงียบ** → สายจากเอาต์พุตรีเลย์ → ขั้ว DNMEGA1 หลวม/ขาด
- **LED ไม่กะพริบ** → สายจากตู้ควบคุม → ขั้ว IN ของโมดูลขาด

หรือใช้ `probe_pin.py` แตะจัมเปอร์จาก GND ไปที่ขั้วที่สงสัย — ถ้าขา Arduino ดี โปรแกรมจะประกาศชื่อขาทันที (ปลอดภัย กระแสแค่ 0.15 mA)

---

## 🔧 บันทึกเวลาที่ยุ่งกับฮาร์ดแวร์ — ต้องจดทุกครั้ง

ขาที่อ่านได้ HIGH แยกไม่ออกระหว่าง "สัญญาณเปิดจริง" กับ "สายถูกถอด" (§6.5)
Gateway เคยอ่านข้อมูลช่วงที่มีคนทำงานกับบอร์ดแล้วสรุปว่าลิฟต์หยุดให้บริการ 41 นาที
**ถ้าไม่จด คนอ่านข้อมูลทีหลังจะตีความผิดแบบเดียวกัน**

| วันเวลา | ลิฟต์ | ทำอะไร |
|---|---|---|
| 12 ก.ย. 16:37 | 5 | เสียบ USB — บอร์ดรีเซ็ตตาม §6.17 |
| 12 ก.ย. 16:38–16:39 | 5 | อ่าน flash เดิมออกเก็บไว้ (รีเซ็ตอีกครั้งจาก bootloader) |
| 12 ก.ย. 16:41 | 5 | แฟลช IODebug 1.2.2 `LIFT=5` ทับโปรแกรมเดิม |
| 12 ก.ย. **16:42:24** | 5 | เริ่ม `log_lift.py COM3 5 --listen` *(เดิมจดไว้ 16:44 — ผิด ล็อกบอกเอง)* |
| 12 ก.ย. 16:42–**16:46:50** | 5 | ⚠️ **ช่วงที่สถานะทั้งสี่เส้นหายพร้อมกัน** — อย่าใช้ตีความพฤติกรรมลิฟต์ ดูแถว Lift 5 |
| 12 ก.ย. 16:57:30 | 5 | หยุดเก็บ — 399 changes / 15.4 นาที / rejected 0 |
| 12 ก.ย. **13:44 – 15:38** | **1** | 🔴 **ซ่อมสาย VS2** — บอร์ดรีเซ็ต 3 ครั้ง (13:44:10 · 13:49:43 · 15:38:58) และ **เงียบสนิท 81.8 นาที (14:17:10 → 15:38:57)** ⇒ ข้อมูล Lift 1 ทั้งช่วงนี้ตีความเป็นพฤติกรรมลิฟต์ไม่ได้ |
| 12 ก.ย. 15:47–15:48 | 1 | วิ่งทดสอบหลังซ่อม ขึ้นชั้น 44 ลงชั้น 1 (ผ่าน ดู CLAUDE.md §8.1) |
| 12 ก.ย. 18:07–18:22 | 5 | ต่อ RS485 ที่ COM14 · **บอร์ดรีเซ็ต 18:22:09 ขณะพอร์ตเปิดค้าง** (ผู้ใช้รีเซ็ตระบบ) |
| 12 ก.ย. 18:29:20 | 5 | เริ่ม session ปัจจุบัน `log_lift.py COM14 5 --listen` |
| 12 ก.ย. **18:30–18:34** | 5 | ✅ **blind test — ผู้ใช้อยู่ในลิฟต์ ไม่มีใครแตะตู้ ⇒ หน้าต่างสะอาด** |

⇒ อะไรที่ดูผิดปกติใน `capture_lift_5.log` ช่วงก่อน 16:44 คือผลจากการทำงานกับบอร์ด ไม่ใช่จากลิฟต์

---

## 📌 งานค้าง — รอผู้ใช้กลับไปหน้างาน (ค้างไว้ 12 ก.ย. 2026 18:5x)

ผู้ใช้ออกจากหน้างานโดยยังไม่ได้ทดสอบชั้นของ Lift 5 ให้จบ **ระหว่างนี้เก็บข้อมูลลิฟต์ 1, 2, 3, 5 ต่อไป**
(เตรียมข้อมูลให้ระบบ backend + API ที่จะดึงสถานะการเคลื่อนที่ไปแสดงบนหน้าจอ)

### เช็คว่ายังเก็บอยู่ไหม — คำสั่งเดียว ไม่แตะพอร์ต

```
python capture_status.py
```

### ถ้ามีคนถอด USB แล้วเสียบใหม่ — ระบบกลับมาเองแล้ว

ทั้งสี่ตัวรันด้วย `--follow` ตั้งแต่ 12 ก.ย. 20:46 น. ⇒ **ถอดแล้วเสียบใหม่ช่องไหนก็ได้**
ตัวบันทึกจะค้นทุกพอร์ตที่ว่าง ฟังว่าบอร์ดไหนประกาศ `LIFT=` ตรงกับตัวเอง แล้วผูกพอร์ตนั้นต่อเอง
ใช้เวลาราว 35 วินาทีต่อพอร์ต (ต้องรอ beacon รอบถัดไป) ⇒ อย่างช้าไม่กี่นาทีก็กลับมาเก็บต่อ

ถ้าต้องสั่งเองใหม่ (เช่นหลัง PC รีบูต) **ไม่ต้องหาเลข COM ก่อน** — ใส่ `auto` ไปเลย:

```
python log_lift.py auto 1 --listen --follow
python log_lift.py auto 2 --listen --follow
python log_lift.py auto 3 --listen --follow
python log_lift.py auto 5 --listen --follow
```

🔴 **กรณีเดียวที่มันจะหยุดคือมีสองพอร์ตประกาศเลขลิฟต์เดียวกัน** = บอร์ดสองตัวถูกแฟลช
`-DLIFT_ID` ซ้ำกัน ต้องแฟลชใหม่ให้เลขไม่ซ้ำก่อน มันจะไม่เดาให้

### ถ้าโปรเซสตาย หรือเครื่องรีบูต — ตั้ง Scheduled Task ครั้งเดียว

`--follow` แก้เรื่องเลข COM เปลี่ยน แต่**ไม่ได้แก้เรื่องโปรเซสตายหรือเครื่องรีบูต**
ตัวที่แก้สองเรื่องนั้นคือ task ที่เรียก `scripts/start_captures.ps1` ซึ่ง**สตาร์ตเฉพาะลิฟต์ที่ยังไม่มี
ตัวบันทึก** (รันซ้ำบนเครื่องที่ปกติจะขึ้นแค่ `nothing to start`)

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\install_capture_task.ps1
```

ไม่ต้องใช้สิทธิ์ admin — เป็น task ระดับ user · ตั้งให้รัน **ตอน logon และทุก 15 นาที**
ดูก่อนโดยไม่ลงทะเบียนจริงได้ด้วย `-WhatIf` · ถอนด้วย `scripts\uninstall_capture_task.ps1`

สั่งทำงานทันทีโดยไม่ต้องรอ:

```powershell
Start-ScheduledTask -TaskName 'WhizdomLift captures'
```

🔴 **ข้อจำกัด**: `AtLogOn` ทำงานตอนมีคน logon — เครื่องรีบูตแล้วไม่มีใคร logon จะไม่มีอะไรสตาร์ต
ถ้าต้องรันแบบ headless จริง ๆ ต้องใช้ task ที่เก็บ credential ซึ่งต้องสิทธิ์ admin

#### ✅ ติดตั้งและยืนยันแล้วบนเครื่องนี้ — 12 ก.ย. 2026 22:48

อ่านกลับจาก `Get-ScheduledTask` ไม่ได้เชื่อข้อความที่สคริปต์พิมพ์:

| | |
|---|---|
| triggers | 2 — `MSFT_TaskLogonTrigger` + `MSFT_TaskTimeTrigger` |
| repetition | **`PT15M`** · `duration` ว่าง = ทำซ้ำไม่มีวันสิ้นสุด |
| action | `powershell.exe … -File "D:\WhizdomLift\scripts\start_captures.ps1" -Lifts 1,2,3,5` |
| รอบแรก | 22:48:48 → `result = 0` (ทุกลิฟต์รันอยู่แล้ว จึงไม่สตาร์ตซ้ำ) |
| **รอบที่สอง** | **23:03:03 → `result = 0` · missed runs 0** ⇒ repetition ยิงจริง ไม่ใช่แค่ตั้งค่าไว้ |

#### 🔴 ผลการรีบูตจริง 13 ก.ย. 2026 — **ล้มเหลว แล้วแก้แล้ว**

รีบูต 00:25:04 · **capture ไม่กลับมาเลยจนถึง 08:06 = หยุดไป 7 ชม. 41 นาที**
ขณะที่ task รันครบทุก 15 นาที `result=0` ทุกรอบ `missed=0`

สามสาเหตุอิสระซ้อนกัน — รายละเอียดอยู่ใน CLAUDE.md §6.19:

| # | สาเหตุ | แก้ด้วย |
|---|---|---|
| 1 | `-Lifts 1,2,3,5` ผ่าน `-File` กลายเป็นเลข **1235** (PowerShell อ่านจุลภาคเป็นตัวคั่นหลักพัน) | รับเป็น string แล้ว split เอง |
| 2 | โปรเซสของ task อ่าน `pyserial` ใน user profile ไม่ได้ → ตายที่ `import serial` **ก่อนเขียนล็อกบรรทัดแรก** | `pip install --target vendor pyserial` + bootstrap ในสคริปต์ |
| 3 | โปรเซสที่ task สตาร์ตซ่อน `CommandLine` → มอนิเตอร์บอก STOPPED ทั้งที่กำลังเก็บ และ watchdog เกือบสตาร์ตซ้อน | logger ทิ้ง `capture_lift_<n>.pid` ไว้ |

**ยืนยันหลังแก้** — หยุด capture ทั้งสี่ตัวอย่างสะอาด แล้วให้ **task เป็นคนกู้คืนเอง**:

```
09:05:13  already capturing: (none)
09:05:13  missing, will start: 1, 2, 3, 5
09:07:56  scan says: COM21=lift3 COM20=lift1 COM22=lift2 COM14=lift5
09:07:56  lift 1 started on COM20 as pid 11724
09:08:02  lift 5 started on COM14 as pid 9384
09:08:12  8s later - alive: 1,2,3,5  gone: none
```

⇒ `python capture_status.py` = **ALL CAPTURING** ครบสี่ตัว มี pid file ครบ

⏳ **ยังไม่ได้ยืนยันซ้ำ: `AtLogOn` หลังแก้** — ต้องรีบูตอีกครั้งถึงจะเห็น
สิ่งที่ยืนยันแล้วคือ **action ของ task กู้คืนได้จริงทั้งสี่ตัว** ซึ่งเป็นส่วนที่เคยพัง
ส่วน trigger เองยิงตรงเวลามาตลอด (`missed=0`)

---

#### ✅ ผลรีบูตรอบที่ 2 — **ผ่าน** (13 ก.ย. 2026)

เครื่องรีบูตและ login อัตโนมัติ — อ่านจาก `capture_launcher.log` ไม่ได้ดูแค่ `result`:

| เวลา | เหตุการณ์ | |
|---|---|---|
| 09:23:50 | boot | |
| 09:24:07 | logon (explorer เริ่ม) | |
| **09:24:24** | **task เริ่ม — 17 วิหลัง logon** นอกรอบ 15 นาที (รอบถัดไปคือ 09:33:33) ⇒ **`AtLogOn` ยิงจริง** | ✅ |
| 09:24:25 | `import serial` ผ่านทาง `vendor/` | ✅ |
| 09:27:09–15 | สตาร์ตครบ 1, 2, 3, 5 บนพอร์ตที่ถูก · `8s later - alive: 1,2,3,5  gone: none` | ✅ |
| 09:28 | `ALL CAPTURING` · pid file ครบ 4 และเขียนหลัง boot | ✅ |

`board_ms` **เดินต่อ ไม่รีเซ็ต** ทั้งสี่ตัว (restarts 0) — ต่างจาก baseline เกินเวลาจริงไม่เกิน 60 วิ
ซึ่งคือช่วง heartbeat (baseline เป็นบรรทัดข้อมูลสุดท้าย ไม่ใช่เวลาหยุด) ⇒ PC รีบูตไม่ได้แตะบอร์ด

⚠️ **ข้อเดียวที่ไม่ตรงเกณฑ์: capture กลับมา 3 นาที 3 วิหลัง logon ไม่ใช่ ~1 นาที**
ต้นเหตุคือ `port_resolver.py` ฟังทีละพอร์ต พอร์ตละ 40 วิเต็มแม้บอร์ดประกาศตัวแล้ว = **163 วิ**
**แก้แล้ว**: ฟังทุกพอร์ตพร้อมกัน + หยุดทันทีที่ได้บรรทัด `LIFT=` ครบบรรทัด
ทดสอบผ่าน task จริงหลังแก้: **สแกน 25 วิ** (09:31:36 → 09:32:01) · สตาร์ตครบใน 28 วิ · ALL CAPTURING

---

#### 🔄 รีบูตทดสอบรอบที่ 2 — 13 ก.ย. 2026 09:21 (หลังแก้ §6.19 ครบแล้ว)

baseline ก่อนรีบูต:

| | |
|---|---|
| เวลา | 2026-09-13 09:21:03 · boot เดิม 00:25:04 |
| task | lastRun **09:18:18** result 0 · nextRun 09:33:33 · missed 0 |
| หยุด capture | 09:21:06 ทั้งสี่ตัว `0 rejected` · **pid file ถูกลบครบ** = ปิดสะอาดจริง |
| `board_ms` สุดท้าย | lift 1 = **63,185,401** · lift 2 = **610,131,144** · lift 3 = **570,060,228** · lift 5 = **53,520,734** |

**สิ่งที่ต้องเห็นถ้า `AtLogOn` ทำงาน** — ต่างจากรอบแรกตรงที่คราวนี้มี `capture_launcher.log`
ให้อ่านว่าเกิดอะไรขึ้นจริง ๆ ไม่ต้องเดาจาก `result`:

1. 🥇 `capture_launcher.log` มีบรรทัด `--- run start ---` **ภายใน ~1 นาทีหลัง logon**
   ตามด้วย `missing, will start: 1, 2, 3, 5` และ `8s later - alive: 1,2,3,5  gone: none`
2. `capture_lift_*.log` มี `===== capture started` ใหม่ และมี `capture_lift_*.pid` ครบสี่ไฟล์
3. `python capture_status.py` = **ALL CAPTURING**
4. **`board_ms` ต้องเดินต่อจากค่าข้างบน ไม่ถอยกลับไป 89** — บอร์ดรับไฟจากตู้ลิฟต์ ไม่ใช่จาก USB

⚠️ ถ้าบรรทัดแรกของ launcher log โผล่ช้าเป็นสิบนาที แปลว่า **watchdog 15 นาทีกู้ให้ ไม่ใช่ logon trigger**
— สองอย่างนี้แยกกันด้วยเวลาเท่านั้น

---

#### 🔄 บันทึกตอนเริ่มทดสอบ — 13 ก.ย. 2026 00:20

หยุด capture ทั้งสี่ตัวอย่างสะอาดแล้วรีบูตเครื่อง **ผู้ใช้ต้อง logon หลังรีบูตถึงจะเห็นผล**
(task เป็น user-level `LogonType=Interactive` — ไม่ logon = ไม่มีอะไรทำงาน ทั้ง logon trigger
และ watchdog 15 นาที)

**baseline ก่อนรีบูต** — ใช้เทียบหลังเครื่องกลับมา:

| | |
|---|---|
| เวลา | 2026-09-13 00:19:30 · uptime เครื่อง 6 วัน 7 ชม. · ไม่มี pending update |
| task | lastRun **00:18:18** result 0 · nextRun **00:33:33** · missed 0 |
| หยุด capture | 00:19:56 ทั้งสี่ตัว `0 rejected` (สะอาด ไม่ใช่ crash) |
| `board_ms` สุดท้าย | lift 1 = **30,924,406** · lift 2 = **577,938,829** · lift 3 = **537,803,730** · lift 5 = **21,246,384** |

**สิ่งที่ต้องเห็นถ้า `AtLogOn` ทำงาน** (เรียงตามความน่าเชื่อถือ):

1. 🥇 **`capture_lift_*.log` มีบรรทัด `===== capture started` ใหม่ ภายใน ~1 นาทีหลัง logon**
   — ไม่มีอะไรอื่นในเครื่องนี้ที่สตาร์ต capture ได้ ⇒ นี่คือหลักฐานตรงที่สุด
   *ถ้ามาช้าเป็นสิบนาที* แปลว่า **watchdog 15 นาทีต่างหากที่สตาร์ตให้ ไม่ใช่ logon trigger*
   — สองอย่างนี้แยกกันด้วย **เวลา** เท่านั้น อย่าสรุปรวม
2. `Get-ScheduledTaskInfo` → `LastRunTime` เป็นเวลาหลัง logon
3. **`board_ms` ต้องเดินต่อ ไม่ถอยกลับไป 89** — บอร์ดรับไฟจากตู้ลิฟต์ ไม่ใช่จาก USB
   ⇒ PC รีบูตต้องไม่ทำให้บอร์ดรีเซ็ต ถ้า `board_ms` ถอย แปลว่ามีอย่างอื่นเกิดขึ้นที่ต้องอธิบาย

ตรวจทั้งหมดทีเดียวด้วย:

```
cd D:\WhizdomLift
python capture_status.py
powershell -NoProfile -Command "Get-ScheduledTaskInfo -TaskName 'WhizdomLift captures' | Format-List LastRunTime,LastTaskResult,NextRunTime,NumberOfMissedRuns"
```

⚠️ **ถ้ากลับมาแล้วไม่มีอะไรเก็บเลย** สตาร์ตมือได้ทันทีโดยไม่ต้องรอแก้อะไร:

```
powershell -NoProfile -ExecutionPolicy Bypass -File D:\WhizdomLift\scripts\start_captures.ps1
```

`CAPTURING` ครบทั้ง 4 ตัว = ปกติ · `STALE` = โปรเซสอยู่แต่ไฟล์ไม่โต · `STOPPED` = ตายแล้ว
· `DEGRADED` = บอร์ดผิดตัว / พอร์ตไม่ตรงหัวไฟล์ / ไม่มี beacon

### การทดสอบที่ต้องทำเมื่อกลับไป — ปิดคำถามรหัส 47 ของ Lift 5

**ห้ามทดสอบแค่ชั้น 7 กับ 19** เพราะตอบได้แค่ครึ่งเดียว ให้จอด Lift 5 ทีละชั้น **ชั้นละ ~10 วินาที
พร้อมจดเวลา** ตามลำดับนี้ แล้วส่งเวลามาให้ — ระบบจะเทียบรหัสที่อ่านได้กับตารางอ้างอิงของลิฟต์ 1–3

| จอดที่ชั้น | รหัสอ้างอิง (ลิฟต์ 1–3) | ถ้า Lift 5 อ่านได้เท่านี้ | ถ้าอ่านได้ +1 |
|---|---|---|---|
| 2 | 3 | ระดับที่เพิ่มอยู่**เหนือ**ชั้น 2 | อยู่ระหว่าง B1–2 |
| 9 | 12 | อยู่เหนือชั้น 9 | อยู่ระหว่าง 2–9 |
| **20** | **22** | อยู่เหนือชั้น 20 | อยู่ระหว่าง 9–20 ← **น่าจะเป็นชั้น 19 ที่ลิฟต์โดยสารข้าม** |
| 33 | 35 | อยู่เหนือชั้น 33 | อยู่ระหว่าง 20–33 |
| 44 | 46 | ชั้น 44 = รหัส 46 ⇒ **รหัส 47 คือระดับเหนือชั้น 44** | ชั้น 44 = รหัส 47 ⇒ **มีชานจอดแทรกอยู่ข้างล่าง** |

ชั้น 20 ตัวเดียวแบ่งครึ่งคำตอบได้ทันที ถ้าเวลาจำกัดให้ทำชั้น 20 กับชั้น 44 ก่อน

ถ้าเป็นไปได้ ให้ลองกดชั้น **7** และ **19** ด้วย (ลิฟต์โดยสารข้ามสองชั้นนี้ — §3.4)
ถ้า Lift 5 จอดได้จริงก็เป็นคำอธิบายของระดับที่เพิ่มมาโดยตรง

---

## Lift 1 VS2 — ขั้นตอนแยกว่าบอร์ดรีเลย์เสียหรือสาย input ขาด

ดู [LIFT1_VS2_RELAY_TEST.md](LIFT1_VS2_RELAY_TEST.md) — เรียงจากเสี่ยงน้อยไปมาก
ข้อ 1 (ดู LED ขณะรถวิ่ง) ตอบคำถามได้โดยไม่ต้องแตะอะไรเลย

---

## แผนที่ขาอ้างอิง (จาก Lift 3)

| ขา | สัญญาณ | | ขา | สัญญาณ |
|---|---|---|---|---|
| D24 | VS2 — bit 0 (น้ำหนัก 1) | | D16 | RUNNING 运行 |
| D25 | VS3 — bit 1 (2) | | D17 | SAFETY 安全 (ปิดตลอด) |
| D26 | VS4 — bit 2 (4) | | D19 | UP 上行 |
| D27 | VS5 — bit 3 (8) | | D20 | DN 下行 |
| D28 | VS6 — bit 4 (16) | | | |
| D29 | VS7 — bit 5 (32) | | | |

`รหัส = 32·VS7 + 16·VS6 + 8·VS5 + 4·VS4 + 2·VS3 + 1·VS2`  และ  `ชั้น 44 = รหัส 46`

⚠️ **ห้ามตั้งขาใดเป็น OUTPUT** — ขาต่อกับหน้าสัมผัสรีเลย์ ขับ HIGH ใส่หน้าสัมผัสที่ปิดอยู่ = ไฟช็อต พอร์ตพัง
